# MusicDialog Electron Parity Matrix

Truth source:

- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MusicDialog.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MediaControl.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MiniModePage.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MusicInfoControl.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MusicLyricsControl.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MusicAlbumArtControl.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/AlbumArtLibraryPickerDialog.tsx`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/hooks/useMusicDialogShortcuts.ts`
- Electron: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/state/useUndoableNotificationStore.ts`
- Electron CSS: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/index.css`
- Electron CSS: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/commandbar.css`
- Electron CSS: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`

## Current Functional Audit

| Area | Electron evidence | Flutter evidence | Status |
| --- | --- | --- | --- |
| Lyrics close confirmation | `MusicDialog.tsx` `requestClose` closes immediately when `!lyricsDirty`; dirty lyrics call shared `requestConfirmDialog({ title: common.confirm, message: song.discardLyricsConfirm })`, which renders Electron `DialogHost`/`RemoveDialog` with `.input-dialog-overlay`, `.input-dialog.remove-dialog`, `width: min(480px, calc(100vw - 48px))`, `24px` padding, `--border-subtle`, `--panel-shadow` in light mode, and the night override `0 24px 60px rgba(0,0,0,.42)`. `.remove-dialog-message` uses `font-size:15px`, `line-height:1.55`, centered normal-weight text. `.input-dialog-actions` centers actions with `gap:18px` and `margin-top:20px`; `.input-dialog-actions button` uses `min-width:88px`, `min-height:36px`, `border:0`, `border-radius:8px`, `background:transparent`, `color:var(--accent-strong)`, `font:inherit`, and `font-weight:700`. `DialogHost.closeConfirmDialog(false)` resolves Cancel without closing MusicDialog; Confirm resolves true and closes. Electron `App.tsx` routes back/Escape through `closeTopPopupDialog()`, which invokes the top PopupDialog close handler before route navigation. `DialogHost` renders the secondary `RemoveDialog` directly rather than as a `PopupDialog`, while `PopupDialog.tsx` registers only the parent dialog through `addPopupDialogCloseHandler`; therefore pressing Escape again while the confirm is open calls the parent close handler again, keeps the existing confirm open, and does not close the parent. Electron runtime capture opens this secondary dialog through the real dirty-lyrics close path and logs `.input-dialog` rect `{x:360,y:362.625,width:480,height:174.75}` plus computed `.input-dialog` style in light/dark mode. | `music_dialog.dart` `_requestClose` directly calls `onClose` when lyrics are clean; dirty lyrics call `_confirmDiscardLyricsAndClose`, which uses `showPopupConfirmDialog`; `_InputDialogShell` uses Electron input-dialog border/shadow instead of the main PopupDialog chrome. `_discardLyricsConfirmOpen` mirrors Electron's single-confirm behavior by ignoring repeated close requests while the secondary confirm is already open, so a second Escape keeps the confirm visible and does not close MusicDialog. `showPopupConfirmDialog` keeps the Electron `.remove-dialog-message` 15px/1.55 centered normal-weight text, and `PopupDialogActionButton` keeps Electron inherited 16px dialog font and 700 action weight for confirm actions. Flutter runtime capture opens this secondary dialog through the real dirty-lyrics close path and now logs `popup-input-dialog-surface` rect plus decoration: light surface `0xfafbfcff`, border `0x2e768499`, shadow `0x2435495f` with `blurRadius=70` and `offsetY=26`; dark surface `0xfa161c24`, border `0x1fd6e0ec`, shadow `0x6b000000` with `blurRadius=60` and `offsetY=24`. Covered by `Popup confirm uses Electron input-dialog chrome`, `MusicDialog clean lyrics close skips confirm like Electron`, `MusicDialog clean lyrics Escape closes like Electron stack`, `MusicDialog close confirm uses PopupConfirm like Electron` including Cancel-not-close and Confirm-close branches, `MusicDialog dirty lyrics Escape opens one confirm like Electron stack`, and runtime screenshots for `lyrics_discard_confirm_light` and `lyrics_discard_confirm_dark`. | Confirmed aligned for source-backed behavior, clean/dirty Escape stack behavior, repeated-Escape single-confirm behavior, chrome, message typography, action typography, and runtime secondary-dialog screenshot evidence. Current dirty-confirm full dialog crop remains visually different in light mode because the parent Flutter PopupDialog backdrop is the user-requested liquid-glass overlay while Electron's parent `.song-dialog-overlay` is plain CSS blur; this is classified as shared backdrop rendering, not a MusicDialog dirty-confirm source diff. |
| Lyrics no-lyrics placeholder | `MusicLyricsControl.tsx` renders the textarea for any non-loading lyrics state and sets the placeholder only when `lyrics?.source === 'none'` with `t('nowPlaying.noLyrics')`; `en-US.json` maps that key to `No lyrics found`. Runtime `lyrics-empty` capture logs the same Lyrics tab, Search/Import/Save commandbar, no timestamp toggle, and textarea rect `{x:239,y:217,width:722,height:568}`. | `music_lyrics_control.dart` renders the same non-loading textarea and sets `hintText` only when `lyrics?.source == LyricsSource.none`; the visual harness now captures `lyrics_empty_light` with `LyricsSource.none`, `No lyrics found`, no timestamp toggle, Save visible, and the same textarea rect/style as normal lyrics. | Confirmed aligned for layout, placeholder text, commandbar membership, and empty-lyrics visual state. Fixed crop comparison `/tmp/smplayer_music_dialog_compare_current_lyrics_empty_light_cropped.png` is `cropped_mean_abs_rgb=1.49`, `cropped_over_16=1.66%`; residual is font/antialiasing/backdrop. |
| Album-art source flyout | `MusicAlbumArtControl.tsx` `AlbumArtEditorControl` opens dialog-layer `MenuFlyout` at button bottom + 6 with `local` (`pictures`) and `library` (`musicLibrary`) actions. Electron `menus-actions.css` gives `.library-context-menu` `min-width: 206px`, `max-width: min(280px, calc(100vw - 16px))`, 6px padding, 1px border, 10px radius, and 34px item height; runtime `album-art-source-menu` logs Change Artwork `{x:589.765625,y:155,width:165.5625,height:40}`, menu `{x:589.765625,y:201,width:206,height:82}`, item buttons `{x:596.765625,y:208,width:192,height:34}` / `{x:596.765625,y:242,width:192,height:34}`, and artwork `{x:430,y:331,width:340,height:340}`. | `_ArtworkSourceButton` opens project `MenuFlyout` with matching local/library items and icon names, now explicitly positions it at button bottom + 6. Shared `MenuFlyout` now uses Electron's `206..280` intrinsic width behavior and border-box height for non-scrollable menus while keeping scrollable flyouts constrained. Refreshed Flutter runtime `album_art_source_menu_light` logs artwork `{x:430,y:331,width:340,height:340}`, Change Artwork `{x:603.6146469116211,y:155,width:157.0064468383789,height:40}`, menu `{x:600.6146469116211,y:201,width:206,height:82}`, and the same source menu state for `album_artwork_source_menu_light`. Covered by `MusicDialog album art source menu mirrors Electron flyout`, which asserts the +6 offset. | Confirmed aligned for source-backed entries, icon names, artwork geometry, dialog-layer source menu positioning, menu width, and menu border-box height. Pixel identity is not claimed because the remaining source-menu crop residual is shared MenuFlyout liquid-glass/text rasterization plus the already-classified Flutter/Electron text measurement difference that makes the Change Artwork button about 8.6px narrower while retaining Electron CSS parameters (`gap:8`, `padding:14`, icon 20, margin 3). |
| Album-art local picker flow | `MusicDialog.tsx` `changeArtwork` calls `pickSongArtworkSource()`, ignores canceled results, shows `song.updateFailed` for `error`, shows `song.musicNoAlbumArt` with `sourceName` for `no-artwork`, and on success sets `artworkUrl`, `artworkSourcePath`, `artworkMissing(false)`, clears `artworkRecommendation`, and hides delete confirmation. Electron main-process `pickArtworkSource` uses native title `song.chooseAlbumArtwork`, image extensions `jpg/png/jpeg/webp/bmp`, shared audio extensions `aac/aiff/alac/ape/flac/m4a/mp3/ogg/opus/wav/wma`, and derives `sourceName` from the selected file basename without extension. | Flutter `_changeArtwork` uses the same local picker title/extensions, derives the no-artwork message title from `basenameWithoutExtension`, sets `_displayArtworkUrl/_artworkSourcePath`, clears missing state/recommendation/delete confirmation on success, and saves the prepared source path through `_saveArtwork`. | Confirmed aligned by `MusicDialog local artwork picker mirrors Electron filters`, `MusicDialog local artwork no-artwork notice uses source basename`, and `MusicDialog local artwork picker saves prepared source like Electron`. |
| Album-art library picker dialog | `AlbumArtLibraryPickerDialog.tsx` ranks source songs from raw title/album/artists search text, uses `getSongArtists()` so unknown artists participate as `Unknown artist`, compares raw `song.album === albumName`, uses sidebar recent-search history with a `dropdown-dismiss-layer`, first 320 candidate snapshots, first 160 searched choices, Enter apply, Space select, double-click apply, and text-only loading/empty messages. | `_AlbumArtLibraryPickerDialog` mirrors those MusicDialog picker rules; covered by picker history/dismiss/search/loading/failure/stale-load/raw-album/Space/Enter/double-click/chrome/mobile tests. | Confirmed aligned for MusicDialog entry. |
| Album-art library picker search icon | Electron `SearchField.tsx` renders the shared project `<Icon name="search" />` inside `.search-commit-button`; runtime Electron screenshot shows a search glyph in the AlbumArtLibraryPicker search box. | Flutter's shared `PageSearchField` default uses the existing Fluent font icon for other pages, but `AlbumArtLibraryPickerDialog` now passes the Electron SVG search icon explicitly so the picker no longer renders the square missing-glyph seen in the Flutter runtime screenshot. | Confirmed aligned by `MusicDialog library artwork picker uses Electron chrome`; the shared search-field default remains unchanged for paused Albums/Artists search areas. |
| Album-art library picker artwork surface | Electron `AlbumArtControl.tsx` renders loaded artwork as `img.album-art-control`; global `album-tile.css` gives `.album-art-control` `background: var(--surface-artwork)` and `box-shadow: var(--artwork-shadow)`, while `song-dialog.css` explicitly removes source-list image shadows with `.album-art-library-picker-list .album-art-library-picker-artwork { box-shadow: none; }` and keeps the preview on the global artwork surface. Runtime picker screenshots show this background behind transparent PNG artwork. | Flutter `_ArtworkImage` previously clipped `Image.file` directly, so transparent PNGs showed the dialog body through the artwork; the first shared `_ArtworkImage` pass also applied preview shadow to source-list images. `_ArtworkImage` now applies the Electron artwork surface, keeps no shadow for source-list choices, and keeps the 8px/18px artwork shadow for preview artwork. | Confirmed aligned by `MusicDialog library artwork picker uses Electron chrome` and the refreshed picker visual harness. |
| Album-art library picker selected state tokens | Electron `song-dialog.css` gives `.album-art-library-picker-source-item:hover`, `:focus-visible`, and `.is-selected` the same desktop style: `border-color: rgba(var(--accent-rgb), 0.44)`, `background: rgba(var(--accent-rgb), 0.12)`, and `box-shadow: none`. Its mobile override uses `background: rgba(var(--accent-rgb), 0.08)` plus `0 8px 18px rgba(29, 42, 60, 0.1)` and an inset `1px rgba(var(--accent-rgb), 0.16)` ring. | Flutter `_AlbumArtChoiceTile` derives the highlighted state from selected/hover/focus and now reads those selected-state color/shadow parameters from `GlobalUI.sourceListSelectedBgColor`, `GlobalUI.sourceListSelectedBorderColor`, `GlobalUI.sourceListSelectedShadow`, `GlobalUI.sourceListMobileSelectedBgColor`, and `GlobalUI.sourceListMobileSelectedShadow` instead of local component constants. | Confirmed aligned for token reuse and Electron-backed selected/hover/focus styling by `MusicDialog library artwork picker uses Electron chrome` and `MusicDialog library artwork picker mobile mirrors Electron list`. |
| This continuation evidence | Refreshed Electron runtime with `node tool/electron_music_dialog_visual_verify.mjs` and Flutter runtime with `flutter test --no-pub test/music_dialog_visual_verify_test.dart --plain-name "writes MusicDialog runtime verification screenshots"`. Re-audited Electron mobile CSS `song-dialog.css` for `.album-art-library-picker-source-item.is-selected`: mobile keeps `border: 0`, `background: rgba(var(--accent-rgb), 0.08)`, and the visible ring is the inset shadow, not a real border. | Re-audited Flutter `_AlbumArtChoiceTile`: desktop selected state uses `GlobalUI.sourceListSelectedBgColor`, `GlobalUI.sourceListSelectedBorderColor`, and `GlobalUI.sourceListSelectedShadow`; mobile selected state uses `GlobalUI.sourceListMobileSelectedBgColor` alpha-blended over the PopupDialog surface and `GlobalUI.sourceListMobileSelectedShadow`, with `border == null` as asserted by `MusicDialog library artwork picker mobile mirrors Electron list`. | Confirmed no product patch for selected UI in this pass. The user-requested selected color/shadow parameters are already centralized in `GlobalUI`; adding a mobile entity border would contradict Electron's `border: 0` CSS, so it remains unmodified. |
| Lyrics saving mobile visual evidence | Electron `MusicLyricsControl.tsx` renders `.music-info-save-progress.SaveProgress` inside the same `CommandBar` on desktop and mobile. Runtime mobile `lyrics-saving` captures confirm `song-dialog.css` mobile layout: commandbar `{x:0,y:94,width:640,height:60}`, textarea `{x:12,y:154,width:616,height:638}`, disabled Save button `{x:255.859375,y:98,width:82.1484375,height:40}`, and `.SaveProgress` `{x:628,y:96,width:0,height:3}` in both light and dark, so there is no visible full-width saving stripe on mobile either. | Flutter visual harness now captures `lyrics_saving_mobile_light` and `lyrics_saving_mobile_dark` at `640x820` after tapping Save against a never-resolving `saveSongLyrics` completer. Runtime screenshots are `/tmp/smplayer_music_dialog_lyrics_saving_mobile_light.png` and `/tmp/smplayer_music_dialog_lyrics_saving_mobile_dark.png`; Electron screenshots are `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_music_dialog_lyrics_saving_mobile_light.png` and `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_music_dialog_lyrics_saving_mobile_dark.png`. | Confirmed aligned for mobile saving layout and invisible progress geometry; fixed-rect comparison results are light `cropped_mean_abs_rgb=3.26`, `cropped_over_16=1.77%`; dark `cropped_mean_abs_rgb=3.14`, `cropped_over_16=1.93%`. No product patch was needed in this pass. |
| This continuation evidence | Refreshed album-related fixed-crop comparisons from the current Electron and Flutter screenshots: MusicDialog album art light `cropped_mean_abs_rgb=2.75`, recommendation light `3.02`, source menu light `3.63`, delete confirm light `3.17`; AlbumArtworkDialog light `2.57`, dark `1.99`, source menu light `3.45`, delete confirm light `2.97`, mobile light `2.56`; AlbumArtLibraryPicker light `3.19`, dark `3.27`, history light `5.10`, empty light `2.50`, mobile light `2.54`, mobile empty light `1.95`. | Comparison artifacts were written to `/tmp/smplayer_music_dialog_compare_current_album_art_*_goal*.png`, `/tmp/smplayer_music_dialog_compare_current_album_artwork_*_goal*.png`, and `/tmp/smplayer_music_dialog_compare_current_album_art_library_picker_*_goal*.png` plus `_cropped.png` variants. Final targeted validation passed: `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`, `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`, `flutter test --no-pub test/music_dialog_test.dart --plain-name "album art"`, `flutter test --no-pub test/music_dialog_test.dart --plain-name "AlbumArtworkDialog"`, and `flutter test --no-pub test/music_dialog_test.dart --plain-name "library artwork picker"`. | This was the pre-fix evidence that identified the picker history panel as the largest album-related residual. A later Electron CSS panel pass fixed that confirmed source-backed surface mismatch for AlbumArtLibraryPicker only; see `Album-art library picker search-history panel surface`. The first parallel `album art` run failed from a transient missing `build/native_assets/macos/objective_c.dylib` signing artifact while other parallel Flutter tests used the same build tree; the same test group passed when rerun alone. |
| Album-art library picker search-history panel surface | Electron `SearchHistoryPanel.tsx` renders `.search-history-panel` inside the picker search shell. `sidebar-search.css` defines the panel as `padding: 8px`, `border: 1px solid rgba(83,99,121,.14)`, `border-radius: 14px`, `background: rgba(244,246,249,.96)`, `box-shadow: 0 18px 36px rgba(53,73,95,.16)`, and `backdrop-filter: blur(18px)`; night mode uses `background: rgba(29,35,43,.98)`, `border-color: var(--night-border)`, and `0 18px 36px rgba(0,0,0,.36)`. Electron runtime logs the picker history panel geometry as `{x:199,y:239,width:802,height:92}` with item/select/remove rects matching the CSS box model. | Flutter `SearchHistoryPanel` now keeps the default `GlassContainer` surface for normal page search panels, but exposes `useElectronPanelStyle` for picker-only use. `AlbumArtLibraryPickerDialog` enables that flag, so only the MusicDialog album-art picker history panel uses Electron's CSS background, border, shadow, radius, and blur parameters. `includeBorderInset` still preserves the Electron border-box text/item rect alignment. | Confirmed aligned for the album-art picker history panel surface without modifying paused Albums/Artists page search surfaces. Covered by `MusicDialog library artwork picker mirrors search history`, `MusicDialog library artwork picker uses Electron chrome`, `library artwork picker`, and `AlbumsPage search history overlays without moving grid` / `AlbumsPage clears only visible album search history` to prove the default GlassContainer path remains intact. Refreshed crop metrics improved from history light `cropped_mean_abs_rgb=5.10`, `cropped_over_16=14.62%` to `3.69`, `6.33%`; history dark is `4.59`, `10.16%`. |
| Album-art library picker empty preview column | Electron `AlbumArtLibraryPickerDialog.tsx` always renders `<aside className="album-art-library-picker-preview">` on desktop, even when `selectedChoice` is null and the aside has no children. The mobile CSS switches picker content to a single column and removes the preview column. | Flutter previously reserved `PreviewColumn` width but only keyed/logged `AlbumArtLibraryPicker.Preview` when a choice existed, so empty desktop runtime evidence could not prove the Electron aside. Flutter now keeps the desktop preview container mounted and empty when no choice is selected, while mobile still omits it. | Confirmed aligned by `MusicDialog library artwork picker handles snapshot failure`, which asserts desktop empty preview rect `{x:761,y:247,width:240,height:460}` with no preview artwork child, plus refreshed visual harness logs: `album_art_library_picker_empty_light` preview `{x:761,y:247,width:240,height:460}` and `album_art_library_picker_empty_mobile_light` preview `null`. |
| Album-art library picker keyboard scope | `AlbumArtLibraryPickerDialog.tsx` handles Enter/Space only on each `role="option"` source item `onKeyDown`; the dialog itself does not apply the first choice on global Enter, and source items have `tabIndex={0}` but no autofocus. | Flutter previously handled Enter/Space at the picker root and autofocused the selected source tile. It now leaves root Enter ignored and keeps Enter/Space behavior on focused choice tiles only; covered by `MusicDialog library artwork picker ignores global Enter like Electron`, `MusicDialog library artwork picker Enter applies choice`, and `MusicDialog library artwork picker Space selects without apply`. | Confirmed aligned. |
| Nested album-art picker close stack | `PopupDialog.tsx` registers each dialog instance with `addPopupDialogCloseHandler(() => onCloseRef.current())`; `MusicDialog.tsx` and `AlbumArtworkDialog.tsx` render `AlbumArtLibraryPickerDialog` as a second `PopupDialog` after the parent dialog when `libraryArtworkPickerOpen` is true, so top-level close/Escape behavior must close the picker before the parent. | `PopupDialog` now installs a top-stack `HardwareKeyboard` Escape handler in addition to its focused handler; only the handler at `_popupDialogCloseHandlers.lastOrNull` closes. Covered by `MusicDialog library artwork picker closes before parent dialog`, `AlbumArtworkDialog library artwork picker closes before parent dialog`, `PopupDialog closes only the top dialog from stack`, and `PopupDialog closes top dialog on Escape`. | Confirmed aligned for both MusicDialog and AlbumArtworkDialog secondary picker entries. |
| Album-art delete confirmation | `MusicAlbumArtControl.tsx` renders inline `.song-dialog-warning RemoveAlbumArtWarningPanel` with `width: min(360px, 100%)`, 12px padding, 8px gap, 8px radius, warning border/background colors, inherited 16px/400 warning text, and generic `.song-dialog button` Yes/Cancel buttons disabled by `controlsDisabled` (`loading || saving`). Electron runtime logs warning rect `{x:420,y:623,width:360,height:114}`, artwork rect `{x:430,y:265,width:340,height:340}`, and Yes/Cancel at y `636`/`684` with 40px height. | `_ArtworkDeleteConfirm` now renders the same fixed 360px warning panel when space allows, keeps the Electron warning colors/radius/border/padding/gap, sets warning text to 16px/400, and disables Yes/Cancel while album-art loading/saving is active. Flutter runtime logs warning rect `{x:420,y:623,width:360,height:114}`, artwork rect `{x:430,y:265,width:340,height:340}`, and matching button y/height; remaining button/text width deltas are font measurement. Covered by `MusicDialog album art delete uses inline confirmation`, `MusicDialog album art delete confirmation disables while saving`, and `album_art_delete_confirm_light` visual evidence. | Confirmed aligned. |
| Missing-artwork recommendation effect | `MusicDialog.tsx` computes `albumArtRecommendationCandidates` via `useMemo` and requests `getSongArtworkSnapshots(candidateSongIds)` only from the `useEffect` dependency changes `[albumArtRecommendationCandidates, artworkMissing, t]`; no per-frame retry occurs when a stable candidate set has no usable artwork. | Before this audit, `music_dialog.dart` scheduled `_loadArtworkRecommendation()` from `build` whenever `_artworkMissing && recommendation == null`, so a stable candidate set with no usable snapshot could repeatedly request snapshots. | Confirmed difference; fixed in this pass with a recommendation request key matching the stable source-song dependency set. |
| Same-song mode prop updates | `MusicDialog.tsx` initializes `activeMode` from `mode`, then mirrors later mode prop changes with `useEffect(() => setActiveMode(mode), [mode])`; the data load/reset effects still depend on `song.id`, not `mode`, so same-song mode updates switch tab without reloading properties/lyrics/artwork. | Flutter previously reset `_mode` and reloaded when `initialMode` changed for the same song. It now switches `_mode` on `initialMode` changes, but only reloads when `song.id` changes; covered by `MusicDialog switches same-song mode without reloading like Electron`. | Confirmed aligned. |
| Properties load failure | `MusicDialog.tsx` starts properties loading, awaits `window.smplayer?.getSongProperties(song.id).then(...)`, and has no `.catch()`, so a rejected properties request leaves the properties page loading and does not show `song.updateFailed`. | Flutter previously caught `getSongProperties` failures, stopped loading, and showed `song.updateFailed`. It now leaves loading active and shows no update-failed notification on properties-load rejection; covered by `MusicDialog keeps property loading state on property load failure`. | Confirmed aligned. |
| Album-art reset recommendation state | `MusicDialog.tsx` `resetArtwork` restores `originalArtworkUrl`, clears `artworkSourcePath`, restores `originalArtworkMissing`, sets `artworkRecommendation(null)`, hides delete confirmation, and shows `song.albumArtReset`; when the restored state is missing, the separate recommendation effect may load a fresh recommendation afterward. | Flutter previously restored the original artwork state but left the old `_artworkRecommendation` object alive. It now clears the recommendation immediately on reset and then allows the next recommendation request to repopulate it; covered by `MusicDialog reset clears album art recommendation like Electron`. | Confirmed aligned. |
| Album-art apply recommendation state | `MusicDialog.tsx` `applyAlbumArtRecommendation` sets `artworkMissing(false)`, and the recommendation `useEffect` immediately clears `artworkRecommendation` whenever `!artworkMissing`; the recommendation is no longer retained after applying it. | `_applyAlbumArtRecommendation` now clears `_artworkRecommendation` when switching `_artworkMissing` false while keeping `_artworkSourcePath` for Save. Covered by `MusicDialog applies recommended library album art`. | Confirmed aligned. |
| Album-art save without pending artwork | `MusicDialog.tsx` `saveArtwork` checks `if (!artworkSourcePath) { return }`, so clicking Save on the Album Art page before choosing/applying artwork is a silent no-op: no `saveSongArtwork`, no `onSaved`, and no notification such as `song.nothingChanged`. | Flutter `_saveArtwork` has the same empty `_artworkSourcePath` branch and returns before repository save, `_notifySaved`, or `_showMessage`. | Confirmed aligned by `MusicDialog save without pending album art is silent like Electron`, plus adjacent save-path reruns `MusicDialog applies recommended library album art` and `MusicDialog saved artwork keeps Electron display source URL`. |
| Missing-artwork recommendation fallback visual | `MusicAlbumArtControl.tsx` renders the missing-artwork recommendation as `.album-art-recommendation` inside the `NoAlbumArtTextBlock` fallback. Electron CSS gives the recommendation root `display:grid`, `gap:8px`, `line-height:1.35`, while the later `.song-dialog-artwork .album-art-control-fallback span` cascade makes the nested text line `overflow:hidden`, `text-overflow:ellipsis`, and `white-space:nowrap`. Runtime logs still show the inner button hit target at `{x:546.5390625,y:711,width:281.4921875,height:40}` and preview `{x:738.7734375,y:623,width:128,height:128}`, but the screenshot proves the visible row is the outer single-line ellipsis (`Smart match: use Artist's ...`), not a separately visible title button. The same runtime logs fallback shell `{x:350,y:251,width:500,height:500}`, default artwork root `{x:350,y:251,width:500,height:420.40625}`, default logo `{x:430,y:318.265625,width:340,height:285.875}`, and recommendation row `{x:350,y:681.40625,width:500,height:69.59375}`. | Flutter now keeps the fallback shell at `500x500`, applies the Electron cascade result for default artwork root/logo (`500x420.40625`, logo `340x285.87625`), keeps the recommendation area at `500x70`, renders a single visible ellipsized recommendation line instead of a standalone visible title button, and keeps the transparent apply/hover hit target plus preview at the Electron runtime rects. Flutter visual harness logs `albumArtDefaultArtwork {x:350,y:251,width:500,height:420.40625}`, `albumArtDefaultArtworkLogo {x:430,y:318.265,width:340,height:285.87625}`, `albumArtRecommendation {x:350,y:681,width:500,height:70}`, hit target `{x:546.5390625,y:711,width:281.4921875,height:40}`, and preview `{x:738.7734375,y:623,width:128,height:128}`. Covered by `MusicDialog album art loading uses Electron cover shell`, `MusicDialog applies recommended library album art`, `MusicDialog album art recommendation hover shows preview`, and `flutter test test/music_dialog_visual_verify_test.dart`. | Confirmed aligned for fallback shell geometry, Electron default-artwork cascade geometry, visible recommendation ellipsis, hit-target rect, preview size/position, hover preview, and apply behavior. Latest fixed crop comparison improved to `cropped_mean_abs_rgb=3.02`, `cropped_over_16=3.69%`. |
| Album-art loading with existing artwork | Electron `AlbumArtEditorControl` computes `imageLoading = loading || (artworkUrl !== '' && loadedArtworkUrl !== artworkUrl)` and renders `AlbumArtControl` whenever `artworkUrl || !loading`; when both `loading` and `artworkUrl` are true, the existing artwork shell remains mounted, `.song-dialog-artwork-shell.is-loading .album-art-control { opacity: 0; }` hides the image, and the loading overlay is shown on top. | Flutter previously replaced the whole artwork body with `AlbumArtLoadingShell` whenever `loading` was true, hiding an existing artwork path during loading. `AlbumArtEditorControl` now only shows the pure loading shell when loading and no artwork file is available; with an existing artwork file it keeps `AlbumArtworkImageShell` mounted, forces the loading overlay, and sets the image opacity to 0 while loading. Covered by `AlbumArtEditorControl keeps artwork shell while loading`. | Confirmed aligned. |
| Properties inline actions | `song-dialog.css` lays property rows out as label/control grid, inline controls as `minmax(0, 1fr) auto` with 8px gap, input height 42px, and MusicInfo mobile keeps inline controls in two columns. `MusicInfoControl.tsx` renders Clear Play Count beside the play-count input and gives the play-count input an Electron `title` from `formatSongPlayCountTitle`; user-requested Flutter overrides add icon-only actions for syncing the title to the filename, clearing play count, and revealing the path. | Flutter constrains property inline action buttons to the same 42px row height as the field, keeps path/reveal square, restores the title `refresh` icon tooltip `song.syncTitleToFilename`, restores the play-count `undo` icon tooltip `song.resetPlayCountToZero`, and keeps play-count tooltip text on the read-only field. Covered by `MusicDialog inline property actions keep Electron row layout`, `MusicDialog title can use filename when it differs`, `MusicDialog clear play count does not enter saving state`, and `MusicDialog shows zero play count without clear action`. | Confirmed aligned for Electron layout/tooltip behavior; icon-only action presentation is a documented user override, not claimed as Electron-identical presentation. |
| Artist field inner padding | `song-dialog.css` sets `.song-dialog-artist-cell .artist-text-box { padding-right: 34px; }` so the overlaid remove button does not cover artist text while ordinary dialog text boxes keep their default 12px horizontal padding. | `_ArtistFieldGrid` now passes `EdgeInsets.fromLTRB(12, 0, 34, 0)` only to artist `_DialogField`s, while default `_DialogField` padding remains `EdgeInsets.symmetric(horizontal: 12)`. Covered by `MusicDialog resolves night mode colors for dialog content`. | Confirmed aligned. |
| Artist row height | `MusicInfoControl.tsx` renders the artist editor as `.song-dialog-artist-editor` with a 40px add button beside 42px inputs, and runtime Electron row/control rects keep the Artist property row at 42px; the full properties grid is 18 rows at 42px with 17 gaps at 10px (`926px`). | `_MusicDialogIconButton` now uses `MaterialTapTargetSize.shrinkWrap`, so the artist add button no longer expands the Flutter row to 48px. Covered by `MusicDialog resolves night mode colors for dialog content` and visual harness row rect logs. | Confirmed aligned. |
| Show in Explorer presentation | Electron renders `Show in Explorer` as a text button in the path inline field. | User explicitly requested this text be changed to icon + tooltip; Flutter now renders a folder icon button with `song.showInExplorer` tooltip while preserving the same reveal callback. | User override applied; not claimed as Electron-identical presentation. |
| Shared album-level artwork editor/picker reuse | Electron opens `AlbumArtLibraryPickerDialog` from `AlbumArtworkDialog.tsx`, passing `albumName`, `currentSong={librarySongs.find(song.id === songId)}`, and `songs={librarySongs}`. The album artwork dialog uses the same `AlbumArtEditorControl` as MusicDialog, so the commandbar, busy progress, artwork body/shell, source flyout, and inline delete warning share layout. Runtime screenshot evidence shows Electron AlbumArtworkDialog uses the PopupDialog default `780x760` rect at `x=210,y=70`, not a smaller custom dialog. The shared IPC `pickArtworkSource` derives `sourceName` with `basename(sourcePath, extname(sourcePath))` and uses native title `song.chooseAlbumArtwork` plus image extensions `jpg/png/jpeg/webp/bmp` and shared audio extensions `aac/aiff/alac/ape/flac/m4a/mp3/ogg/opus/wav/wma`. `AlbumArtLibraryPickerDialog` accepts nullable `currentSong`; when absent, it derives artist keys from songs whose raw album equals `albumName`, excludes only `currentSong?.id`, uses same-artist songs first when there is no query, and falls back to the first 20 library songs only when there are no same-artist songs. Electron album-level save/delete uses the same `albumName` prop through `saveAlbumArtwork(albumName, artworkSourcePath)` and `deleteAlbumArtwork(albumName)`, and the main-process SQL uses the passed value directly in `WHERE Album = ?` / `WHERE Name = ?`. After a successful save, Electron runs `setOriginalArtworkUrl(currentArtworkUrl)` and clears only `artworkSourcePath`, so a library choice keeps displaying `sourceUrl` while persisting `sourcePath`. | Flutter now has shared `AlbumArtEditorControl`; `MusicAlbumArtControl` is a wrapper around it and `AlbumArtworkDialog` uses the same editor control. `AlbumArtworkDialog` uses PopupDialog's default `780x760`, opens the same local/library source menu, uses the shared album-art library picker component, passes nullable current song from `libraryContentDataProvider`, applies library choices by setting `sourceUrl/sourcePath`, saves the pending source path, preserves the displayed `sourceUrl` as the new original artwork URL after save, sends `widget.albumName` directly for album-level save/delete instead of a separate raw stored album key, and renders the shared inline delete warning with matching AlbumArtworkDialog runtime rects. The shared picker now mirrors Electron's nullable-current-song ranking path. | Confirmed aligned for function/layout flow by `AlbumArtworkDialog local picker mirrors Electron filters and source basename`, `AlbumArtworkDialog library picker mirrors Electron source menu`, `AlbumArtworkDialog delete uses Electron albumName prop`, `AlbumArtworkDialog saved artwork keeps Electron display source URL`, `AlbumArtworkDialog library artwork picker closes before parent dialog`, the existing MusicDialog picker tests, and runtime visual logs for `album_artwork_light` plus `album_artwork_delete_confirm_light`. |
| AlbumArtworkDialog local picker flow | `AlbumArtworkDialog.tsx` `changeArtwork` calls `pickAlbumArtworkSource()`, ignores canceled results, shows status `song.updateFailed` for `error`, shows status `song.musicNoAlbumArt` with `sourceName` for `no-artwork`, and on success sets `currentArtworkUrl`, `artworkSourcePath`, hides delete confirmation, and clears `statusMessage`. Its Save path persists `saveAlbumArtwork(albumName, artworkSourcePath)`, then clears `artworkSourcePath`, shows `song.albumArtSaved`, and calls `onSaved()`. | Flutter `_changeArtwork` uses the same FilePicker title/extensions and basename no-artwork handling, clears `_statusMessage` on success, stores the prepared source path, then `_saveArtwork` persists `widget.albumName` plus `_artworkSourcePath`, clears dirty state, shows the saved status, and calls `widget.onSaved()`. | Confirmed aligned by `AlbumArtworkDialog local picker mirrors Electron filters and source basename` and `AlbumArtworkDialog local picker saves prepared source like Electron`. |
| AlbumArtworkDialog save without pending artwork | `AlbumArtworkDialog.tsx` `saveArtwork` checks `if (!artworkSourcePath) { setStatusMessage(''); return }`, so clicking Save with no selected local/library artwork only clears an existing status message and does not call `saveAlbumArtwork` or `onSaved`. | Flutter `_saveArtwork` has the same empty `_artworkSourcePath` branch: it clears `_statusMessage` and returns before calling `libraryRepositoryProvider.saveAlbumArtwork` or `widget.onSaved`. | Confirmed aligned by `AlbumArtworkDialog save without pending artwork clears status like Electron`, which first creates a delete status message, then clicks Save with no pending source path and asserts the status disappears, `savedAlbumArtworkName/path` stay empty, and `onSaved` is not called again. |
| AlbumArtworkDialog song artwork resolution | Electron `AlbumArtworkDialog.tsx` passes `songId` into shared `AlbumArtEditorControl`, which passes it to `AlbumArtControl`; `AlbumArtControl.tsx` calls `useSongArtwork(songId, artworkUrl)` and requests `getSongArtworkSnapshots([songId])` when the supplied artwork URL is empty or generated/unusable. | Flutter previously passed `songId: null` from `_AlbumArtworkDialogBody` and the shared `AlbumArtEditorControl` did not use its `songId` field for display resolution. Flutter now passes the real album dialog `songId` and resolves a missing/unusable direct artwork path once per `songId|artworkUrl` key through `libraryRepositoryProvider.getSongArtworkSnapshots([songId])`, while existing direct file paths render without an extra request. | Confirmed aligned by `AlbumArtworkDialog resolves artwork from songId like Electron hook`; shared `MusicDialog album art` tests still pass. |
| Album artwork resolution failure | Electron `useSongArtwork.ts` resolves display artwork asynchronously; a failed `getSongArtworkSnapshots` request rejects the request promise but does not replace the dialog with a Flutter-style error UI or crash the visible dialog surface. `AlbumArtLibraryPickerDialog.tsx` separately catches picker candidate snapshot failures and clears candidate results to show `song.noLibraryArtwork`. | Flutter `_AlbumArtEditorControlState._syncResolvedArtwork` previously let its parent artwork resolution future error escape during picker-open tests, so the dialog path could fail before the picker showed Electron's empty library artwork message. It now catches the parent resolution failure and leaves the current resolved artwork state unchanged; the picker failure path still shows `No available album art in the library`. | Confirmed aligned by `MusicDialog library artwork picker handles snapshot failure` and `AlbumArtworkDialog resolves artwork from songId like Electron hook`. |
| AlbumArtworkDialog status message | Electron `AlbumArtworkDialog.tsx` passes `afterNav={statusMessage ? <p className="song-dialog-status">{statusMessage}</p> : null}` to `PopupDialog`; `PopupDialog.tsx` renders `afterNav` immediately after nav. No `.song-dialog-status` CSS rule exists, and global `index.css` resets `p { margin: 0; }`, so the message uses inherited dialog text color with normal block text, no custom blue/accent state styling, and no extra paragraph margin. | Flutter previously rendered status messages as custom blue 13px/600 text with extra horizontal/vertical spacing. Flutter now renders a full-width after-nav text block with inherited PopupDialog text color, 16px/400 text, and no extra status-label padding. | Confirmed aligned by `AlbumArtworkDialog delete uses Electron albumName prop`, which checks status message text, color, size, weight, and after-nav row left edge. |
| AlbumArtworkDialog tab chrome | Electron `AlbumArtworkDialog.tsx` renders a single `button.AlbumArtItem.AlbumArtPivotItem.is-active` with `<Icon name="albums" />`; `icons.tsx` defines `albums` as a 24px viewBox stroked double-circle record icon with `strokeWidth=1.35`. `song-dialog.css` keeps generic dialog button SVGs at 16px, scopes the 18px tab-icon override to `.music-dialog`, and applies `.AlbumDialog .song-dialog-tabs button:not(.song-dialog-icon-button) { border-radius: 8px; }`. | Flutter previously used a Fluent square album icon through `PopupDialogTab` without first/last radius flags, and the first local SVG pass incorrectly used the MusicDialog 18px icon size. `AlbumArtworkDialog` now uses the Electron albums SVG path at the confirmed AlbumDialog 16px size and passes `first: true, last: true` so the single tab has 8px radius on both ends. | Confirmed aligned by `AlbumArtworkDialog delete uses Electron albumName prop` icon/radius assertions and refreshed `album_artwork_light` screenshot comparison. |
| MusicDialog entry tab label and queue contract | `MusicDialog.tsx` uses `getDialogTabLabel` to remove leading `See`/`查看` from nav tab labels, so the opened dialog shows `Music Info`, `Lyrics`, and `Album Art`, not the context-menu labels. `ArtistsPage.tsx` passes `selectedQueueSongIds = selectedArtistSongs.map(song.id)` to `MusicMenuFlyout`, so the dialog queue is the selected artist's song list for that page state. | Flutter uses `_dialogTabLabel` with the same leading-label removal and ArtistsPage opens MusicDialog with the current selected artist song ids. Entry tests now assert the opened `MusicDialog` mode/song/queue contract or dialog tab labels instead of stale context-menu labels. | Confirmed aligned by the current entry-test rerun listed below. |

## This Pass

| Category | Items |
| --- | --- |
| This pass changes | Stop repeated missing-artwork recommendation snapshot requests for an unchanged MusicDialog candidate source set; reset the request key when the dialog loads a different song or returns to missing artwork through delete/reset. Align same-song mode prop updates with Electron's mode effect plus `song.id` data-load behavior: switch tab without reloading data. Align properties-load failure handling with Electron's no-catch behavior: keep loading and do not show `song.updateFailed`. Align album-art reset recommendation state with Electron: clear the old recommendation immediately, then allow the next recommendation effect/request to repopulate. Align album-art apply-recommendation state with Electron's `!artworkMissing` effect: clear the recommendation after applying it while keeping the pending source path for Save. Align album-art library picker keyboard scope: no global Enter/Space application and no autofocus on the default source tile; only focused source items handle Enter/Space. Align album-art delete confirmation disabled state with Electron `controlsDisabled`. Align property inline action layout: Clear Play Count matches the field row height; Show in Explorer uses the requested folder icon + tooltip; play-count tooltip mirrors Electron title text; artist fields keep Electron's 34px right padding for the overlaid remove button while ordinary text fields keep 12px horizontal padding; artist add button no longer expands the property row beyond Electron's 42px row height. Align album-art recommendation and library picker artist/album matching with Electron: unknown artists participate through `Unknown artist`, MusicDialog passes raw `song.album` to the picker, and picker search/ranking compares raw album text. Add Electron picker dismiss-layer behavior and test coverage for Space and double-click interactions. Align local artwork no-artwork messages with Electron `sourceName` generation for both MusicDialog and AlbumArtworkDialog, align AlbumArtworkDialog's local picker title/extension list with Electron `pickArtworkSource`, wire AlbumArtworkDialog to the same local/library source menu plus shared library picker flow as Electron, share `AlbumArtEditorControl` so AlbumArtworkDialog uses the same commandbar/artwork-shell/delete-warning layout as MusicDialog, remove Flutter's smaller custom AlbumArtworkDialog size so it uses Electron's default `780x760`, refresh entry tests to assert Electron's dialog tab-label and Artists selected-queue contract, refine the loaded artwork light shell so its background/shadow more closely matches the verified Electron runtime screenshot, extend the Flutter visual harness to log runtime TextField/textarea style JSON for direct Electron computed-style comparison, and apply the requested PopupDialog BackgroundFilter-to-liquid-glass override by removing the old standalone `BackdropFilter blur(12)` wrapper so the backdrop is driven by the same `GlassContainer` liquid-glass parameters used by MenuFlyout. |
| This continuation changes | Align lyrics dirty-close confirmation secondary dialog chrome with Electron `RemoveDialog`/`.input-dialog`: 480px shell, 24px padding, light `--border-subtle`, light `--panel-shadow`, and night `0 24px 60px rgba(0,0,0,.42)` shadow, instead of reusing the heavier main PopupDialog border/shadow. |
| This continuation changes | Align AlbumArtworkDialog's shared artwork display path with Electron `useSongArtwork(songId, artworkUrl)`: pass the album dialog `songId` into `AlbumArtEditorControl`, resolve missing/unusable direct artwork with a single `getSongArtworkSnapshots([songId])` request for the current key, and use the explicit `showBusy` prop instead of recomputing busy state inside the shared editor. |
| This continuation changes | Align AlbumArtworkDialog status messages with Electron's plain `p.song-dialog-status` after-nav rendering: inherited dialog text color, normal 16px/400 text, full-width block row, and no Flutter-specific blue/600 status-label spacing. |
| This continuation changes | Align shared AlbumArtEditorControl loading behavior with Electron: keep an existing artwork shell mounted during loading and show the loading overlay on top instead of replacing the body with the pure loading placeholder. |
| This continuation changes | Align loaded album-art shadow with Electron `.song-dialog-artwork .album-art-control { box-shadow: 0 18px 42px rgba(32, 42, 58, 0.16); }`. Flutter previously used the right alpha but wrong RGB (`0x2932423a`, rgba(50,66,58,.16)); it then matched the RGB with `0x29202a3a` but still rendered a hard gray band under the cover because the Flutter `BoxShadow` was painted inside the tight image shell. Flutter now keeps the loaded artwork foreground decoration to Electron surface/radius only and paints the Electron-colored shadow in an expanded independent layer so the screenshot matches Electron's soft drop shadow more closely. Loading-state shadow remains `0x1f202a3a`, matching Electron's separate `.song-dialog-artwork-loading` `.12` shadow. |
| This continuation changes | Align MusicDialog primary command-button shadow with Electron runtime theme variables. Electron `applyThemeColor` sets `--accent-shadow` to accent alpha `.26`, and `.song-dialog-primary-button` uses `0 10px 22px var(--accent-shadow)`; Flutter `_MusicDialogCommandButton` now uses the same `.26` alpha instead of the prior `.14`. |
| This continuation changes | Add standalone AlbumArtLibraryPickerDialog runtime visual coverage for both Electron and Flutter secondary dialogs. Align the picker search button icon with Electron's shared SVG `Icon name="search"` while keeping the shared `PageSearchField` default unchanged outside this picker. |
| This continuation changes | Align AlbumArtLibraryPickerDialog source-tile and preview artwork surfaces with Electron `.album-art-control`: light/dark artwork background, 8px/18px artwork shadow, and existing picker-specific radii/sizes. |
| This continuation changes | Align AlbumArtLibraryPickerDialog search height with Electron's final shared SearchField CSS override: `.search-form { height: 40px; }` in `sidebar-search.css`, moving the picker list/preview content from Flutter y=249/height=458 to Electron y=247/height=460 without changing shared search defaults elsewhere. |
| This continuation changes | Move AlbumArtLibraryPickerDialog selected source-item UI parameters into `GlobalUI`: desktop source-list selected background/border/no-shadow now use Electron `rgba(var(--accent-rgb), .12)` / `.44` / `box-shadow: none`, while mobile uses Electron `rgba(var(--accent-rgb), .08)` / `.16` plus `0 8px 18px rgba(29,42,60,.1)`. The picker now consumes those shared tokens instead of PopupDialog active-button colors or local literals. |
| This continuation changes | Refine AlbumArtLibraryPickerDialog mobile selected source-item structure against Electron runtime. Electron mobile logs `.album-art-library-picker-source-item.is-selected` with no ordinary border and a two-layer `box-shadow`: `0 8px 18px rgba(29,42,60,.1)` plus `inset 0 0 0 1px rgba(var(--accent-rgb), .16)`. Flutter now models the ring as a `GlobalUI.sourceListMobileSelectedShadow` inner shadow token and removes the mobile ordinary border while keeping desktop source-list border behavior unchanged. |
| This continuation changes | Align AlbumArtLibraryPickerDialog preview title typography with Electron's `<strong>` preview title. Electron computed runtime style for `.album-art-library-picker-preview strong` is 16px/700 with a 22.5px line box; Flutter previously used w600 and an 18px line box, and now uses w700 with `22.5 / 16` line height, with both Electron and Flutter visual harnesses logging `pickerPreviewTitle` style data. |
| This continuation changes | Align AlbumArtLibraryPickerDialog source-item title typography with Electron runtime cascade. Electron desktop CSS sets `.album-art-library-picker-copy strong` to 15px/700/18px; the mobile `font-weight:650` rule has lower specificity and does not override it at runtime, so mobile runtime also computes 15px/700 with `currentColor` from the source item. Flutter now uses 15px/700/18px and strong text color for both desktop and mobile source titles. |
| This continuation changes | Add dark runtime visual coverage for AlbumArtworkDialog and AlbumArtLibraryPickerDialog secondary dialogs. The new Electron dark picker evidence confirms an asymmetric artwork-surface rule: source-list choice artwork remains `rgba(255,255,255,.76)` even in night mode, while preview and album-level artwork use the global dark artwork surface `rgba(255,255,255,.08)`. Flutter now mirrors that by forcing only source-list choice artwork to the light artwork surface; preview and main album artwork remain dark-surface. |
| This continuation changes | Correct the missing-artwork recommendation visual target from source-only transparent-button CSS to Electron's runtime cascade: `.song-dialog button` wins for `.album-art-recommendation-button`, so Flutter now uses the same generic dialog button chrome, 18px horizontal padding, 8px radius, light border/fill/shadow, and exact `281.4921875x40` rect. |
| This continuation changes | Align the default album-art fallback cascade inside `NoAlbumArtTextBlock`: Electron's fallback shell remains `500x500`, but `.album-art-control-fallback span` overrides the default artwork span so the background root is `500x420.40625` and the app-icon logo renders at `340x285.875`; Flutter now mirrors those runtime rects instead of filling the whole shell with a 340px square logo. |
| This continuation changes | Align PopupDialog's mobile titlebar back icon with Electron runtime/source by replacing the Fluent font icon with the Electron `ArrowLeftRegular` SVG path at 18px. This fixes the visual harness square/missing-glyph icon in AlbumArtLibraryPickerDialog mobile without changing the 40x32 button geometry or hover/active colors. |
| This continuation changes | Re-align shared MusicDialog commandbar vertical geometry with Electron's global `.uwp-commandbar .uwp-commandbar-button { margin: 4px 3px; min-height: 40px; }` cascade for commandbars that do not contain the lyrics timestamp toggle. Flutter had regressed to a 42px visual slot, centering 40px buttons at y=156 on desktop album-art dialogs and y=99 on mobile AlbumArtworkDialog. Non-timestamp commandbars now keep the 40px slot, restoring desktop album-art/AlbumArtworkDialog buttons to y=155, source menus to y=201, and mobile AlbumArtworkDialog buttons to y=98. |
| This continuation changes | Align AlbumArtLibraryPickerDialog empty desktop preview structure with Electron: the desktop preview aside remains mounted at `240px` wide even with no selected choice, while mobile still omits the preview column. |
| This continuation changes | Align shared AlbumArtEditorControl artwork-resolution failure behavior with Electron's visible dialog behavior: a failed parent `getSongArtworkSnapshots([songId])` resolution no longer throws through the Flutter widget test path or blocks the picker failure UI; the picker still shows `song.noLibraryArtwork` for candidate snapshot failures. |
| This continuation changes | Align AlbumArtLibraryPickerDialog mobile title line box with Electron runtime. Electron mobile picker logs nav/title/body/list rects as `79/25/79/131/135`; Flutter's title text was 26px tall, shifting the body/list down by 1px. The picker title now uses `height: 25 / 18` only in mobile mode, so the nav, body, list frame, and first choice match Electron coordinates. |
| This continuation changes | Align AlbumArtLibraryPickerDialog desktop nav-title width with Electron runtime. Electron logs `.album-art-library-picker-nav h2` at `{x:199,y:137.5,width:760,height:31}`; Flutter's title was sharing the nav row with PopupDialog's generic trailing `Spacer`, giving the title only `380px`. PopupDialog now skips the extra desktop spacer only for the confirmed `album-art-library-picker-dialog` class, so the title keeps Electron's full remaining nav width while other unaudited PopupDialog titles are unchanged. |
| This continuation changes | Correct the Electron runtime visual harness to mirror `App.tsx` theme initialization: it now calls `applyThemeColor('#0078D7')` before rendering MusicDialog, AlbumArtworkDialog, and picker cases, so `--accent-shadow` computes to Electron source's `.26` alpha instead of the stale default `.22` harness value. No Flutter product UI changed in this step. |
| This continuation changes | Extend the Electron runtime visual harness with `commandBarButtons[]` so every MusicDialog commandbar button reports text, class name, computed style, and rect. The new album-art/AlbumArtworkDialog evidence shows desktop Electron buttons right-align to the same group edge as Flutter (`Delete` right edge `958`) and mobile right-aligns to `625`; the remaining per-button width/x differences are text measurement/rasterization, not a confirmed commandbar layout-rule difference. No Flutter product UI changed in this step. |
| This continuation changes | Align the shared album-art source MenuFlyout with Electron's `MusicAlbumArtControl.tsx` and `menus-actions.css`: MusicDialog now opens the local/library source menu at the confirmed button bottom + 6 offset, and shared non-scrollable `MenuFlyout` panels use Electron's intrinsic `min-width:206px` / `max-width:280px` contract instead of forcing every short menu to 280px. Scrollable flyouts keep their existing constrained layout. |
| This continuation changes | Align AlbumArtworkDialog's album-level save state with Electron: saving a library-sourced artwork still persists `sourcePath`, but the displayed/original artwork URL remains the selected `sourceUrl` instead of being replaced by the file/source path. |
| This continuation changes | Align mobile MusicDialog commandbar button padding with Electron's responsive `.uwp-commandbar-button` cascade. Electron mobile AlbumArtworkDialog runtime logs commandbar buttons at `padding-left/right: 10px` with Delete right edge `625`; Flutter commandbar buttons previously kept the desktop 14px padding on mobile. `_MusicDialogCommandButton` and commandbar overflow width estimates now use 10px horizontal padding only at the mobile breakpoint. |
| This continuation changes | Align dirty timed lyrics commandbar membership and timestamp-toggle runtime height with Electron. Electron source gives `.song-dialog-lyrics-timestamp-toggle { min-height: 42px; }`, but runtime `lyrics-dirty-timed` places the toggle in the 48px commandbar row at `{x:768.0625,y:151,width:192.9375,height:48}` while Search/Import/Save/Reset remain inline at y=155 and no More button appears. Flutter now keeps the same inline membership in light and dark with no More at the desktop dialog width and logs `lyricsTimestampToggle` `{x:780.984375,y:151,width:180.015625,height:48}` plus textarea `{x:239,y:217,width:722,height:568}`. |
| This continuation audit | Rechecked same-dialog song switch while lyrics timestamps are hidden. Electron's song-load effect sets `setLyricsText(snapshot.rawText)` directly, not `updateLyricsEditorRawText`, while `lyricsDirty` is derived from `mergePlainLyricsWithTimedRaw(lyricsRawText, lyricsText) !== originalLyricsText` and `onReset` is passed when dirty. Flutter already matched the raw-text display and Reset-visible behavior; the stale Flutter test was corrected to expect Reset instead of treating it as a diff. |
| This continuation changes | Correct shared MusicDialog commandbar row vertical geometry against Electron runtime. Electron's commandbar row places buttons 4px below the commandbar top (`y=155` desktop from `commandbar y=151`, `y=98` mobile AlbumArtworkDialog from `commandbar y=94`). Flutter now keeps the 48px command row and applies the same 4px vertical command-button margin to regular command buttons and `_ArtworkSourceButton`, restoring lyrics empty/loading buttons, album-art Change Artwork/Save/Delete, and mobile AlbumArtworkDialog buttons to the verified `y=155`/`y=98` coordinates. Verified by `dart analyze lib/src/library/ui/music_dialog_controls.dart test/music_dialog_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog preserves timed lyrics when timestamps are hidden"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog dirty timed lyrics commandbar stays inline at Electron desktop width"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "AlbumArtworkDialog mobile commandbar mirrors Electron geometry"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart --plain-name "writes MusicDialog runtime verification screenshots"`. |
| This continuation changes | Align the visible missing-artwork recommendation row with Electron runtime CSS cascade. Flutter no longer renders the recommended title as a standalone visible button block; it now shows the Electron-like single ellipsized line while preserving the Electron runtime hit-target and hover-preview rects for apply/preview behavior. Verified by `dart analyze lib/src/library/ui/music_dialog_controls.dart test/music_dialog_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog applies recommended library album art"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog album art recommendation hover shows preview"`; refreshed `album_art_recommendation_light` screenshot comparison. |
| This continuation changes | Align AlbumArtLibraryPickerDialog preview artwork shadow rendering with Electron's preview `.album-art-control` runtime style (`rgba(32,45,63,.13) 0 8px 18px`). Flutter previously applied the matching shadow values as a tight `BoxShadow`, which created a hard gray band below the 220px preview artwork. The preview now paints the Electron-colored shadow in an expanded independent layer while the foreground artwork surface keeps only Electron background/radius; source-list choice artwork remains shadow-free per Electron `.album-art-library-picker-list .album-art-library-picker-artwork { box-shadow: none; }`. Verified by `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; refreshed picker screenshot comparisons. |
| This continuation evidence | Rechecked shared commandbar-adjacent album surfaces after the 40px slot correction. `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog album art source menu mirrors Electron flyout"`, `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`, and `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog disabled command buttons keep Electron chrome"` pass. Refreshed fixed-crop comparisons are album-art light `cropped_mean_abs_rgb=3.26`, `cropped_over_16=3.87%`; AlbumArtworkDialog light `cropped_mean_abs_rgb=3.08`, `cropped_over_16=3.62%`; AlbumArtworkDialog delete-confirm light `cropped_mean_abs_rgb=3.38`, `cropped_over_16=4.06%`. The first parallel attempt at the source-menu test hit a transient missing `build/native_assets/macos/native_assets.json`; the same test passed when rerun by itself. |
| This continuation evidence | Added AlbumArtworkDialog saving-state coverage for Electron's `AlbumArtEditorControl` contract: `saving` drives visible `.SaveProgress` and disables Change Artwork / Save / Delete until `saveAlbumArtwork(albumName, artworkSourcePath)` completes, then clears the dirty reset action and shows `song.albumArtSaved`. Covered by `flutter test --no-pub test/music_dialog_test.dart --plain-name "AlbumArtworkDialog disables artwork controls while saving like Electron"` and the full `AlbumArtworkDialog` focused test group. No production behavior change was needed for the save-state flow. |
| This continuation evidence | Re-ran the broad album-related focused suites after the local picker flow coverage additions. `flutter test --no-pub test/music_dialog_test.dart --plain-name "album art"` passed 12 MusicDialog album-art tests; `flutter test --no-pub test/music_dialog_test.dart --plain-name "AlbumArtworkDialog"` passed 10 secondary-dialog tests; `flutter test --no-pub test/music_dialog_test.dart --plain-name "library artwork picker"` passed 19 picker interaction/layout tests; `flutter test --no-pub test/music_dialog_visual_verify_test.dart --plain-name "writes MusicDialog runtime verification screenshots"` regenerated Flutter runtime screenshots and rect/style logs for MusicDialog album-art, AlbumArtworkDialog, and AlbumArtLibraryPickerDialog states. No new source-backed MusicDialog/AlbumArtworkDialog functional layout or color-token difference was found; remaining documented deltas stay classified as text measurement, pixel rasterization, or shared Glass/MenuFlyout rendering residuals rather than album-dialog-specific behavior/layout gaps. |
| This continuation evidence | Added AlbumArtLibraryPickerDialog mobile empty-state runtime coverage without changing product UI. Electron mobile empty picker logs the full-viewport shell `{x:0,y:0,width:640,height:820}`, body `{x:0,y:79,width:640,height:669}`, list frame `{x:12,y:131,width:616,height:617}`, and empty message text `{x:12,y:157,width:616,height:22.5}` from `.album-art-library-picker-list { padding: 4px 0 12px; }`. Flutter visual logs now capture `album_art_library_picker_empty_mobile_light` with matching shell/body/list/message geometry and text `{x:12,y:157,width:616,height:23}`. Verified by `dart analyze test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile empty message mirrors Electron list padding"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart --plain-name "writes MusicDialog runtime verification screenshots"`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 0,0,640,820 current_album_art_library_picker_empty_mobile_light ...` produced fixed-crop `cropped_mean_abs_rgb=1.95`, `cropped_over_16=1.15%`. |
| This continuation audit | Re-ran Electron and Flutter runtime visual harnesses for the album-art, album-art recommendation, AlbumArtworkDialog light/delete-confirm light/dark/mobile, and AlbumArtLibraryPickerDialog light/dark/mobile states. Electron and Flutter still agree on the layout-critical album coordinates: loaded artwork `340x340`, recommendation fallback/default artwork/button/preview rects, picker desktop `860x680` shell with list `{x:199,y:247,width:544,height:460}`, and picker mobile full-viewport shell with first selected source item `{x:12,y:135,width:616,height:92}`. The selected source-item background/border/shadow continues to come from `GlobalUI.sourceListSelected*` / `GlobalUI.sourceListMobileSelected*`. No new confirmed Flutter product UI difference was found in this audit. |
| This continuation changes | Extend Electron and Flutter visual harness evidence for AlbumArtLibraryPickerDialog footer buttons. Flutter now gives the picker Cancel/Apply buttons stable visual-test keys, and Electron logs every `.album-art-library-picker-footer button` rect/style. Runtime evidence shows the footer buttons share Electron's row geometry: desktop `y=725`, mobile `y=760`, `height=40`, `gap=10`, and matching right edge (`1001` desktop, `628` mobile). Remaining per-button width/x deltas are text measurement/rasterization, not a confirmed footer layout or padding difference. No product UI behavior changed. |
| This continuation changes | Align AlbumArtLibraryPickerDialog search input horizontal geometry with Electron `SearchField`. Electron's final `.search-form` grid is `40px` search button, `10px` gap, `minmax(0, 1fr)` input, and `10px` right padding; Flutter's picker-specific `PageSearchField` previously placed the input immediately after the 40px search button. `PageSearchField` now has a default-zero `leadingGap` parameter, and only AlbumArtLibraryPickerDialog passes `leadingGap: 10`, leaving other paused/shared search surfaces unchanged. |
| This continuation changes | Align AlbumArtLibraryPickerDialog dark search-form surface and inset highlight with Electron runtime CSS without changing other shared search fields. Electron `sidebar-search.css` sets night `.search-form` to `rgba(255,255,255,.045)` and inset highlight `rgba(255,255,255,.04)`, which the runtime reports as alpha `0x0b` and `0x0a`; Flutter picker previously used shared dark search tokens at `0x0e` for both. `PageSearchField` now accepts optional `searchSurface` and `insetHighlight` overrides, and only AlbumArtLibraryPickerDialog supplies the Electron picker values. |
| This continuation changes | Add AlbumArtLibraryPickerDialog search-history-open runtime coverage for Electron and Flutter. Electron harness now has `album-art-library-picker-history` light/dark states with one sidebar recent search and a real focused search input. Flutter visual harness now captures matching `album_art_library_picker_history_light/dark` states. The confirmed Electron geometry includes CSS border-box size for `.search-history-panel`, so Flutter `SearchHistoryPanel` now supports an optional 1px border inset and AlbumArtLibraryPickerDialog enables it; other shared search panels keep their previous layout. |
| This recommendation-fallback pass | Electron evidence: `AlbumArtControl.tsx` fallback renders `DefaultAlbumArtwork` before fallback text, `MusicAlbumArtControl.tsx` renders `AlbumArtFallbackText`, and `song-dialog.css` confirms `NoAlbumArtTextBlock` uses a `min(500px, calc(100vw - 92px))` fallback shell with the default artwork layer plus recommendation text. Runtime evidence further confirms that the default artwork span is `500x420.40625`, the logo is `340x285.875`, and `.song-dialog button` overrides the source-level transparent recommendation button into generic dialog-button chrome. Flutter now mirrors those runtime rects/chrome, waits for `assets/branding/app-icon.png` before visual capture, positions recommendation content at the fallback bottom in the recommendation state, and keeps the 128px hover preview behavior covered by widget tests. |
| This continuation changes | Re-audit AlbumArtLibraryPickerDialog sizing against both Electron TSX and CSS. `AlbumArtLibraryPickerDialog.tsx` does not pass dimensions, but `song-dialog.css` explicitly sets `.album-art-library-picker-dialog` to `width: min(860px, calc(100vw - 48px))` and `height: min(680px, calc(100vh - 48px))`; Flutter keeps `width: 860` / `height: 680` and now has a widget assertion for that desktop runtime size. |
| This continuation changes | Align AlbumArtworkDialog's single album-art tab with Electron: replace the Fluent square album icon with Electron's custom `albums` SVG at AlbumDialog's confirmed 16px size and give the lone tab both first/last radius so it matches `.AlbumDialog`'s 8px pill shape. |
| This continuation changes | Add real dirty-lyrics close confirmation runtime capture to both visual harnesses by wiring Electron `DialogHost` and triggering the close-confirm flow. Fix PopupDialog action label rendering by explicitly carrying the theme font family/color into the button text, matching Electron `.input-dialog-actions button`'s explicit color/font styling and preventing missing-glyph bars in the screenshot harness. |
| This continuation changes | Align the dirty-lyrics close confirmation title with Electron's `RemoveDialog`/`.input-dialog h3` contract without changing shared `PopupDialogTitle` surfaces. Electron leaves the `h3` at browser bold weight with 22px/1.25 sizing; Flutter now uses a private `_InputDialogTitle` for input/confirm shells with 22px, `FontWeight.w700`, and 1.25 line height. The existing picker/main dialog title component is unchanged. |
| This continuation changes | Correct lyrics commandbar desktop overflow against Electron's `CommandBar` algorithm. Electron measures real commandbar item widths and does not reserve an extra 24px; at the 780px MusicDialog width the normal lyrics toolbar keeps `Search`, `Import`, `Save`, and `Show timestamps` visible with no `More` button. Flutter removed the extra reserve and now uses Electron CSS-derived item estimates for commandbar buttons (`14px` horizontal padding, `8px` icon gap, `20px` icon) plus the timestamp toggle (`12px` horizontal padding, `18px` checkbox, `8px` gap), avoiding inflated offstage Flutter measurements. |
| This continuation evidence | Add explicit coverage for the Electron lyrics-search fallback path: when the internet lyrics snapshot is empty, Flutter launches the locale-aware Bing lyrics URI with external-application mode and shows `song.openBrowserSuccessful`. Add coverage for Electron's thrown internet-lyrics branch: if `getLyrics(..., 'internet')` fails, Flutter opens the same browser URI and shows `song.openBrowserSuccessful`; if that browser launch also fails, Flutter shows `song.searchLyricsFailed`. Add import-success coverage for Electron's lyrics import side effect: the picker path is passed to the repository, editor text is replaced, and the lyrics editor scrolls to top without adding an unconfirmed success notification. Add import cancel/failure coverage: a canceled picker leaves the editor untouched without a failure notification, while a read/import failure keeps the original editor text and shows `song.importLyricsFailed`. Add manual-save coverage for Electron's save guard branches: unchanged lyrics show `song.nothingChanged` without saving, and a repeated save trigger while saving shows `song.processingRequest`. Add pending-save coverage for Electron's `activeMode === 'lyrics'` guard and refreshed save notification: dirty lyrics do not produce a pending-save notification after switching away from the lyrics tab, and saving an old-song pending snapshot shows `song.lyricsUpdatedAndRefreshed` with saved/current titles. Align Electron's unchanged-search behavior: when internet lyrics match the current visible editor text, Flutter now shows `song.nothingChanged` and keeps the existing lyrics scroll offset instead of scrolling to top. Align pending-save Discard Changes with Electron: restore the current dialog song's original lyrics without showing `song.lyricsReset` or scrolling to top. |
| This continuation evidence | `dart analyze lib/src/library/ui/music_album_art_control.dart lib/src/library/ui/album_artwork_dialog.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "AlbumArtworkDialog resolves artwork from songId like Electron hook"`; `flutter test test/music_dialog_test.dart --plain-name "AlbumArtworkDialog"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog album art"`. |
| This continuation evidence | `dart analyze lib/src/library/ui/album_artwork_dialog.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "AlbumArtworkDialog"`. |
| This continuation evidence | `dart analyze lib/src/library/ui/album_artwork_dialog.dart test/music_dialog_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "AlbumArtworkDialog saved artwork keeps Electron display source URL"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`. |
| This continuation evidence | `node tool/electron_music_dialog_visual_verify.mjs` latest mobile AlbumArtworkDialog runtime log shows Change Artwork/Save/Delete at `y=98`, button padding `10px`, Delete right edge `625`, and artwork `{x:150,y:303,width:340,height:340}`. Verified Flutter with `dart analyze lib/src/library/ui/music_dialog_controls.dart test/music_dialog_test.dart` and `flutter test --no-pub test/music_dialog_test.dart --plain-name "AlbumArtworkDialog mobile commandbar mirrors Electron geometry"`, which now asserts 10px commandbar padding and Delete right edge `625`. |
| This continuation evidence | `dart analyze lib/src/library/ui/music_album_art_control.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "AlbumArtEditorControl keeps artwork shell while loading"` now asserts Electron's `.song-dialog-artwork-shell.is-loading .album-art-control { opacity: 0; }` behavior by keeping the artwork shell mounted, showing the loading overlay, and hiding the image while loading. |
| This continuation evidence | `sed -n '1324,1362p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `sed -n '1936,1955p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `dart analyze lib/src/library/ui/music_album_art_control.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog resolves current artwork like Electron hook"`; `flutter test test/music_dialog_visual_verify_test.dart`; refreshed visual logs show loaded `albumArtworkImage` shadow `0x29202a3a 0 18 42`, and fixed-crop comparisons are album-art light `cropped_mean_abs_rgb=3.20`, `cropped_over_16=3.82%`; AlbumArtworkDialog light `cropped_mean_abs_rgb=3.01`, `cropped_over_16=3.57%`. |
| This continuation evidence | `sed -n '136,154p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/appModel.ts`; `sed -n '1086,1122p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `dart analyze lib/src/library/ui/music_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog saved artwork keeps Electron display source URL"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog save"`; `flutter test test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs` now logs Electron primary buttons with `boxShadow=rgba(0,120,215,.26) 0 10px 22px`, matching `applyThemeColor`; refreshed fixed-crop comparisons after primary-button shadow alignment are album-art light `cropped_mean_abs_rgb=3.04`, `cropped_over_16=2.87%`; AlbumArtworkDialog light `cropped_mean_abs_rgb=2.85`, `cropped_over_16=2.61%`. |
| This continuation evidence | `dart analyze test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `flutter test test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`; picker rect logs confirm Electron and Flutter both render the secondary picker at `{x:170,y:110,width:860,height:680}` with matching body/footer geometry. Flutter visual logs now also emit `albumArtworkImage`, `pickerChoiceArtwork`, and `pickerPreviewArtwork` decoration JSON so artwork color/radius/shadow can be compared against Electron computed styles. |
| This continuation evidence | `dart analyze lib/src/library/ui/page_search_history_panel.dart lib/src/library/ui/search_commit_icon_button.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test test/music_dialog_visual_verify_test.dart`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 168,108,864,684 current_album_art_library_picker_light ...` produced picker fixed-crop `cropped_mean_abs_rgb=4.58`, `cropped_over_16=10.48%` after removing the unconfirmed source-list artwork shadow. |
| This continuation evidence | `sed -n '176,190p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/AlbumArtLibraryPickerDialog.tsx`; `sed -n '1,90p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/sidebar-search.css`; `sed -n '180,205p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/sidebar-search.css`; `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; `flutter test test/music_dialog_visual_verify_test.dart`; picker visual logs now match Electron list geometry at `{x:199,y:247,width:544,height:460}` and the refreshed fixed crop is `cropped_mean_abs_rgb=3.84`, `cropped_over_16=9.07%`. |
| This continuation evidence | `sed -n '274,335p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `sed -n '1700,1732p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `dart analyze lib/src/app/app_interaction_colors.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; `flutter test test/music_dialog_visual_verify_test.dart`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 168,108,864,684 current_album_art_library_picker_light ...` produced picker fixed-crop `cropped_mean_abs_rgb=3.35`, `cropped_over_16=4.01%` after selected source-item token alignment. |
| This continuation evidence | Electron runtime logs `pickerTitle` `{x:199,y:137.5,width:760,height:31}` for desktop AlbumArtLibraryPickerDialog; `dart analyze lib/src/library/ui/popup_dialog.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test test/music_dialog_visual_verify_test.dart`; refreshed Flutter visual logs now match title `{x:199,y:137.5,width:760,height:31}`, and `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 170,110,860,680 current_album_art_library_picker_light ...` produced picker fixed-crop `cropped_mean_abs_rgb=3.14`, `cropped_over_16=3.22%`. |
| This continuation evidence | Electron mobile runtime logs selected picker source item `backgroundColor=rgba(0,120,215,.08)`, `borderRadius=9px`, `boxShadow=rgba(29,42,60,.1) 0 8px 18px, rgba(0,120,215,.16) 0 0 0 1px inset`; Flutter visual logs now emit `pickerChoiceSurface` with `border=null` and two `GlobalUI.sourceListMobileSelectedShadow` layers. `dart analyze lib/src/app/app_interaction_colors.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; `flutter test test/music_dialog_visual_verify_test.dart`; mobile picker fixed-crop comparison is now `cropped_mean_abs_rgb=2.52`, `cropped_over_16=3.12%`. The remaining threshold-pixel increase is classified as Flutter inner-shadow rasterization, not an ordinary-border style mismatch. |
| This continuation evidence | `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs` with parsed `commandBarButtons[]`: desktop album-art/AlbumArtworkDialog Electron logs Change Artwork `{x:589.765625,width:165.5625}`, Save `{x:761.328125,width:90.1484375}`, Delete `{x:857.4765625,width:100.5234375}` so the right edge is `958`; Flutter visual logs Change Artwork `{x:603.6146469116211,width:157.0064468383789}`, Save `{x:766.62109375,width:88.3101577758789}`, Delete `{x:860.9312515258789,width:97.0687484741211}` so the same right edge is `958`. Mobile Electron logs right edge `625` and Flutter logs the same `625`. This classifies the remaining commandbar button x/width delta as text measurement, not a confirmed layout gap/padding/right-alignment diff. |
| This continuation evidence | Source-menu audit and verification: `sed -n '100,190p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MusicAlbumArtControl.tsx`; `sed -n '1,120p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/menus-actions.css`; `dart analyze lib/src/library/ui/menu_flyout.dart lib/src/library/ui/music_dialog_controls.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog album art source menu mirrors Electron flyout"`; `flutter test test/command_bar_test.dart --plain-name "MenuFlyout"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs`. Electron `album-art-source-menu` logs menu `{x:589.765625,y:201,width:206,height:82}` and item buttons `{x:596.765625,y:208,width:192,height:34}` / `{x:596.765625,y:242,width:192,height:34}`. Flutter `album_art_source_menu_light` and `album_artwork_source_menu_light` now log menu `{x:600.6146469116211,y:201,width:206,height:82}` with the confirmed +6 vertical offset. Refreshed fixed-crop comparisons are source-menu light `cropped_mean_abs_rgb=3.63`, `cropped_over_16=6.06%`, and refreshed AlbumArtworkDialog source-menu light `cropped_mean_abs_rgb=3.45`, `cropped_over_16=5.80%`; remaining residual is liquid-glass/text rasterization. |
| This continuation evidence | `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; parsed picker footer logs show Electron desktop Cancel `{x:753.296875,y:725,width:90.2734375,height:40}` and Apply `{x:853.5703125,y:725,width:147.4296875,height:40}`, while Flutter desktop Cancel `{x:765.828125,y:725,width:86.4046859741211,height:40}` and Apply `{x:862.2328109741211,y:725,width:138.7671890258789,height:40}` preserve the same 10px gap and right edge `1001`. Mobile Electron right edge is `628`; Flutter mobile right edge is also `628`. |
| This continuation evidence | Electron source: `SearchField.tsx` renders `.search-commit-button` then `input`; `sidebar-search.css` final override keeps `.search-form { grid-template-columns: 40px minmax(0, 1fr); height: 40px; padding: 0 10px 0 0; gap: 10px; }`. Runtime logs now show Electron picker search field `{x:199,y:191,width:802,height:40}`, input `{x:249,width:742}`, and mobile input `{x:62,width:556}`. Flutter visual logs now match field `{x:199,y:191,width:802,height:40}`, input `{x:249,width:742}`, and mobile input `{x:62,width:556}`. Verified by `dart analyze lib/src/library/ui/page_search_history_panel.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`. Refreshed picker fixed-crop comparisons are light `cropped_mean_abs_rgb=3.08`, `cropped_over_16=3.23%`; dark `3.57`, `3.68%`; mobile light `2.54`, `3.16%`. |
| This continuation evidence | Electron dark picker search form logs `backgroundColor=rgba(255,255,255,.043)` and inset highlight `rgba(255,255,255,.04)` from `sidebar-search.css`; Flutter visual logs now emit `pickerSearchDecoration.color=0x0bffffff` and first inset-shadow color `0x0affffff`. Verified again with `dart analyze lib/src/library/ui/page_search_history_panel.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`; refreshed dark picker fixed-crop comparison improved to `cropped_mean_abs_rgb=3.28`, `cropped_over_16=3.14%`. |
| This continuation evidence | Electron source: `SearchHistoryPanel.tsx` with `sidebar-search.css` `.search-history-panel` uses absolute `top: calc(100% + 8px)`, `padding: 8px`, `border: 1px`, `border-radius: 14px`, one header row `30px`, `gap: 6px`, and one `.search-history-item` `38px`; CSS border participates in the DOM border-box. Electron runtime logs history panel `{x:199,y:239,width:802,height:92}`, item `{x:208,y:284,width:784,height:38}`, select `{x:208,y:284,width:750,height:38}`, remove `{x:962,y:288,width:30,height:30}` in both light and dark. Flutter visual logs matched those rects after the picker-only border-inset alignment. | Verified by `dart analyze lib/src/library/ui/page_search_history_panel.dart lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mirrors search history"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`. | This was geometry-only evidence before the later source-backed panel-surface fix. The then-current history-open fixed crops were light `cropped_mean_abs_rgb=4.97`, `cropped_over_16=13.73%`, and dark `5.94`, `15.70%`; the later `Album-art library picker search-history panel surface` row supersedes the old GlassContainer residual classification for AlbumArtLibraryPicker. |
| This continuation evidence | Electron runtime logs desktop `.album-art-library-picker-preview strong` as 16px/700 with rect height `22.5`; Flutter visual logs now emit `pickerPreviewTitle` `{fontSize:16,fontWeight:700,height:1.40625}`. Verified by `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`. The first ordinary `flutter test` attempt was interrupted after hanging in dependency resolution, and a first `--no-pub` retry hit a transient missing native-assets manifest before the visual test regenerated assets; the subsequent same-name `--no-pub` widget test passed. |
| This continuation evidence | Electron source `AlbumArtLibraryPickerDialog.tsx` renders loading/empty as `<p className="album-art-library-picker-message">`, and `song-dialog.css` gives the parent `.album-art-library-picker-list` `padding-right: 10px` plus message `margin: 22px 0` / `color: var(--muted)`, so the text inherits the dialog's 16px/400 font inside the padded list content. Electron runtime for `album-art-library-picker-empty` logs `pickerMessage` text rect `{x:199,y:269,width:534,height:22.5}`, `fontSize=16px`, `fontWeight=400`. Flutter now applies the same list padding to loading/empty message branches as to populated source lists; the visual harness logs the empty text at `{x:199,y:269,width:534,height:23}` with `fontSize=16`, `fontWeight=400`. Verified by `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker loading uses Electron text"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart --plain-name "writes MusicDialog runtime verification screenshots"`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 170,110,860,680 current_album_art_library_picker_empty_light ...` produced empty-picker fixed-crop `cropped_mean_abs_rgb=2.26`, `cropped_over_16=1.10%`. |
| This continuation evidence | Electron source: `MusicAlbumArtControl.tsx` renders `.song-dialog-warning RemoveAlbumArtWarningPanel`; `song-dialog.css` defines its 360px warning panel, 12px padding, 8px gap/radius, and light/dark warning colors. Electron runtime `album-art-delete-confirm` logs artwork `{x:430,y:265,width:340,height:340}`, warning `{x:420,y:623,width:360,height:114}`, warning text `fontSize=16px`, `fontWeight=400`, and generic warning buttons with 40px height. Flutter now matches those layout-critical rects and text size/weight after fixing the previous shrink-wrapped warning width. Verified by `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog album art delete uses inline confirmation"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_album_art_delete_confirm_light ...` produced delete-confirm fixed-crop `cropped_mean_abs_rgb=3.58`, `cropped_over_16=4.37%`. |
| This continuation evidence | Added AlbumArtworkDialog-specific delete-confirm runtime evidence for the secondary album artwork dialog. Electron `AlbumArtworkDialog.tsx` passes `showDeleteConfirm` into the same shared `AlbumArtEditorControl`; runtime `album-artwork-delete-confirm` logs dialog `{x:210,y:70,width:780,height:760}`, artwork `{x:430,y:289,width:340,height:340}`, warning `{x:420,y:647,width:360,height:66}`, and warning text `fontSize=16px`, `fontWeight=400`. Flutter `album_artwork_delete_confirm_light` logs the same dialog/artwork/warning rects and text size/weight; remaining Yes/Cancel/text width deltas are font measurement. Verified by `dart analyze test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_album_artwork_delete_confirm_light ...` produced AlbumArtworkDialog delete-confirm fixed-crop `cropped_mean_abs_rgb=3.38`, `cropped_over_16=4.06%`. |
| This continuation evidence | `sed -n '335,376p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `sed -n '1745,1768p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `node tool/electron_music_dialog_visual_verify.mjs`; Electron mobile runtime logs `.album-art-library-picker-copy strong` as `fontSize=15px`, `fontWeight=700`, `height=18px`, and `color=rgb(31,37,43)` despite the lower-specificity mobile 650 source rule. Flutter visual logs now emit desktop `pickerChoiceTitle {fontSize:15,fontWeight:700,height:1.2}` and mobile `pickerChoiceTitle {fontSize:15,fontWeight:700,height:1.2,maxLines:2,color:textStrong}`. Verified by `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test --no-pub test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`. |
| This continuation evidence | `node --check tool/electron_music_dialog_visual_verify.mjs`; `node tool/electron_music_dialog_visual_verify.mjs`; `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker dark source artwork mirrors Electron"`; `flutter test test/music_dialog_visual_verify_test.dart`. Electron dark logs show picker choice artwork `backgroundColor=rgba(255,255,255,.76)` and preview artwork `backgroundColor=rgba(255,255,255,.08)`; Flutter dark logs now match `0xb8ffffff` for `pickerChoiceArtwork` and `0x14ffffff` for `pickerPreviewArtwork`. New dark fixed-crop comparisons are AlbumArtworkDialog dark `cropped_mean_abs_rgb=2.30`, `cropped_over_16=2.58%`, and AlbumArtLibraryPickerDialog dark `cropped_mean_abs_rgb=3.66`, `cropped_over_16=3.74%` after source-list artwork surface alignment. |
| This recommendation-fallback evidence | `sed -n '200,270p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MusicAlbumArtControl.tsx`; `sed -n '1368,1438p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `sed -n '1800,1865p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `node tool/electron_music_dialog_visual_verify.mjs`; `dart analyze lib/src/library/ui/music_album_art_control.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog album art loading uses Electron cover shell"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog applies recommended library album art"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog album art recommendation hover shows preview"`; `flutter test test/music_dialog_visual_verify_test.dart`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_album_art_recommendation_light ...` produced `/tmp/smplayer_music_dialog_compare_current_album_art_recommendation_light_cropped.png` with current `cropped_mean_abs_rgb=3.38`, `cropped_over_16=4.05%`. Remaining recommendation screenshot residual is now small enough to classify mainly as general text/font rasterization and backdrop differences, not fallback geometry, recommendation button chrome, default artwork rects, or visible line clipping. |
| This continuation evidence | `rg -n "album-art-library-picker-dialog\|width:.*860\|height:.*680" /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/PopupDialog.tsx`; `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`; refreshed runtime logs confirm Electron picker rect `{x:170,y:110,width:860,height:680}` and Flutter picker rect `{x:170,y:110,width:860,height:680}`. |
| This continuation evidence | `sed -n '1,240p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/AlbumArtworkDialog.tsx`; `sed -n '145,175p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `sed -n '50,75p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/icons.tsx`; source audit confirms the 18px tab icon override is scoped to `.music-dialog`, while AlbumDialog inherits the generic 16px SVG size. `dart analyze lib/src/library/ui/album_artwork_dialog.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "AlbumArtworkDialog delete uses Electron albumName prop"`; `flutter test test/music_dialog_visual_verify_test.dart`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_album_artwork_light ...` produced AlbumArtworkDialog fixed-crop `cropped_mean_abs_rgb=2.74`, `cropped_over_16=3.19%`. |
| This continuation evidence | `sed -n '208,410p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `sed -n '1650,1780p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `dart analyze lib/src/library/ui/album_art_library_picker_dialog.dart lib/src/library/ui/popup_dialog.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker mobile mirrors Electron list"` now asserts the mobile picker shell is full viewport at `640x820`, with zero radius, no border, no shadow, mobile body/footer padding, hidden preview, 92px tile, and 84px artwork. |
| This continuation evidence | `sed -n '208,270p' /Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/song-dialog.css`; `dart analyze lib/src/library/ui/popup_dialog.dart test/popup_dialog_test.dart test/music_dialog_test.dart test/music_dialog_visual_verify_test.dart`; `flutter test test/popup_dialog_test.dart --plain-name "Popup confirm uses Electron input-dialog chrome"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog close confirm uses PopupConfirm like Electron"`; `flutter test test/music_dialog_visual_verify_test.dart --reporter expanded`; `node tool/electron_music_dialog_visual_verify.mjs`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_lyrics_discard_confirm_light ...` produced whole-dialog confirm crop `cropped_mean_abs_rgb=38.22`, `cropped_over_16=86.87%`. The large whole-dialog diff is dominated by the explicit liquid-glass/backdrop override; the previous input-dialog-only crop remains `cropped_mean_abs_rgb=10.09`, `cropped_over_16=19.35%`. |
| This continuation evidence | Lyrics search/import/save/loading coverage: `flutter test test/music_dialog_test.dart --plain-name "MusicDialog import lyrics"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog hidden timestamps import displays plain text but saves raw"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog lyrics loading state mirrors Electron"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog saving lyrics reports processing like Electron"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog shortcuts mirror Electron dialog shortcuts"`; `flutter test test/music_dialog_test.dart --plain-name "lyrics"`; `dart analyze lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_lyrics_control.dart test/music_dialog_test.dart`. The lowercase `lyrics` group does not include the hidden-timestamp search/import tests, so those were run separately by exact test name. |
| This continuation evidence | Lyrics close behavior: `flutter test test/music_dialog_test.dart --plain-name "MusicDialog clean lyrics close skips confirm like Electron"` covers Electron's clean close branch, and `flutter test test/music_dialog_test.dart --plain-name "MusicDialog close confirm uses PopupConfirm like Electron"` covers dirty Confirm/Cancel branches. `dart analyze lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_lyrics_control.dart test/music_dialog_test.dart` remains clean. |
| This continuation evidence | Pending lyrics save failure coverage: `flutter test test/music_dialog_test.dart --plain-name "MusicDialog pending lyrics save failure mirrors Electron"`; `flutter test test/music_dialog_test.dart --plain-name "pending lyrics"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog pending current lyrics save failure keeps dirty editor like Electron"` confirms Electron's same-song failed pending save branch keeps the edited lyrics dirty and still opens the discard confirmation on close; `dart analyze lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_lyrics_control.dart test/music_dialog_test.dart`. |
| This continuation evidence | Aligned lyrics-loading commandbar busy state with Electron runtime visibility. Electron `MusicDialog.tsx` passes `lyricsBusy = lyricsLoading || saving` into `MusicLyricsControl`, but runtime `lyrics-loading` logs `.SaveProgress` as a `3px` high, `0px` wide node, so no visible full-width progress line is present. Flutter keeps full-width `_MusicDialogCommandBar` busy progress for saving only and does not draw a visible `MusicDialog.SaveProgress` line during lyrics loading; `MusicDialog lyrics loading state mirrors Electron` asserts loading disables Search/Import/Save, hides the textarea, shows `MusicDialog.LoadingSpinner`, and has no visible `MusicDialog.SaveProgress`. |
| This continuation evidence | Added light-theme lyrics runtime proof alongside the existing dark proof: `dart analyze test/music_dialog_visual_verify_test.dart`; `node --check tool/electron_music_dialog_visual_verify.mjs`; `flutter test test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_lyrics_light ... current_lyrics_dark ...`. Current fixed-crop results are lyrics light `cropped_mean_abs_rgb=2.55`, `cropped_over_16=3.43%` and lyrics dark `cropped_mean_abs_rgb=3.48`, `cropped_over_16=2.71%`. |
| This continuation evidence | Added focused coverage for Electron's hidden-timestamp search branch where the visible plain text is unchanged but the timed raw lyrics differ. Electron updates `lyricsRawText` before the unchanged-visible-text check, shows `song.nothingChanged`, does not reveal raw timestamps in the editor, and a later Save persists the new timed raw lyrics. Verified by `flutter test test/music_dialog_test.dart --plain-name "MusicDialog hidden timestamp search keeps plain text but updates raw like Electron"`, then rechecked the adjacent branches with `flutter test test/music_dialog_test.dart --plain-name "MusicDialog hidden timestamps search displays plain text but saves raw"` and `flutter test test/music_dialog_test.dart --plain-name "MusicDialog unchanged lyrics search keeps scroll like Electron"`. `dart analyze test/music_dialog_test.dart` is clean; `pubspec.lock` was restored afterward. |
| This continuation evidence | Scoped visible-timestamp raw-text synchronization to the lyrics textarea listener, matching Electron `onLyricsTextChange`. Property/tag field edits now only refresh dirty UI state; only lyrics text edits update `_lyricsRawText` when timestamps are visible. The behavior remains covered by `MusicDialog visible timestamp edits update toggle source like Electron` and `MusicDialog timestamp toggle restores edited timed text like Electron`. |
| This continuation evidence | Added focused shortcut guard coverage from Electron `useMusicDialogShortcuts.ts`: Ctrl+F calls `preventDefault()` and searches only when `activeMode === 'lyrics'`; outside lyrics mode it should not request internet lyrics. Verified by `flutter test test/music_dialog_test.dart --plain-name "MusicDialog ignores Ctrl+F outside lyrics like Electron"` and rechecked the positive shortcut path with `flutter test test/music_dialog_test.dart --plain-name "MusicDialog shortcuts mirror Electron dialog shortcuts"`; `pubspec.lock` was restored afterward. |
| This continuation evidence | Tightened lyrics browser fallback layering to match Electron. Electron renderer calls `window.smplayer?.openLyricsSearchInBrowser(song.id)` from `MusicDialog.tsx`; `preload.ts` forwards to IPC `lyrics:open-search-browser`; `library-ipc.ts` calls `shell.openExternal(getLyricsService().getLyricsSearchUrl(songId))`; `lyrics-service.ts` builds the Bing query from the song title/artist and `VoiceAssistantPreferredLanguage` (`lyrics` on `www.bing.com`, `歌词` on `cn.bing.com` for `zh-CN`/`zh-Hant`). Flutter now calls `LibraryRepository.openLyricsSearchInBrowser(songId)` from `MusicDialog._searchLyrics`, and URL construction lives in `LibraryLyricsService.getLyricsSearchUri`. Verified by `dart analyze lib/src/library/data/library_lyrics_service.dart lib/src/library/data/library_repository.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_helpers.dart test/music_dialog_test.dart test/library_repository_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog search opens browser when internet lyrics are empty"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog search falls back to browser when internet lyrics fail"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog search reports failure when internet and browser both fail"`; `flutter test test/library_repository_test.dart --plain-name "getLyricsSearchUri follows Electron preferred language search rules"`. |
| This continuation evidence | Tightened lyrics save snapshot state to match Electron `saveLyricsSnapshot`: after `saveSongLyrics`, Electron only sets `lyricsRawText`, `originalLyricsText`, and `setLyrics((current) => current ? { ...current, rawText: snapshot.lyrics } : current)` for the current dialog song; it does not re-parse lines, change `source`, or recompute `isSynced` in the renderer. Flutter now mirrors that by replacing only `rawText` on the existing `LyricsSnapshot` for manual save and pending-save current-song branches. Verified by `dart analyze lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_helpers.dart test/music_dialog_test.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog lyrics save keeps current snapshot source like Electron"`; adjacent save behavior rechecked with `flutter test test/music_dialog_test.dart --plain-name "MusicDialog preserves timed lyrics when timestamps are hidden"`. |
| This continuation evidence | Tightened lyrics parsing to match Electron `LyricsService.parseLyricsLines`: metadata and offset tags are skipped, timestamp-only lines with no lyric text are skipped, repeated timestamp tags create repeated lines, offset-adjusted negative timestamps are clamped to 0, and untimed text lines sort before timed lines. Flutter `LibraryLyricsService._parseLyricsLines` now mirrors those rules, which affects MusicDialog loaded `LyricsSnapshot.lines` and `isSynced`. Verified by `dart analyze lib/src/library/data/library_lyrics_service.dart test/library_repository_test.dart`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics parses local lyrics lines like Electron"`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics"`. |
| This continuation evidence | Tightened sidecar lyrics selection to match Electron `LyricsService.getSidecarLyrics`: `.lrc` is used only when its text is non-empty after `trim()`, otherwise `.txt` is checked; empty `.txt` also returns no sidecar. Flutter now ignores empty sidecar files instead of letting an empty `.lrc` block a valid `.txt`, preserving MusicDialog's loaded source/placeholder behavior. Verified by `dart analyze lib/src/library/data/library_lyrics_service.dart test/library_repository_test.dart`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics ignores empty sidecar files like Electron"`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics"`. |
| This continuation evidence | Tightened auto internet lyrics loading to match Electron `LyricsService.getSyncedInternetLyrics`: auto mode checks raw searched internet lyrics for synchronized timestamps before applying `PreserveInternetLyricsTimestamps`, while explicit internet/search mode still prepares lyrics through the setting. Flutter now keeps synced internet lyrics available in auto mode even when the preference would strip timestamps for explicit internet requests. Verified by `dart analyze lib/src/library/data/library_lyrics_service.dart test/library_repository_test.dart`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics auto keeps synced internet lyrics before timestamp stripping like Electron"`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics"`. |
| This continuation evidence | Tightened no-lyrics placeholder normalization to match Electron `LyricsService.isNoLyricsPlaceholder`: metadata tags `ti/ar/al/au/by/offset/re/ve/length` are removed before placeholder matching. Flutter now removes the same metadata set so placeholder-like text inside those metadata tags is not misclassified. Verified by `dart analyze lib/src/library/data/library_lyrics_service.dart test/library_repository_test.dart`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics keeps placeholder text inside metadata like Electron"`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics"`. |
| This continuation evidence | Tightened QQ internet lyrics invalid-response filtering to match Electron `LyricsService.searchInternetLyrics`: Electron filters empty responses, the specific QQ invalid mojibake marker `濮濄倖鐡曢弴韫...`, and the Chinese no-lyrics placeholder after stripping metadata/timestamps. Flutter now uses the same Chinese placeholder text instead of the mojibake text and filters the invalid QQ marker before creating the MusicDialog internet lyrics snapshot. Verified by `dart analyze lib/src/library/data/library_lyrics_service.dart test/library_repository_test.dart`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics filters QQ"`; `flutter test test/library_repository_test.dart --plain-name "getSongLyrics keeps placeholder text inside metadata like Electron"`. |
| This continuation evidence | Corrected MusicDialog command bar layout against the full Electron cascade: `song-dialog.css` sets the shared `song-dialog-commandbar` gap/padding, while global `commandbar.css` keeps `.uwp-commandbar-primary { margin-left: auto; justify-content: flex-end; }`, so the primary command group is right-aligned in runtime album-art screenshots. Flutter now keeps the shared MusicDialog command bar right-aligned with `12px` spacing and no per-item padding; button icons were not changed. Verified by `flutter test test/music_dialog_test.dart --plain-name "MusicDialog disabled command buttons keep Electron chrome"`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog library artwork picker uses Electron chrome"`; `flutter test test/music_dialog_visual_verify_test.dart`. |
| This continuation evidence | Aligned MusicDialog commandbar overflow behavior with Electron `CommandBar.tsx`: dynamic overflow remains enabled, overflowable command buttons are moved from right to left into the More flyout, non-button children such as `.song-dialog-lyrics-timestamp-toggle` stay in the primary row, and overflow menu items invoke the same button actions. Flutter `_MusicDialogCommandBar` now measures children and opens a dialog-layer `MenuFlyout` from `MusicDialog.CommandBar.MoreButton` instead of wrapping to a second row. Verified by `flutter test test/music_dialog_test.dart --plain-name "MusicDialog lyrics commandbar overflows into More like Electron"`. |
| This continuation evidence | Refreshed Flutter runtime screenshot evidence after the commandbar correction with `flutter test test/music_dialog_visual_verify_test.dart`. The visual harness passed and wrote `/tmp/smplayer_music_dialog_album_art_light.png` and `/tmp/smplayer_music_dialog_album_artwork_light.png` with right-aligned album-art command buttons. Latest album-art comparisons are `album_art_light cropped_mean_abs_rgb=3.37 cropped_over_16=3.83%`, `album_artwork_light cropped_mean_abs_rgb=3.19 cropped_over_16=3.58%`, `album_art_library_picker_light cropped_mean_abs_rgb=3.38 cropped_over_16=4.24%`, and `album_art_library_picker_mobile_light cropped_mean_abs_rgb=2.52 cropped_over_16=2.80%`. |
| This continuation evidence | Added AlbumArtworkDialog mobile runtime coverage and corrected the shared commandbar row geometry. Electron mobile AlbumArtworkDialog logs commandbar buttons at `y=98`, with the shared commandbar button margin placing buttons 4px below the `y=94` commandbar and artwork at `{x:150,y:303,width:340,height:340}`. Flutter `_MusicDialogCommandBar` now uses a 48px command row with a 4px top offset and a stretched/right-aligned command group; `AlbumArtworkDialog mobile commandbar mirrors Electron geometry` asserts button top `98`, right edge `625`, 10px commandbar button padding, and artwork top-left `{150,303}`. `flutter test test/music_dialog_visual_verify_test.dart` now writes `/tmp/smplayer_music_dialog_album_artwork_mobile_light.png`; fixed full-dialog comparison against `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_music_dialog_album_artwork_mobile_light.png` is `cropped_mean_abs_rgb=3.18`, `cropped_over_16=3.22%`. |
| This continuation evidence | Tightened pending lyrics save coverage for Electron's `saveLyricsSnapshot`: when saving a pending snapshot for the previous song after the dialog has switched to a different song, Electron persists the previous `songId`, shows the refreshed saved/current title notification, and does not call `onSaved` for the current dialog song because `snapshot.songId !== song.id`. `MusicDialog offers pending lyrics save when song changes` now asserts that `onSaved` stays false in that old-song save path; `MusicDialog offers pending lyrics save when current track leaves` still proves the same-song current-track-leave path calls `onSaved`. The first parallel verification hit a Flutter macOS native-asset startup race (`objective_c.dylib` missing) before assertions; the same test passed when rerun serially. `pubspec.lock` was restored afterward. |
| This continuation evidence | Added focused coverage for Electron's song-switch lyrics load state while timestamps are hidden. Electron `MusicDialog.tsx` initializes new-song loaded lyrics with `setLyricsText(snapshot.rawText)` instead of `updateLyricsEditorRawText(snapshot.rawText)`, while `showLyricsTimestamps` remains unchanged; with timestamps hidden, `currentLyricsRawText` then uses `mergePlainLyricsWithTimedRaw(lyricsRawText, lyricsText)`, so the raw timestamped text remains visible and the reset command appears. Flutter already matched this state flow; `MusicDialog song switch keeps raw loaded lyrics when timestamps are hidden like Electron` now locks the behavior. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_helpers.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog song switch keeps raw loaded lyrics when timestamps are hidden like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's empty lyrics import branch. Electron `lyrics:import` returns `{ canceled:false, rawText }` from `LyricsService.readLyricsFromFile`; `getEmbeddedLyrics` returns `''` on missing lyrics or parse failure, and `MusicDialog.importLyrics` treats that as a successful import by calling `updateLyricsEditorRawText(result.rawText)` without showing `song.importLyricsFailed`. Flutter already matched this branch; `MusicDialog import empty lyrics mirrors Electron result` now asserts the editor is cleared, the timestamp checkbox disappears, the Reset command appears, no failure notification is shown, and a later Save persists empty raw lyrics. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog import empty lyrics mirrors Electron result"`. |
| This continuation evidence | Added focused coverage for Electron's manual lyrics save failure branch. Electron `saveLyricsSnapshot` catches failed `saveSongLyrics`, shows `song.updateFailed`, does not update `lyricsRawText`/`originalLyricsText`, does not call `onSaved`, and clears only `saving` in `finally`, leaving the dirty editor retryable. Flutter already matched this branch; `MusicDialog lyrics save failure keeps dirty state like Electron` now asserts the failed save leaves Reset visible and a later successful retry persists the same editor text. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog lyrics save failure keeps dirty state like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's cross-tab dirty lyrics close branch. Electron `requestClose` checks only `lyricsDirty`, not `activeMode`, so dirty lyrics still open the shared confirm dialog after switching to Properties or Album Art. Flutter already matched this branch; `MusicDialog dirty lyrics close confirms outside lyrics tab like Electron` now asserts the confirmation appears after editing lyrics, switching to Music Info, and pressing the close button. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_state_helpers.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog dirty lyrics close confirms outside lyrics tab like Electron"`. |
| This continuation evidence | Fixed and covered the Escape-triggered dirty lyrics close stack. Electron `PopupDialog` registers one close handler through `popupDialogStack`; pressing Escape/top-close reaches `requestClose` once, and `RemoveDialog` itself has no Escape handler, so Escape must not stack duplicate dirty-lyrics confirms. Flutter previously could open two confirm dialogs from one Escape because both global and focused PopupDialog handlers reached `_requestClose`. `_discardLyricsConfirmOpen` now gates the dirty-lyrics confirm to one instance. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_state_helpers.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog dirty lyrics Escape opens one confirm like Electron stack"`. |
| This continuation evidence | Added focused coverage for Electron's active-page reset shortcut rule with dirty lyrics in the background. Electron `resetActivePage` checks `activeMode`, so Ctrl+R on Properties does not reset dirty lyrics even when `lyricsDirty` is true; closing afterward still opens the dirty-lyrics confirmation. Flutter already matched this branch; `MusicDialog Ctrl+R outside lyrics keeps dirty lyrics like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog Ctrl+R outside lyrics keeps dirty lyrics like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's active-page save shortcut rule with dirty lyrics in the background. Electron `saveActivePage` checks `activeMode`, so Ctrl+S on Properties does not call `saveLyrics` even when `lyricsDirty` is true; unchanged properties still show `song.propertiesUpdated`, and closing afterward still opens the dirty-lyrics confirmation. Flutter already matched this branch; `MusicDialog Ctrl+S outside lyrics does not save dirty lyrics like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog Ctrl+S outside lyrics does not save dirty lyrics like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's old-song pending Discard Changes action after a song switch. Electron only restores editor text when `snapshot.songId === song.id`; for an old-song pending snapshot it just clears the pending notification state, leaving the current song's loaded lyrics untouched and showing no `song.lyricsReset`. Flutter already matched this branch; `MusicDialog pending lyrics discard after song change leaves current song like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog pending lyrics discard after song change leaves current song like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's timestamp-toggle merge branch. Electron `toggleLyricsTimestamps` first reads `currentLyricsRawText`, so when timestamps are hidden and the user edits plain text, toggling timestamps back on immediately displays the merged timed raw text without saving. Flutter already matched this branch; `MusicDialog timestamp toggle restores edited timed text like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_helpers.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog timestamp toggle restores edited timed text like Electron"`. |
| This continuation evidence | Added direct coverage for Electron's manual lyrics save success branch. Electron `saveLyricsSnapshot` persists the current dialog song, updates `lyricsRawText`/`originalLyricsText`, updates the current `LyricsSnapshot.rawText`, calls `onSaved`, and shows `song.lyricsUpdated`, which clears the dirty Reset command. Flutter already matched this branch; `MusicDialog lyrics save success clears dirty state like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_helpers.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog lyrics save success clears dirty state like Electron"`. |
| This continuation evidence | Added direct MusicDialog coverage for Electron's clean Escape close path. Electron `App.tsx` `goBackFromSidebar` calls `closeTopPopupDialog()`, `popupDialogStack.ts` invokes the top PopupDialog close handler, and `MusicDialog.tsx` `requestClose` calls `onClose()` immediately when `!lyricsDirty`, so Escape on clean lyrics closes without the discard confirm. Flutter already matched this branch; `MusicDialog clean lyrics Escape closes like Electron stack` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart lib/src/library/ui/music_dialog_state_helpers.dart lib/src/library/ui/popup_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog clean lyrics Escape closes like Electron stack"`; adjacent dirty path rechecked with `flutter test test/music_dialog_test.dart --plain-name "MusicDialog dirty lyrics Escape opens one confirm like Electron stack"`. |
| This continuation evidence | Added focused coverage for Electron's stale lyrics load cleanup. Electron `MusicDialog.tsx` wraps `getLyrics(song.id, 'embedded')` in an effect-local `canceled` flag, so a previous song's late lyrics result is ignored after song switch. Flutter already matched this with its load-generation guard; `MusicDialog ignores stale lyrics load after song switch` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog ignores stale lyrics load after song switch"`. |
| This continuation evidence | Added focused coverage for Electron's song-switch lyrics-load failure branch. Electron's `getLyrics(...).catch` only clears `lyricsLoading` and shows `song.getLyricsFailed`; it does not clear `lyrics`, `lyricsText`, `lyricsRawText`, or `originalLyricsText`, so the previous editor text remains visible after the failed new-song load. Flutter already matched this branch; `MusicDialog song switch lyrics failure keeps previous text like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog song switch lyrics failure keeps previous text like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's current-track pending-save negative branch. Electron only queues pending lyrics when `previousTrackId === latest.songId`, the new `currentTrackId !== latest.songId`, `activeMode === 'lyrics'`, and lyrics are dirty. If the dialog song was not the previous current track, changing `currentTrackId` must not show the pending-save notification. Flutter already matched this branch; `MusicDialog does not offer pending lyrics save when dialog song was not current like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog does not offer pending lyrics save when dialog song was not current like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's active-page save shortcut rule on Album Art. `useMusicDialogShortcuts.ts` always routes Ctrl+S to `saveActivePage`, and `MusicDialog.tsx` only calls `saveLyrics` when `activeMode === 'lyrics'`; on Album Art with dirty background lyrics and no pending artwork source, Ctrl+S must not save lyrics or artwork. Flutter already matched this branch; `MusicDialog Ctrl+S on album art does not save dirty lyrics like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog Ctrl+S on album art does not save dirty lyrics like Electron"`. |
| This continuation evidence | Added focused coverage for Electron's active-page reset shortcut rule on Album Art. `resetActivePage` only calls `resetLyrics` when `activeMode === 'lyrics'`; on Album Art it calls `resetArtwork()` only when there is a pending `artworkSourcePath`, so dirty background lyrics must stay dirty and no reset notification should appear. Flutter already matched this branch; `MusicDialog Ctrl+R on album art keeps dirty lyrics like Electron` now locks it. Verified by `dart analyze test/music_dialog_test.dart lib/src/library/ui/music_dialog.dart`; `flutter test test/music_dialog_test.dart --plain-name "MusicDialog Ctrl+R on album art keeps dirty lyrics like Electron"`. |
| This continuation evidence | Re-ran the Flutter and Electron visual harnesses for lyrics states after the commandbar overflow fix. Flutter visual logging now includes top-level Search/Import/Save/Reset/More/timestamp-toggle rects plus the `lyricsTextarea` rect. Electron `styles.textarea.rect` and Flutter `lyricsTextarea` both report `{x:239,y:217,width:722,height:568}` for normal light/dark lyrics and the dirty-lyrics confirm background state; Flutter `lyrics_empty_light` reports the same rect while Electron `lyrics-empty` uses the same textarea CSS path. Commandbar right-edge geometry is also source-consistent in the dirty-confirm state: Electron Reset ends at `x=958.0`, and Flutter Reset ends at `x=958.0`; normal lyrics keeps `More` absent in both harnesses. This confirms the visible lyrics editor geometry and commandbar containment are aligned; remaining screenshot deltas in those states are not a confirmed layout difference. Verified by `dart analyze test/music_dialog_visual_verify_test.dart`; `flutter test --no-pub test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`. |
| This pass does not change | Pixel-level AlbumArtworkDialog screenshot parity is not claimed; current proof is source-backed shared layout, widget behavior tests, and runtime screenshot geometry/side-by-side artifacts. |
| Still unconfirmed | Full runtime pixel parity for every MusicDialog state and album-level artwork dialog parity remain in the Not Yet Proven section. Current text-size/weight/padding evidence is confirmed, but residual text rasterization differences in side-by-side screenshots are not yet proven to be a Flutter implementation diff. |

## Confirmed Aligned

| Area | Electron rule | Flutter evidence |
| --- | --- | --- |
| Properties load | Properties load independently from lyrics/artwork for the selected song; a properties-load rejection has no Electron catch path, so loading remains active without `song.updateFailed`. | `test/music_dialog_test.dart`: `MusicDialog keeps properties available when lyrics load fails`, `MusicDialog keeps property loading state on property load failure` |
| Lyrics load | Lyrics load uses embedded mode and `song.getLyricsFailed` only on lyrics load failure. Electron's lyrics textarea placeholder is `nowPlaying.noLyrics` only when `lyrics?.source === 'none'`; an empty editor from another source does not show that placeholder. Electron ignores stale `getLyrics` results after song switch through the effect cleanup `canceled` flag. On song-switch load failure, Electron clears only the loading state and keeps the previous editor text/snapshot visible. On successful song switch, Electron writes `snapshot.rawText` directly into the lyrics editor even when the timestamp checkbox is currently hidden, so raw timestamps can remain visible until another timestamp-toggle/search/import/reset path rewrites the displayed text. | `test/music_dialog_test.dart`: `MusicDialog loads embedded lyrics like Electron`, `MusicDialog ignores stale lyrics load after song switch`, `MusicDialog song switch lyrics failure keeps previous text like Electron`, `MusicDialog shows no-lyrics placeholder only for none source`, `MusicDialog keeps properties available when lyrics load fails`, `MusicDialog song switch keeps raw loaded lyrics when timestamps are hidden like Electron` |
| Auto internet lyrics | Electron auto mode only accepts internet lyrics when raw searched lyrics are synced and non-empty; this raw auto-mode check does not apply `PreserveInternetLyricsTimestamps`. Explicit internet/search requests do apply the setting through `prepareInternetLyrics`. | `test/library_repository_test.dart`: `getSongLyrics auto keeps synced internet lyrics before timestamp stripping like Electron` |
| No-lyrics placeholder normalization | Electron strips `ti/ar/al/au/by/offset/re/ve/length` metadata tags and timestamp tags before matching the QQ no-lyrics placeholder string, and `searchInternetLyrics` also rejects QQ's known invalid mojibake lyrics marker. | `test/library_repository_test.dart`: `getSongLyrics keeps placeholder text inside metadata like Electron`, `getSongLyrics filters QQ no-lyrics placeholder like Electron`, `getSongLyrics filters QQ invalid mojibake lyrics like Electron` |
| Lyrics parser and sidecar selection | Electron `LyricsService.getSidecarLyrics` uses `.lrc` only when it has non-empty trimmed text, then checks `.txt` with the same non-empty rule. Electron `parseLyricsLines` trims raw input, skips known metadata tags and offset tags, keeps untimed lyric lines before timed lines, skips timestamp-only lines after removing tags, creates one line per repeated timestamp tag, and clamps offset-adjusted negative timestamps to `0`. | `test/library_repository_test.dart`: `getSongLyrics respects Electron lyrics request modes`, `getSongLyrics ignores extended Electron metadata tags`, `getSongLyrics parses local lyrics lines like Electron`, `getSongLyrics ignores empty sidecar files like Electron` |
| Reset commands | Reset is passed/rendered only when the active page is dirty. | `test/music_dialog_test.dart`: `MusicDialog shows property reset only when dirty like Electron`, `MusicDialog reset scrolls lyrics editor to top like Electron`, `MusicDialog applies recommended library album art` |
| Property display/save | Text fields trim on save; artists normalize through Electron split/dedupe rules; numeric track/year accept digits; read-only composers/genre tag lists replace `, ` with `common.comma`; date-created/date-modified fields mirror Electron `new Date(...).toLocaleString()` output for the verified locale state (`yyyy/M/d HH:mm:ss`). | `test/music_dialog_test.dart`: `MusicDialog normalizes artists and numeric fields like Electron`, `MusicDialog formats readonly tag lists like Electron` |
| No-op property save | Saving unchanged properties updates local/original state and notification without repository update. | `test/music_dialog_test.dart`: `MusicDialog skips property save when nothing changed` |
| Playback | Current playing song toggles pause; non-current or unknown current-track state plays the dialog song through `onPlayTrack` with a queue containing the song. | `test/music_dialog_test.dart`: `MusicDialog play button starts dialog song like Electron`, `MusicDialog plays dialog song when current track is unknown`, `MusicDialog pause button toggles current track like Electron` |
| Clear play count | Clear Play Count calls the play-count update directly and does not enter the global saving state or disable the command buttons. | `test/music_dialog_test.dart`: `MusicDialog clear play count does not enter saving state` |
| Lyrics save on switch | Dirty lyrics on song/current-track switch show a two-action pending-save notification only when Electron's latest lyrics state has `activeMode === 'lyrics'`. The current-track path also requires `previousTrackId === latest.songId` and `currentTrackId !== latest.songId`, so a dialog song that was not previously current does not show the pending-save prompt when some other track becomes current. Saving a pending old-song snapshot uses `song.lyricsUpdatedAndRefreshed` with `{savedTitle, currentTitle}` and calls `onSaved` only when `snapshot.songId === song.id`; therefore a song-change pending save persists the old song without marking the current dialog song saved, while the current-track-leave path for the same dialog song does call `onSaved`. If the pending save throws, Electron shows `song.updateFailed`, does not show a success notification, and does not update `originalLyricsText`, so a failed same-song pending save leaves the editor dirty and close still confirms. The Electron Discard Changes action restores `originalLyricsText` only when the pending snapshot is for the current dialog song, clears the pending snapshot, and does not use the reset command path, so it does not show `song.lyricsReset` or scroll the editor; after a song switch, discarding the old-song pending snapshot leaves the current song editor untouched. | `test/music_dialog_test.dart`: `MusicDialog offers pending lyrics save when song changes`, `MusicDialog pending lyrics discard after song change leaves current song like Electron`, `MusicDialog pending lyrics save failure mirrors Electron`, `MusicDialog pending current lyrics save failure keeps dirty editor like Electron`, `MusicDialog does not offer pending lyrics save outside lyrics tab`, `MusicDialog offers pending lyrics save when current track leaves`, `MusicDialog does not offer pending lyrics save when dialog song was not current like Electron`, `MusicDialog pending lyrics discard restores text without reset notice` |
| Lyrics manual save | `MusicDialog.tsx` `saveLyrics()` first checks `saving` and shows `song.processingRequest`; then checks `currentLyricsRawText === originalLyricsText` and shows `song.nothingChanged` without calling `saveLyricsImmediately`; otherwise it saves the current raw text. After save, Electron `saveLyricsSnapshot` only replaces the current snapshot `rawText` (`current ? { ...current, rawText: snapshot.lyrics } : current`), while preserving existing `source`, `isSynced`, and `lines` in renderer state, updating `originalLyricsText`, calling `onSaved`, and clearing dirty UI. If `saveSongLyrics` throws, Electron only shows `song.updateFailed` and leaves the editor/original dirty state retryable. `searchLyrics()` and `importLyrics()` use the same `saving` guard and show `song.processingRequest` before returning. `MusicLyricsControl.tsx` disables Search/Import/Save/Reset and the timestamp checkbox while `loading || saving`, disables the textarea while `saving`, and visibly shows full-width save progress while saving. During `lyricsLoading`, Electron replaces the textarea body with `song-dialog-loading-placeholder`; runtime `.SaveProgress` is 0px wide, so Flutter mirrors the visible state by showing only the spinner/loading body and no `MusicDialog.SaveProgress` widget. Shortcut-triggered Save/Search still reach the same guards. | `test/music_dialog_test.dart`: `MusicDialog preserves timed lyrics when timestamps are hidden`, `MusicDialog lyrics save success clears dirty state like Electron`, `MusicDialog lyrics save keeps current snapshot source like Electron`, `MusicDialog unchanged lyrics save mirrors Electron notice`, `MusicDialog lyrics save failure keeps dirty state like Electron`, `MusicDialog saving lyrics reports processing like Electron`, `MusicDialog disables lyrics controls while saving like Electron`, `MusicDialog re-enables lyrics controls after save like Electron`, `MusicDialog re-enables lyrics controls after search like Electron`, `MusicDialog re-enables lyrics controls after search fallback like Electron`, `MusicDialog re-enables lyrics controls after search fallback failure like Electron`, `MusicDialog re-enables lyrics controls after import like Electron`, `MusicDialog re-enables lyrics controls after import failure like Electron`, `MusicDialog lyrics loading state mirrors Electron`, `MusicDialog shortcuts mirror Electron dialog shortcuts` |
| Lyrics save refreshes player lyrics | `MusicDialog.tsx` `saveLyricsSnapshot` calls `dispatchLyricsUpdatedEvent(snapshot.songId)` after every successful lyrics save; `dispatchLyricsUpdatedEvent` emits `smplayer:lyrics-updated` with `{ songId }`. `MediaControl.tsx` and `MiniModePage.tsx` both listen for that event, compare the event `songId` with the current song id, and only then call `getLyrics(songId, playerLyricsSource)` to refresh their visible lyrics. The event is dispatched for manual saves and old-song pending saves even when `onSaved` is not called for the current dialog song. | `MusicDialog` now calls `notifyLyricsSaved(ref, songId)` after manual lyrics saves and after pending lyrics saves using the actual saved `songId`. `ShellPage` listens to `lyricsSavedEventProvider`, compares the saved song id to the current player song, clears the cached desktop/player lyrics state only for the matching current song, and increments `_playerLyricsRefreshRevision`. `MiniModeSurface` reloads lyrics when that revision changes even if the current song and lyrics source are unchanged. Covered by `MusicDialog offers pending lyrics save when song changes`, `ShellOverlayHost opens MusicDialog from player dialog state`, and `mini mode refreshes lyrics after MusicDialog save event`. | Confirmed aligned for current-song and old-song pending-save event publication, current-player song filtering, cached desktop/player lyrics reset, and MiniMode reload. |
| MusicDialog command bar layout | Electron `song-dialog-commandbar` supplies `gap: 12px`, `padding: 0 28px 18px` on desktop and `0 12px 12px` on mobile, and the global `.uwp-commandbar-primary` keeps the command group right-aligned with `margin-left:auto` and `justify-content:flex-end`. Electron `CommandBar.tsx` measures item outer widths and moves overflowable `CommandBarButton`s from right to left into the More flyout. In `song-dialog.css`, both `.song-dialog-commandbar` and `.song-dialog-commandbar .uwp-commandbar-primary` are `overflow: visible`, so the command row is allowed to visually overflow instead of clipping while layout is settling. `MusicLyricsControl`, `MusicInfoControl`, and `MusicAlbumArtControl` all use the same Electron commandbar class. | Flutter `_MusicDialogCommandBar` measures item widths, converts overflowed command buttons into the same dialog-layer `MenuFlyout`, and keeps non-button children such as the lyrics timestamp toggle in the primary row. The visible row is wrapped in a fixed-height `OverflowBox`, matching Electron's overflow-visible commandbar behavior while avoiding Flutter `RenderFlex` overflow failures. Button state tests now check both inline command buttons and overflowed MenuFlyout items. Covered by `MusicDialog offers pending lyrics save when song changes`, `MusicDialog lyrics commandbar overflows into More like Electron`, `MusicDialog disabled command buttons keep Electron chrome`, `MusicDialog disables lyrics controls while saving like Electron`, `MusicDialog re-enables lyrics controls after search like Electron`, `MusicDialog re-enables lyrics controls after import like Electron`, `MusicDialog lyrics loading state mirrors Electron`, and `MusicDialog mobile lyrics body mirrors Electron padding`; runtime album-art screenshots show the command group on the right. |
| Pending lyrics save | Save-immediately persists the snapshot, updates current dialog lyrics when applicable, shows refreshed notification, and calls `onSaved` for the current dialog song. Save failure shows `song.updateFailed` instead of the refreshed success notification. Discard Changes restores the editor for same-song pending lyrics without a reset notification and leaves current-song loaded lyrics untouched for old-song pending snapshots after a song switch. | `test/music_dialog_test.dart`: `MusicDialog offers pending lyrics save when current track leaves`, `MusicDialog pending lyrics save failure mirrors Electron`, `MusicDialog pending lyrics discard restores text without reset notice`, `MusicDialog pending lyrics discard after song change leaves current song like Electron` |
| Close lyrics | Closing with clean lyrics calls `onClose` directly; closing with dirty lyrics uses the app PopupConfirm/RemoveDialog-style confirmation, not Material AlertDialog, and Cancel keeps MusicDialog open while Confirm closes. Electron checks dirty lyrics globally, so the same confirmation appears even after leaving the lyrics tab. Escape/top-close must route through the PopupDialog close stack once and must not stack duplicate dirty-lyrics confirmations; clean Escape closes directly, and Electron `RemoveDialog` has no Escape cancel handler. | `test/music_dialog_test.dart`: `MusicDialog clean lyrics close skips confirm like Electron`, `MusicDialog clean lyrics Escape closes like Electron stack`, `MusicDialog close confirm uses PopupConfirm like Electron`, `MusicDialog dirty lyrics close confirms outside lyrics tab like Electron`, `MusicDialog dirty lyrics Escape opens one confirm like Electron stack` |
| Lyrics timestamp toggle | Hiding timestamps preserves timed raw lyrics when saving; toggling timestamps rewrites the editor text through `toggleLyricsTimestamps` by first reading `currentLyricsRawText`, so hidden plain-text edits are immediately merged back into timed raw text when timestamps are shown again. When timestamps are visible, Electron `onLyricsTextChange` also updates `lyricsRawText`, so manually deleting all timestamp tags removes the checkbox and a later Save persists untimed lyrics. The toggle does not call `scrollLyricsToTop`, so the editor scroll position is preserved. Timestamp visibility is derived from Electron `hasLyricsTimestamps(lyricsRawText)`, so replacing the raw editor state with untimed edited/searched/imported lyrics removes the checkbox, and replacing it again with timed searched lyrics restores the checkbox. Timestamp label follows Electron's 42px min-height, 12px horizontal padding, 8px gap, 18px checkbox, and 650 text weight. | `test/music_dialog_test.dart`: `MusicDialog preserves timed lyrics when timestamps are hidden`, `MusicDialog timestamp toggle restores edited timed text like Electron`, `MusicDialog visible timestamp edits update toggle source like Electron`, `MusicDialog timestamp toggle does not scroll lyrics to top`, `MusicDialog untimed search hides timestamp toggle like Electron`, `MusicDialog timed search restores timestamp toggle like Electron`, `MusicDialog untimed import hides timestamp toggle like Electron` |
| Lyrics search/import | Search requests internet lyrics before browser fallback; when the internet lyrics result is empty, Electron calls `window.smplayer?.openLyricsSearchInBrowser(song.id)` and shows `song.openBrowserSuccessful`. If the internet lyrics request throws, Electron tries the same browser fallback; if that fallback also throws, it shows `song.searchLyricsFailed` without changing `lyricsText`/`lyricsRawText`, so any user-edited lyrics remain dirty. The browser fallback is not built in the renderer: `preload.ts` forwards to IPC `lyrics:open-search-browser`, `library-ipc.ts` calls `shell.openExternal(getLyricsService().getLyricsSearchUrl(songId))`, and `LyricsService.getLyricsSearchUrl(songId)` builds the query from `[keyword, song.title, song.artist]`, where `keyword`/host come from `VoiceAssistantPreferredLanguage` (`lyrics`/`https://www.bing.com/search` by default, `歌词`/`https://cn.bing.com/search` for `zh-CN` or `zh-Hant`). When internet lyrics are non-empty, Electron updates the editor from the snapshot; if timestamps are hidden, `updateLyricsEditorRawText(snapshot.rawText)` stores the timed raw text but displays `stripLyricsTimestamps(snapshot.rawText)`, so a later Save persists the raw timed lyrics. Untimed non-empty searched lyrics replace `lyricsRawText` too, so the timestamp checkbox disappears and Save persists untimed raw text; a later timed search replaces `lyricsRawText` again, restoring the checkbox and saving timed raw text. If the resulting visible editor text is unchanged, it shows `song.nothingChanged` and returns before `requestAnimationFrame(scrollLyricsToTop)`, so the current scroll position is preserved; the raw timed text is still updated before that unchanged-visible-text return. The import command label uses Electron `common.import`; Electron IPC uses native title `song.importLyrics`, native `buttonLabel: common.open`, and three filter groups: lyrics-or-music (`lrc/txt` plus `AUDIO_EXTENSIONS`), lyrics-only (`lrc/txt`), and music-only (`AUDIO_EXTENSIONS`). Flutter `file_picker` exposes `dialogTitle`, `type`, and a flat `allowedExtensions` list, so Flutter maps the title and combined extension set but cannot map Electron's native button label or filter group names through the current API. Electron import success calls `updateLyricsEditorRawText(result.rawText)` and `requestAnimationFrame(scrollLyricsToTop)` without a success notification; when timestamps are hidden, import uses the same raw-storage/plain-display path as search. Canceled import returns without editor changes or notification; thrown import shows `song.importLyricsFailed`. Empty imported lyrics are still a successful import: Electron clears the editor, removes the timestamp checkbox, shows no failure, and a later Save persists empty raw lyrics. | `test/music_dialog_test.dart`: `MusicDialog search loads internet lyrics before browser fallback`, `MusicDialog hidden timestamps search displays plain text but saves raw`, `MusicDialog hidden timestamp search keeps plain text but updates raw like Electron`, `MusicDialog untimed search hides timestamp toggle like Electron`, `MusicDialog timed search restores timestamp toggle like Electron`, `MusicDialog unchanged lyrics search keeps scroll like Electron`, `MusicDialog search opens browser when internet lyrics are empty`, `MusicDialog search falls back to browser when internet lyrics fail`, `MusicDialog search reports failure when internet and browser both fail`, `MusicDialog failed lyrics search keeps edited text like Electron`, `MusicDialog import lyrics picker mirrors Electron filters`, `MusicDialog import lyrics updates editor and scrolls to top`, `MusicDialog hidden timestamps import displays plain text but saves raw`, `MusicDialog import empty lyrics mirrors Electron result`, `MusicDialog import lyrics cancel leaves editor unchanged`, `MusicDialog import lyrics failure mirrors Electron message`; `test/library_repository_test.dart`: `getLyricsSearchUri follows Electron preferred language search rules`; Electron evidence: `MusicLyricsControl.tsx` uses `t('common.import')`, `MusicDialog.tsx` `searchLyrics` only calls `setLyrics(...)`/`updateLyricsEditorRawText(...)` after a non-empty internet snapshot and otherwise only shows fallback/failure notifications, `MusicDialog.tsx` `updateLyricsEditorRawText` stores raw lyrics and strips only visible text when timestamps are hidden before the unchanged-visible-text search return, search/import success only changed search/import/reset paths scroll to top, `MusicDialog.tsx` calls `openLyricsSearchInBrowser(song.id)`, `preload.ts`/`library-ipc.ts` route the browser fallback through main-process IPC, `library-ipc.ts` returns `{ canceled:false, rawText }` for empty imports, `lyrics-service.ts` returns `''` for missing embedded lyrics, `lyrics-service.ts` builds the URL from preferred language settings, and `electron/ipc/library-ipc.ts` is the native import picker source for title, button label, and filter groups. |
| Lyrics editor scroll | Changed search/import/reset update editor text and scroll back to top; unchanged internet search and timestamp toggles keep the existing editor scroll offset because Electron does not call `scrollLyricsToTop` on those paths. | `test/music_dialog_test.dart`: `MusicDialog reset scrolls lyrics editor to top like Electron`, `MusicDialog import lyrics updates editor and scrolls to top`, `MusicDialog unchanged lyrics search keeps scroll like Electron`, `MusicDialog timestamp toggle does not scroll lyrics to top` |
| Album art recommendation | Missing artwork loads a same-artist recommendation using Electron's `getSongArtists()` unknown-artist fallback, raw album comparison, and MusicDialog-specific title normalization, renders Electron's single `No album art` fallback text plus centered recommendation grid, uses current text color with 16px/1.35 ordinary text, 3px horizontal link margin, 760 link weight, and 128px hover preview, then can apply/save it. Applying or resetting clears the old recommendation immediately before any fresh recommendation request repopulates it. | `test/music_dialog_test.dart`: `MusicDialog applies recommended library album art`, `MusicDialog reset clears album art recommendation like Electron`, `MusicDialog album art recommendation hover shows preview`, `MusicDialog recommendation keeps Electron raw album comparison`, `MusicDialog recommendation matches Electron unknown artists`, `MusicDialog recommendation uses Electron title normalization` |
| Album art save display URL | Electron saves `artworkSourcePath` but keeps `originalArtworkUrl` equal to the current display `artworkUrl`, so a library/cached `sourceUrl` stays visible after saving even when `sourcePath` points to a different original file. | Flutter now mirrors this state flow: save uses `sourcePath` for persistence and keeps the displayed image on `sourceUrl`. Covered by `MusicDialog saved artwork keeps Electron display source URL`. |
| Album art cover shell | Album-art image and loading placeholder use Electron's `min(340px, 100vw - 92px)` square shell with 10px radius and 18px/42px artwork shadow. Electron's loaded `img.album-art-control` inherits the global `.album-art-control { background: var(--surface-artwork) }` from `album-tile.css`; `index.css` defines `--surface-artwork` as `rgba(255,255,255,.72)` in light mode and `rgba(255,255,255,.08)` in night mode. Flutter now uses those same loaded-image background values, keeps the foreground decoration shadow-free, and paints the Electron-colored soft shadow in an expanded independent layer instead of clipping or hard-banding it inside the image bounds. Loading spinner is centered inside the cover shell; empty `NoAlbumArtTextBlock` fallback uses Electron's later override, `min(500px, 100vw - 92px)`, transparent background, no radius, centered text/recommendation content, and the Electron `default-album-artwork` cascade result where the background span is `500x420.40625` and the app-icon logo is `340x285.875` inside the 500px fallback. AlbumArtworkDialog now shares this same editor shell through `AlbumArtEditorControl`. | `test/music_dialog_test.dart`: `MusicDialog album art loading uses Electron cover shell`, `MusicDialog resolves current artwork like Electron hook`, `AlbumArtworkDialog library picker mirrors Electron source menu`; `flutter test test/music_dialog_visual_verify_test.dart` logs Flutter loaded artwork as `340x340`, `color=0xb8ffffff`, radius `10`, with `MusicDialog.AlbumArtworkImageShadow` mounted as the independent shadow layer, matching Electron `img.album-art-control` computed `340x340`, `rgba(255,255,255,.72)`, `10px`, `rgba(32,42,58,.16) 0 18 42`; recommendation fallback visual logs now match Electron default-artwork root/logo rects within subpixel tolerance; `python3 tool/music_dialog_screenshot_compare.py --dialog-rect 208,68,784,764 current_album_art_light ...` |
| Album art resolved display | When `getSongArtworkSnapshot` reports missing artwork, Electron `AlbumArtControl` still resolves the current song through `useSongArtwork(songId, artworkUrl)` and `getSongArtworkSnapshots([songId])`; Flutter mirrors this with a display-only artwork URL so a cached/generated current artwork is shown without marking the artwork dirty or changing save state. When a non-empty artwork URL is present, Flutter now also mirrors Electron's separate image-loading shell: render the 340px artwork image shell instead of fallback content while the image frame is pending, and the visual harness waits for the image frame before capturing the album-art screenshot. | `test/music_dialog_test.dart`: `MusicDialog resolves current artwork like Electron hook`; `tool/electron_music_dialog_visual_verify.mjs` logs album-art DOM state with `hasArtworkImage: true`, `artworkNaturalWidth: 256`, no fallback DOM, and computed loaded image background `rgba(255,255,255,.72)` |
| Album art body padding | Album-art content and loading states inherit Electron `.song-dialog-body` padding: desktop `0 28px 44px`, mobile `0 12px 28px`, so artwork is centered inside the same padded body as Electron. | `test/music_dialog_test.dart`: `MusicDialog mobile album art body mirrors Electron padding` |
| Album art source menu | Change Artwork opens a dialog-layer MenuFlyout with local/library choices at button bottom + 6. AlbumArtworkDialog uses the same shared editor source menu as MusicDialog. Primary Save command buttons use Electron's runtime `--accent-shadow` alpha `.26` with `0 10px 22px` shadow. | `test/music_dialog_test.dart`: `MusicDialog album art source menu mirrors Electron flyout`, `AlbumArtworkDialog library picker mirrors Electron source menu`, `MusicDialog saved artwork keeps Electron display source URL`; visual evidence: `/tmp/smplayer_music_dialog_album_art_source_menu_light.png`, `/tmp/smplayer_music_dialog_album_artwork_source_menu_light.png`, `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_music_dialog_album_art_source_menu_light.png`, `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_music_dialog_album_artwork_source_menu_light.png`; Electron evidence: `appModel.ts` `applyThemeColor`, `.song-dialog-primary-button` CSS, `MusicAlbumArtControl.tsx`, `MenuFlyout.tsx`, and `menus-actions.css`. |
| Album art local picker filters and source name | Electron `pickSongArtworkSource`/`pickAlbumArtworkSource` use native title `song.chooseAlbumArtwork`, allow image extensions `jpg/png/jpeg/webp/bmp` plus shared `AUDIO_EXTENSIONS` (`aac/aiff/alac/ape/flac/m4a/mp3/ogg/opus/wav/wma`), and derive no-artwork `sourceName` as `basename(sourcePath, extname(sourcePath))`. Electron also sets native `buttonLabel: common.open`; Flutter `file_picker` has no matching `pickFiles` button-label parameter, so that sub-item is not mapped. | Flutter now sends the same dialog title and combined extension list to `FilePicker.pickFiles` for MusicDialog and AlbumArtworkDialog local artwork selection, and both no-artwork messages use the no-extension source basename. Covered by `MusicDialog local artwork picker mirrors Electron filters`, `MusicDialog local artwork no-artwork notice uses source basename`, and `AlbumArtworkDialog local picker mirrors Electron filters and source basename`. |
| Album art library picker | Picker search uses title/raw-album/artists only, shows sidebar recent searches on focus, dismisses the recent-search panel through Electron's outside dismiss layer, commits selected/submitted history back as sidebar search, supports remove/clear, renders Electron's text-only loading/empty messages with inherited 16px/400 dialog text in both desktop and mobile layouts, ignores stale snapshot loads after query/candidate changes, handles artwork snapshot load failure by ending loading, ranks raw album matches before trimmed-album near-misses, ignores global Enter, and lets focused source items handle Space select, Enter apply, and double-click apply. | `test/music_dialog_test.dart`: `MusicDialog library artwork picker mirrors search history`, `MusicDialog library artwork picker dismisses search history like Electron`, `MusicDialog library artwork picker search does not match file path`, `MusicDialog library artwork picker ranks raw album matches like Electron`, `MusicDialog library artwork picker loading uses Electron text`, `MusicDialog library artwork picker mobile empty message mirrors Electron list padding`, `MusicDialog library artwork picker handles snapshot failure`, `MusicDialog library artwork picker ignores stale snapshot loads`, `MusicDialog library artwork picker ignores global Enter like Electron`, `MusicDialog library artwork picker Space selects without apply`, `MusicDialog library artwork picker Enter applies choice`, `MusicDialog library artwork picker double-click applies choice`; `test/music_dialog_visual_verify_test.dart` now captures `album_art_library_picker_empty_light` and `album_art_library_picker_empty_mobile_light`. |
| Album art library picker chrome | Picker search reuses the project SearchField surface that mirrors Electron `.search-shell/.search-form`; the picker-specific field uses Electron's final `.search-form { height: 40px; }` override; picker title uses 22px desktop and 18px mobile with the Electron mobile 25px title line box; body padding is `0 28px 0` desktop and `0 12px 0` mobile; search/list gap is 16px desktop and 12px mobile; footer padding is `18px 28px 24px` desktop and `12px 12px 20px` mobile with 10px button gap. User correction: the footer Cancel button now reuses the actual project CommandBar bottom layer, `SmPlayerTextIconButton`, with 40px height, 44px minimum width, 10px radius, 14px horizontal padding, 14px/720 text, CommandBar surface/border, and `SmPlayerTextIconButtonColors` hover tokens; the primary Apply button keeps the dialog primary filled style. Desktop selected/hovered/focused source items use shared `GlobalUI.sourceListSelected*` tokens for Electron `rgba(var(--accent-rgb), .12)` background, `.44` border, and no shadow; mobile selected/hovered/focused source items use `GlobalUI.sourceListMobileSelected*` for Electron `.08` background, `.16` outline, and `0 8px 18px rgba(29,42,60,.1)` shadow, with the mobile fill alpha-blended against the dialog surface to avoid Flutter's shadow darkening the translucent interior; source-item titles use Electron runtime 15px/700/18px on both desktop and mobile, with mobile allowing two lines; right-side preview is centered with 8px gaps, one-line title, Electron 16px/700 preview title, and Electron line heights; mobile uses a full-viewport square-corner dialog shell, hides the preview, uses the Electron 18px SVG `ArrowLeftRegular` titlebar icon, and switches list items to 92px rows with 84px artwork. | `test/music_dialog_test.dart`: `MusicDialog library artwork picker uses Electron chrome`, `MusicDialog library artwork picker mobile mirrors Electron list`, `MusicDialog library artwork picker mobile empty message mirrors Electron list padding`; `test/popup_dialog_test.dart`: `PopupDialog switches to Electron mobile shell at 720px`; visual logs confirm the picker choice artwork is `64x64`, radius `6`, no shadow, choice title is 15px/700/18px, preview artwork is radius `8` with Flutter `0xb8ffffff` foreground surface and an independent `0x21202d3f` shadow layer, and preview title is 16px/700 with `22.5 / 16` line height, while Electron computes the source item artwork at `64x64`, radius `6`, no source-item image shadow, `.album-art-library-picker-copy strong` at 15px/700/18px, and `.album-art-library-picker-preview strong` at 16px/700 with a 22.5px line box. The picker surface/selection geometry matches. Mobile picker runtime rects now match Electron for nav `{height:79}`, title `{x:12,y:44,width:616,height:25}`, body `{y:79,height:669}`, list frame `{x:12,y:131,width:616,height:617}`, first choice `{x:12,y:135,width:616,height:92}`, empty message text `{x:12,y:157,width:616,height:22.5/23}`, and footer `{y:748,height:72}`. The refreshed Electron DOM log confirms mobile selected source-item computed style is `rgba(0,120,215,.08)` background, `9px` radius, and `rgba(29,42,60,.1) 0 8px 18px, rgba(0,120,215,.16) 0 0 0 1px inset`; Flutter runtime logs the corresponding selected surface as `0xfae7f1fc` alpha-blended fill plus `GlobalUI.sourceListMobileSelectedShadow` values `0x1a1d2a3c 0/8/18` and `0x290078d7` inset spread `1`. Desktop picker exact dialog crop after the independent preview-shadow layer is `cropped_mean_abs_rgb=2.95`, `cropped_over_16=3.23%`; dark exact dialog crop is `cropped_mean_abs_rgb=3.27`, `cropped_over_16=3.15%`; mobile populated picker screenshot compare is `cropped_mean_abs_rgb=2.52`, `cropped_over_16=2.80%`; mobile empty picker screenshot compare is `cropped_mean_abs_rgb=1.95`, `cropped_over_16=1.15%`; selected-row sample Electron `(231,240,250)` vs Flutter `(231,241,251)`. |
| Album art library picker desktop columns | `song-dialog.css` sets `.album-art-library-picker-content { grid-template-columns: minmax(0, 1fr) 240px; gap: 18px; }`, `.album-art-library-picker-preview { padding: 4px 0; gap: 8px; }`, and the preview artwork is `min(220px, 100%)`. | `_AlbumArtLibraryPickerContent` now keeps a 240px preview column with 18px list/preview gap, 4px vertical preview padding, and 220px preview artwork. Covered by `MusicDialog library artwork picker uses Electron chrome`. |
| Album art library picker song-source updates | Electron receives `songs={librarySongs}` from `MusicDialog.tsx` and `AlbumArtworkDialog.tsx`; inside `AlbumArtLibraryPickerDialog.tsx`, `rankedSongs`, `candidateSongIds`, and the snapshot load effect are derived from the latest `songs`, `albumName`, `currentSong`, and query. If the library songs arrive after the dialog first renders, Electron recomputes candidates and loads snapshots instead of staying empty. | Flutter `AlbumArtLibraryPickerDialog.didUpdateWidget` now re-runs `_loadSnapshots()` when `songs`, `albumName`, or `currentSong.id` changes, so an initially empty/partial parent provider state does not leave the picker on `song.noLibraryArtwork`. Covered by `MusicDialog library artwork picker loads choices after songs update`. |
| Album art library picker scrollbar | Electron wraps the source list in `CustomScrollbar`; desktop uses `.custom-scrollbar { right:-5px; bottom:4px; width:9px }` and `.custom-scrollbar-thumb { left/right:2px }`, while mobile overrides `.album-art-library-picker-scrollbar { right:-12px; width:16px }` and thumb `left:5px; right:6px`. Electron list padding is internal: desktop `padding-right:10px`, mobile `padding:4px 0 12px`. | Flutter picker now reuses `_SongDialogScrollbarHost` with the same desktop/mobile geometry and keeps list padding inside the `ListView`. Covered by `MusicDialog library artwork picker scrollbar mirrors Electron` and `MusicDialog library artwork picker mobile scrollbar mirrors Electron`. |
| Album art delete | Delete opens inline `song-dialog-warning` panel with Electron day/night border/background colors and ordinary dialog command buttons; Yes/Cancel disable with `loading || saving`; confirm deletes artwork and shows notification. AlbumArtworkDialog now shares this delete-warning layout through `AlbumArtEditorControl`. | `test/music_dialog_test.dart`: `MusicDialog album art delete uses inline confirmation`, `MusicDialog album art delete confirmation disables while saving` |
| MusicDialog icons | Dialog tabs, command buttons, artist add/remove, album-art source menu, AlbumArtworkDialog nav, and the desktop PopupDialog close button use Electron `Icon` names mapped through `src/components/icons.tsx` to the same default-root SVG path data for `albums`, `info`, `lyrics`, `pictures`, `play`, `pause`, `save`, `undo`, `search`, `import`, `edit`, `trash`, `close`, `plus`, and `musicLibrary`; command-bar icons follow Electron `.uwp-commandbar .uwp-commandbar-button svg` at 20px, tabs stay 18px, artist add follows `--top-titlebar-back-icon-size` at 18px, and artist remove keeps the Electron 28px button/14px icon geometry. Latest user override: MusicDialog command buttons, MusicInfo square icon buttons, PopupDialog close, and artist remove all use shared `GlobalUI.buttonHoverBgColorDay/Night` blue hover surfaces instead of the Electron-specific hover colors. | `test/music_dialog_test.dart`: `MusicDialog icon names mirror Electron Fluent mappings`, `MusicDialog artist remove button hover uses GlobalUI blue surface`, `MusicDialog icon action hover uses GlobalUI blue surface`, `MusicDialog resolves night mode colors for dialog content`, `MusicDialog album art source menu mirrors Electron flyout`; Electron evidence for icon paths: `src/components/icons.tsx` maps `albums` to the stroked three-circle custom icon and `close` to `DismissRegular`, and `PopupDialog.tsx` renders `<Icon name="close" className="dialog-close-icon" />`. User override evidence for hover: 2026-06-07 request says not to reference Electron and to put the unified blue hover effect in `GlobalUI`. |
| MusicDialog tabs | Desktop tabs use Electron's 138px tab width and `button + button { margin-left: -1px }` border-overlap geometry; tab icons are 18px and tab text is Electron computed 16px/650; mobile tabs still fill three equal grid cells. | `test/music_dialog_test.dart`: `MusicDialog resolves night mode colors for dialog content`, `test/popup_dialog_test.dart`: `PopupDialog tabs fill mobile nav cells like Electron`; `node tool/electron_music_dialog_visual_verify.mjs` computed style log |
| MusicDialog mobile titlebar | Mobile uses Electron's fixed 32px titlebar with left 8px padding, a 40x32 back button, 18px `arrowLeft` icon, light hover `rgba(17,24,39,.07)`, dark hover `rgba(255,255,255,.08)`, and active accent `.12`; the desktop close button is hidden in mobile. Flutter uses the Electron `ArrowLeftRegular` SVG path instead of a local font glyph so runtime screenshots render the same icon without depending on Fluent icon fonts. | `test/popup_dialog_test.dart`: `PopupDialog switches to Electron mobile shell at 720px`; `flutter test test/music_dialog_visual_verify_test.dart` refreshed the AlbumArtLibraryPickerDialog mobile screenshot. |
| PopupDialog backdrop glass | Electron source uses `.song-dialog-overlay` tint plus `backdrop-filter: blur(12px)`. User explicitly requested the Flutter dialog BackgroundFilter be changed to the project liquid-glass treatment and noted MenuFlyout already uses liquid glass. Flutter now keeps Electron's overlay tint, removes the old standalone `BackdropFilter blur(12)` wrapper, and reuses the same `GlassContainer` liquid-glass settings as MenuFlyout (`blur:46`, `thickness:20`, `saturation:1.65`, `refractiveIndex:1.06`, minimal quality). | `test/popup_dialog_test.dart`: `PopupDialog portal covers the root app overlay`; `flutter test test/music_dialog_visual_verify_test.dart` regenerated root-overlay MusicDialog screenshots after the backdrop change. |
| MusicDialog light/dark popup chrome | Light dialog text now mirrors Electron computed `rgb(95,98,95)` for dialog/label/input/ordinary button text and `rgb(31,37,43)` for strong text; dark overlay/surface/shadow/button surfaces use Electron `song-dialog.css` values, `--night-border` is Electron `rgba(214,224,236,.12)`, dark muted text uses Electron `rgba(203,213,225,.68)`, and active/hover tab text uses Electron `color-mix(in srgb, #0078d7 72%, #f5fbff 28%)` resolved to `#459de2`. | `test/music_dialog_test.dart`: `MusicDialog resolves night mode colors for dialog content`; `node tool/electron_music_dialog_visual_verify.mjs` computed style log |
| MusicDialog button/input chrome | Top command buttons keep Electron commandbar geometry and typography: 14px/720 text, 20px icons, 40px height, 14px horizontal padding, 10px radius, light `rgba(255,255,255,.56)` surface with `rgba(83,99,121,.14)` border, light inset top highlight `rgba(255,255,255,.42) 0 1px 0 inset`, dark `rgba(255,255,255,.067)` surface with `--night-border`, dark `box-shadow: none`, inherited CommandBar text (`--text-strong`/`--night-text`), right-aligned 12px command gap, 48px command row, `0 28px 18px` desktop padding, and `0 12px 12px` mobile padding. Latest user override: hover surfaces for MusicDialog commandbar buttons, PopupDialog close, and MusicInfo square icon buttons are unified through `GlobalUI.buttonHoverBgColorDay/Night` and intentionally differ from Electron's white hover; picker footer Cancel follows the real project CommandBar `SmPlayerTextIconButtonColors` instead. Primary buttons keep Electron `.song-dialog-primary-button` 52% accent border, accent fill, white text, 10px radius, 14px padding, 10px y-offset, and 22px blur using runtime `--accent-shadow` alpha `.26`. Generic footer/inline dialog buttons keep `.song-dialog button` 16px/650, 18px padding, 8px radius, ordinary dialog border/surface/shadow. Property read-only fields are a user-restored product style: selectable/read-only text fields with neutral `GlobalUI.readOnlyField*` fill, border, and inset highlight so read-only is lightly distinguishable without looking selected/focused; the emphasized blue shadow/fill was removed. Lyrics saving textarea opts out of that read-only styling and keeps the Electron disabled textarea chrome. Selection color still mirrors Electron `rgba(var(--accent-rgb), .22)`. | `test/music_dialog_test.dart`: `MusicDialog resolves night mode colors for dialog content`, `MusicDialog disabled command buttons keep Electron chrome`, `MusicDialog dark command button hover uses GlobalUI blue surface`, `MusicDialog text fields use restored read-only chrome and focus ring`, `MusicDialog lyrics textarea uses Electron selection color`, `MusicDialog library artwork picker uses Electron chrome`, `MusicDialog disables lyrics controls while saving like Electron`, `MusicDialog lyrics loading state mirrors Electron`, `AlbumArtworkDialog mobile commandbar mirrors Electron geometry`, `MusicDialog mobile lyrics body mirrors Electron padding`; Electron evidence for geometry: `MusicLyricsControl.tsx`, `MusicInfoControl.tsx`, and `MusicAlbumArtControl.tsx` all use `song-dialog-commandbar`, while global `commandbar.css` right-aligns `.uwp-commandbar-primary` and `song-dialog.css` defines input/textarea selection colors. User override evidence for hover/read-only tuning: 2026-06-07 request. |
| Mobile properties layout | At Electron's 720px breakpoint, commandbar padding switches to `0 12px 12px`, properties body padding switches to `0 12px 28px`, property rows become a single-column grid with 6px row gap, and the artist add button moves below the artist field grid. | `test/music_dialog_test.dart`: `MusicDialog mobile properties layout mirrors Electron grid` |
| Dialog loading spinner | Properties, lyrics, and album-art loading states reuse Electron `.song-dialog-loading`: 38px square, 3px stroke, 16% accent track, accent top stroke, and top offset based on viewport `min(180px, 28vh)`, not body-height percentage. Electron CSS evidence is `.song-dialog-loading { width:38px; height:38px; margin:min(180px, 28vh) auto 0; border:3px solid rgba(var(--accent-rgb), .16); border-top-color:var(--accent); }`. | `test/music_dialog_test.dart`: `MusicDialog loading spinner mirrors Electron song dialog`; current Flutter visual log for `lyrics_loading_light` confirms `loadingSpinner` `{x:581,y:397,width:38,height:38}` at the 1200x900 viewport. |
| Dialog overlay scrollbars | Properties/album-art body scrolling and lyrics textarea scrolling use Electron-style overlay scrollbars: 9px track, 5px body right offset, 31px lyrics right offset, 38px minimum thumb, 2px inset normal thumb, 1px inset hover/drag thumb, Electron day/night thumb colors, and proportional drag-to-scroll behavior. | `test/music_dialog_test.dart`: `MusicDialog body scrollbar mirrors Electron overlay`, `MusicDialog lyrics scrollbar mirrors Electron overlay` |
| Mobile lyrics body | At Electron's 720px breakpoint, the lyrics body uses `0 12px 28px` padding and the lyrics scrollbar moves from the desktop 31px right offset to 15px. Runtime 640x820 Electron lyrics captures keep Search/Import/Save and the timestamp toggle inline, with no More button; Electron logs Search `{x:148.5703125,y:98,width:96.234375,height:40}`, Import `{x:250.8046875,y:98,width:93.109375,height:40}`, Save `{x:349.9140625,y:98,width:82.1484375,height:40}`, timestamp toggle `{x:435.0625,y:94,width:192.9375,height:48}`, and textarea `{x:12,y:154,width:616,height:638}`. Runtime 640x820 dirty timed Electron lyrics also keeps Search/Import/Save/Reset/timestamp inline with no More; Electron logs Reset `{x:344.0078125,y:98,width:88.0546875,height:40}` and the same textarea rect. | `test/music_dialog_test.dart`: `MusicDialog mobile lyrics body mirrors Electron padding`, `MusicDialog mobile lyrics commandbar keeps Save visible like Electron`, `MusicDialog mobile dirty timed lyrics commandbar stays inline like Electron`; Flutter visual logs now keep Save visible, More absent, timestamp toggle at y=94, and textarea `{x:12,y:154,width:616,height:638}` for `lyrics_mobile_light`, `lyrics_mobile_dark`, `lyrics_dirty_timed_mobile_light`, and `lyrics_dirty_timed_mobile_dark`. |
| Mobile dirty timed visual evidence | Electron 640x820 dirty timed captures write `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_music_dialog_lyrics_dirty_timed_mobile_light.png` and `_dark.png`; the commandbar keeps Search/Import/Save/Reset/timestamp inline and no overflow menu is rendered. | Flutter writes `/tmp/smplayer_music_dialog_lyrics_dirty_timed_mobile_light.png` and `_dark.png`; fixed crop comparisons write `/tmp/smplayer_music_dialog_compare_current_lyrics_dirty_timed_mobile_light_cropped.png` and `_dark_cropped.png`, with light `cropped_mean_abs_rgb=2.86`, `cropped_over_16=3.08%` and dark `cropped_mean_abs_rgb=4.59`, `cropped_over_16=3.32%`. |
| Dirty lyrics confirm visual evidence | Electron `InputDialog.tsx`/`playlists.css` renders the secondary dirty-lyrics confirmation with `.input-dialog-overlay { background: rgba(24,30,38,.24); backdrop-filter: blur(12px); }`, night override `rgba(4,8,13,.62)`, desktop `.input-dialog` rect `{x:360,y:362.625,width:480,height:174.75}`, and mobile rect `{x:80,y:322.625,width:480,height:174.75}`. Electron runtime logs `.input-dialog` computed style for background, border, radius, and shadow. | Flutter `_InputDialogShell` uses a dedicated 12px blurred input-dialog backdrop with the same day/night overlay colors instead of Material's default barrier; `Popup confirm uses Electron input-dialog chrome` asserts a `BackdropFilter`, Electron overlay color, 480px shell, border/radius, and day shadow. Visual logs now include `styles.inputDialog`: desktop `{x:360,y:363.5,width:480,height:173}` and mobile `{x:80,y:323.5,width:480,height:173}`, plus matching surface/border/shadow tokens in light/dark. Current full-dialog dirty-confirm crops remain dominated by the parent PopupDialog liquid-glass overlay difference, which is user-requested shared chrome and not patched as a MusicDialog dirty-confirm source diff. |
| Lyrics command and pending-save audit | Electron `MusicLyricsControl.tsx` disables Search/Import/Save/Reset while `loading || saving`; `MusicDialog.tsx` Search first calls `getLyrics(song.id, 'internet')`, falls back to `openLyricsSearchInBrowser(song.id)` on rejection or empty raw text, Import sets saving while the native import dialog runs and only updates the editor on a non-canceled result, Save no-ops with `nothingChanged` when `currentLyricsRawText === originalLyricsText`, and Reset only runs on dirty lyrics. Electron pending-save notifications are triggered when a dirty lyrics dialog switches song while active on lyrics, or when `currentTrackId` leaves the dialog song; Save Immediately persists the captured song id and Discard only restores the editor when the captured song is still the dialog song. | Flutter `music_dialog.dart` mirrors the same command calls and pending-save triggers. Existing focused tests cover saving/processing/no-change, search success/unchanged/browser fallback/failure, import cancel/success/empty/failure, reset, Ctrl+S/Ctrl+R/Ctrl+F mode scope, song-change pending save, current-track-leaves pending save, discard, and save-failure cases. No new confirmed Flutter product difference was found in this audit pass. |
| Lyrics notification and load-failure audit | Electron pending-save uses `useUndoableNotificationStore.showButtons` with two action buttons (`Save Immediately`, `Discard Changes`), rendered by `InAppNotificationWithButton`; the old `.save-lyrics-later-panel` CSS is not referenced by current `MusicDialog.tsx`. Electron lyrics-load failure catch only clears `lyricsLoading` and shows `song.getLyricsFailed`; it does not clear existing `lyrics`, `lyricsText`, `lyricsRawText`, or `originalLyricsText`. | Flutter `showAppNotification` supports a `List<AppNotificationAction>` and renders both actions in `_AppNotificationOverlay`, so pending-save does not collapse to one action. Flutter `_loadLyrics` failure clears only `_lyricsLoading` and shows `song.getLyricsFailed`, preserving previous editor text/snapshot like Electron. Covered by pending-save tests plus `MusicDialog keeps properties available when lyrics load fails` and `MusicDialog song switch lyrics failure keeps previous text like Electron`. No product change was needed in this audit pass. |
| Lyrics saving disabled textarea chrome | Electron `song-dialog.css` gives `.song-dialog textarea:disabled` the same disabled field tokens as ordinary song-dialog inputs: light `border-color: rgba(190,200,214,.42)`, `background: rgba(230,235,243,.68)`, `color: rgba(83,93,108,.82)`, `opacity:1`, `box-shadow:none`, and the night override keeps `var(--night-border)`, `rgba(255,255,255,.035)`, and `var(--night-muted)`. Runtime `lyrics-saving` Electron DOM evidence shows the save progress node exists at `{x:961,y:153,width:0,height:3}`, Save uses the disabled primary style, the timestamp toggle stays visible, and the textarea is disabled with the same field tokens. | Flutter keeps lyrics-saving textarea on the Electron disabled border/fill/shadow path by passing `emphasizeReadOnly: false`, while property read-only fields use the neutral `GlobalUI.readOnlyField*` style. `MusicDialog disables lyrics controls while saving like Electron` asserts the saving lyrics textarea disabled fill, disabled border, and empty frame shadow; `MusicDialog text fields use restored read-only chrome and focus ring` asserts the property read-only field tokens. `test/music_dialog_visual_verify_test.dart` captures `lyrics_saving_light` and `lyrics_saving_dark`, and `tool/electron_music_dialog_visual_verify.mjs` captures matching `lyrics-saving` Electron states. |
| Lyrics textarea sizing | Lyrics editor uses Electron's textarea model instead of single-line input constraints: it fills the remaining dialog body, uses 12px padding, Electron computed 16px inherited dialog text, ordinary 400 weight, 1.7 line height, and does not inherit the 42px max-height used by property text fields. | `test/music_dialog_test.dart`: `MusicDialog lyrics scrollbar mirrors Electron overlay`; `/tmp/smplayer_music_dialog_lyrics_dark.png` shows both sample lines visible without clipping; `node tool/electron_music_dialog_visual_verify.mjs` computed style log; `flutter test test/music_dialog_visual_verify_test.dart` now logs `lyricsTextarea` style `{fontSize:16,fontWeight:400,height:1.7,contentPadding:12}` |
| Commandbar busy progress | Electron source creates `music-info-save-progress` for page busy states as a `.uwp-commandbar-primary` flex item, not an absolutely positioned full-width line. Runtime evidence must be followed per state: in `lyrics-loading`, Electron logs `.SaveProgress` as `height:3px` but `width:0px` at `{x:961,y:153}`, so no full-width progress line is visible; in desktop `lyrics-saving`, Electron also logs `.SaveProgress` at `width:0px` while Save/Search/Import/Reset and the timestamp checkbox are disabled and the textarea remains visible but disabled. Mobile `lyrics-saving` logs the same zero-width progress geometry at `{x:628,y:96,width:0,height:3}`. | Flutter `_MusicDialogCommandBar` now keeps `MusicDialog.SaveProgress` as a zero-width, 3px-high row child instead of a full-width overlay line. This removes the visible saving progress stripe while preserving the busy node/state on desktop and mobile. Covered by `MusicDialog text fields use restored read-only chrome and focus ring`, `MusicDialog disabled command buttons keep Electron chrome`, `MusicDialog disables lyrics controls while saving like Electron`, `MusicDialog lyrics loading state mirrors Electron`; `flutter test test/music_dialog_visual_verify_test.dart`; `node tool/electron_music_dialog_visual_verify.mjs`; side-by-side `/tmp/smplayer_music_dialog_compare_current_lyrics_loading_light_cropped.png`, refreshed desktop saving crops, and mobile saving crops. |
| Shortcuts | `useMusicDialogShortcuts.ts` listens for Ctrl+S/Ctrl+R/Ctrl+F, calls `event.preventDefault()` for handled shortcuts, saves the active page on Ctrl+S, resets the active page on Ctrl+R, and only runs lyrics search on Ctrl+F when `activeMode === 'lyrics'`. Ctrl+S/Ctrl+R outside the lyrics tab do not save or reset dirty lyrics in the background; this applies to both Properties and Album Art active pages. | `test/music_dialog_test.dart`: `MusicDialog shortcuts mirror Electron dialog shortcuts`, `MusicDialog ignores Ctrl+F outside lyrics like Electron`, `MusicDialog Ctrl+R outside lyrics keeps dirty lyrics like Electron`, `MusicDialog Ctrl+R on album art keeps dirty lyrics like Electron`, `MusicDialog Ctrl+S outside lyrics does not save dirty lyrics like Electron`, `MusicDialog Ctrl+S on album art does not save dirty lyrics like Electron`; Flutter `_handleShortcutKey` returns `KeyEventResult.handled` for handled shortcuts, matching Electron's default-prevention intent. |
| Pending lyrics clear after save | Electron stores pending dirty lyrics in `pendingSwitchLyrics`; `saveLyricsSnapshot` clears that state with `setPendingSwitchLyrics((current) => current?.songId === snapshot.songId ? null : current)` after a successful save. The same pending flow is triggered from both `song.id` changes and current-track changes when the dialog song was the previous current track. | Flutter now has explicit regression coverage for this source-backed behavior: `MusicDialog clears pending lyrics after save like Electron` saves the pending current-track lyrics once, flips `currentTrackId` again, and proves no duplicate pending prompt/save occurs. The broader pending coverage also includes song-change save/discard/failure, current-track leave save/discard/failure, non-lyrics-tab suppression, and background-song suppression. |
| Notification duration | Undo/action notification duration is 5 seconds. | `test/undoable_notification_test.dart`: `undoable notification duration matches Electron store` |
| Notification replacement | New action notification replaces the existing notification with hide reason. | `test/undoable_notification_test.dart`: `showUndoableSnackBar replaces the current undo notification` |
| Notification running state | Running action is tracked by action index; only clicked action shows spinner while all action buttons disable. | `test/undoable_notification_test.dart`: `notification action spinner follows Electron action index` |
| ShellOverlay player dialog | Player-dialog host renders MusicDialog only when the library snapshot is available, passes current track/play state/queue, and clears dialog state on close, matching Electron's `mediaControlDialogSong && mediaControlDialogState` conditional rendering. | `test/shell_page_test.dart`: `ShellOverlayHost opens MusicDialog from player dialog state` |

## Entry Points Covered

Current rerun after the shared AlbumArtEditorControl/AlbumArtworkDialog changes passed for all entries below; `pubspec.lock` was restored and remained clean afterward.

| Entry | Evidence |
| --- | --- |
| MusicLibrary song menu | `flutter test test/music_library_page_test.dart --plain-name "MusicLibraryPage song view menu opens MusicDialog"` |
| Local song menu | `flutter test test/local_page_test.dart --plain-name "LocalPage song view menu opens MusicDialog"` |
| Recent song menu | `flutter test test/recent_page_test.dart --plain-name "RecentPage song view menu opens MusicDialog"` |
| Artists song menu | `flutter test test/artists_page_test.dart --plain-name "ArtistsPage song view menu opens MusicDialog"` |
| Search song menu | `flutter test test/search_page_test.dart --plain-name "SearchPage song view menu opens MusicDialog"` |
| HeaderedPlaylist song menu | `flutter test test/album_detail_page_test.dart --plain-name "HeaderedPlaylistControl song view menu opens MusicDialog"` |
| ShellOverlay player dialog | `flutter test test/shell_page_test.dart --plain-name "ShellOverlayHost opens MusicDialog from player dialog state"` |
| NowPlaying more dialog | `flutter test test/now_playing_page_test.dart --plain-name "NowPlayingFullPage more view dialog pins player bar"` |
| NowPlaying queue dialog | `flutter test test/now_playing_page_test.dart --plain-name "NowPlayingFullPage queue view opens dialog and closes panel"` |

## Current Continuation Evidence

| Area | Evidence |
| --- | --- |
| MusicInfo commandbar consistency | Electron `MusicInfoControl.tsx`, `MusicLyricsControl.tsx`, and `MusicAlbumArtControl.tsx` all use the same `song-dialog-commandbar music-info-control-commandbar` and pass `showBusy` into the shared commandbar; their command buttons use Electron `Icon` names. Flutter now passes `showBusy: saving` from `MusicInfoControl` and uses `_ElectronIcon` for Play/Pause, Save, and Reset instead of Fluent UI icons. Covered by `MusicDialog disabled command buttons keep Electron chrome`, which asserts the Electron SVG path fragments for Play, Save, and Reset plus the existing save-progress geometry. |
| GlobalUI blue button hover override | User explicitly requested not to follow Electron for this item: MusicDialog commandbar buttons, the outside CommandBar hover surface, MusicInfo square/icon buttons, PopupDialog close, and artist remove should share the same blue hover effect from `GlobalUI`. Flutter now defines `GlobalUI.buttonHoverBgColorDay/Night` and `GlobalUI.buttonHoverBorderColorDay/Night`; `CommandBarColors.buttonHoverSurface`, MusicDialog command buttons, `_MusicDialogIconButton`, `_ArtistRemoveButton`, and PopupDialog close read those shared tokens. Covered by `PopupDialog uses Electron desktop shell dimensions`, `MusicDialog icon action hover uses GlobalUI blue surface`, `MusicDialog artist remove button hover uses GlobalUI blue surface`, and `MusicDialog dark command button hover uses GlobalUI blue surface`. |

## Not Yet Proven

| Area | Status |
| --- | --- |
| Flutter runtime screenshot path | Confirmed for Flutter-only runtime evidence. `flutter test test/music_dialog_visual_verify_test.dart` writes nonblank 1200x900 screenshots for properties light, properties dark, lyrics light, lyrics dark, lyrics mobile light/dark, lyrics dirty timed light, lyrics dirty timed dark, lyrics dirty timed mobile light/dark, lyrics empty light, lyrics empty dark, lyrics loading light, lyrics loading dark, lyrics saving light, lyrics saving dark, lyrics saving mobile light/dark, album-art light, album-art source-menu light, album-art delete-confirm light, album-art recommendation light, AlbumArtworkDialog light, AlbumArtworkDialog source-menu light, AlbumArtworkDialog delete-confirm light, AlbumArtworkDialog dark, AlbumArtworkDialog mobile light, AlbumArtLibraryPickerDialog light, AlbumArtLibraryPickerDialog dark, AlbumArtLibraryPickerDialog history light/dark, AlbumArtLibraryPickerDialog empty light, AlbumArtLibraryPickerDialog empty mobile light, AlbumArtLibraryPickerDialog mobile light, and dirty-lyrics confirm light/dark to `/tmp/smplayer_music_dialog_*.png` paths. The harness captures the app root `RepaintBoundary` above `MaterialApp`, which includes PopupDialog's root overlay. The harness loads `/System/Library/Fonts/Supplemental/Arial.ttf`, which is closer to Electron's macOS fallback for the Electron `index.css` font stack than the prior Helvetica TTC test font; MusicDialog and PopupDialog close icons render from inline Electron SVG path data, so no local Electron icon font asset is required. The harness prints `Flutter MusicDialog rects: {...}` for runtime geometry checks; current album-art logs include the right-aligned command buttons, source-menu panel rects, delete-confirm warning rect/style, recommendation fallback/default-artwork rects, AlbumArtworkDialog desktop/mobile/delete-confirm rects, picker history panel rects, and picker empty message text rect/style. Current commandbar geometry logs prove `album_art_light` and `album_artwork_light` buttons at y=155, `album_art_source_menu_light` and `album_artwork_source_menu_light` source menu panels at y=201, and `album_artwork_mobile_light` buttons at y=98 after the shared 40px commandbar-slot fix. Current lyrics logs prove normal timed lyrics and dirty timed lyrics keep `lyricsTimestampToggle` height 48 at y=151, dirty timed lyrics keeps Search/Import/Save/Reset inline with no More button on desktop and mobile, mobile lyrics keeps Search/Import/Save/timestamp inline with no More at 640px, no-toggle lyrics loading/empty commandbars keep buttons at y=155, `lyrics_empty_dark` keeps textarea `{x:239,y:217,width:722,height:568}`, `lyrics_loading_dark` logs `loadingSpinner` at `{x:581,y:397,width:38,height:38}` for Electron viewport-based loading geometry, `lyrics_saving_light/dark` hold the editor in saving state with `MusicDialog.SaveProgress` present and the lyrics TextField disabled, and `lyrics_saving_mobile_light/dark` now cover the same saving state at `640x820`. The same JSON now includes property input, lyrics textarea, album artwork, loading spinner, lyrics timestamp toggle, delete-confirm warning, picker choice artwork, picker preview artwork, and picker empty-message styles so text and artwork decoration can be compared against Electron computed style instead of visual guesses. |
| Electron runtime screenshot path | Confirmed for Electron-only runtime evidence. `node tool/electron_music_dialog_visual_verify.mjs` writes nonblank 1200x900 Electron screenshots for matching properties light/dark, lyrics light/dark/mobile light/mobile dark/dirty-timed light/dirty-timed dark/dirty-timed mobile light/dirty-timed mobile dark/empty light/empty dark/loading light/loading dark/saving light/saving dark/saving mobile light/saving mobile dark, album-art light/source-menu light/delete-confirm light/recommendation, AlbumArtworkDialog light/source-menu light/delete-confirm light/dark/mobile, AlbumArtLibraryPickerDialog light/dark/history light/history dark/empty light/empty mobile light/mobile light, and dirty-lyrics confirm light/dark states to the system temp directory as `electron_music_dialog_*.png` using a temporary Vite harness plus Electron `capturePage()`. The harness logs commandbar/body/computed styles and DOM rects for runtime geometry checks; Electron `lyrics-dirty-timed` logs Search/Import/Save/Reset inline with no More button, timestamp toggle `{x:768.0625,y:151,width:192.9375,height:48}`, and textarea `{x:239,y:217,width:722,height:568}` in both light and dark mode; Electron mobile lyrics logs Search/Import/Save/timestamp inline with no More and textarea `{x:12,y:154,width:616,height:638}` in both light and dark mode, while mobile dirty-timed lyrics additionally keeps Reset inline at `{x:344.0078125,y:98,width:88.0546875,height:40}`; Electron `lyrics-empty` logs the non-loading textarea at `{x:239,y:217,width:722,height:568}` with `No lyrics found` in both light and dark mode; Electron `lyrics-loading` logs `.SaveProgress` rect `{x:961,y:153,width:0,height:3}`, disabled Save rect `{x:867.8515625,y:155,width:90.1484375,height:40}`, and `textarea:null` in both light and dark mode; Electron desktop `lyrics-saving` logs `.SaveProgress` rect `{x:961,y:153,width:0,height:3}`, disabled Save rect `{x:572.859375,y:155,width:90.1484375,height:40}`, timestamp toggle `{x:768.0625,y:151,width:192.9375,height:48}`, and disabled textarea `{x:239,y:217,width:722,height:568}` in light mode; Electron mobile `lyrics-saving` logs `.SaveProgress` rect `{x:628,y:96,width:0,height:3}` with the disabled editor still at `{x:12,y:154,width:616,height:638}`; Electron `lyrics-discard-confirm` dark logs `.input-dialog` rect `{x:360,y:362.625,width:480,height:174.75}` with dark dialog background, night border, and `0 24px 60px rgba(0,0,0,.42)` shadow; Electron `album-art-source-menu` logs menu rect `{x:589.765625,y:201,width:206,height:82}`; Electron `album-art-delete-confirm` logs warning rect `{x:420,y:623,width:360,height:114}` and artwork rect `{x:430,y:265,width:340,height:340}`; Electron `album-artwork-delete-confirm` logs warning rect `{x:420,y:647,width:360,height:66}` and artwork rect `{x:430,y:289,width:340,height:340}`; Electron `album-art-recommendation` logs default artwork root `{x:350,y:251,width:500,height:420.40625}`, logo `{x:430,y:318.265625,width:340,height:285.875}`, generic recommendation button `{x:546.5390625,y:711,width:281.4921875,height:40}`, and preview `{x:738.7734375,y:623,width:128,height:128}`; Electron picker empty logs desktop message text rect `{x:199,y:269,width:534,height:22.5}` and mobile message text rect `{x:12,y:157,width:616,height:22.5}`, both at `fontSize=16px` and `fontWeight=400`. |
| Side-by-side screenshot comparison | Partially confirmed. `python3 tool/music_dialog_screenshot_compare.py ...` compares the generated Electron and Flutter PNGs and writes current side-by-side images to `/tmp/smplayer_music_dialog_compare_current_properties_light.png`, `/tmp/smplayer_music_dialog_compare_current_properties_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_mobile_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_mobile_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_dirty_timed_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_dirty_timed_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_empty_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_empty_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_loading_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_loading_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_saving_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_saving_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_saving_mobile_light.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_saving_mobile_dark.png`, `/tmp/smplayer_music_dialog_compare_current_lyrics_discard_confirm_dark.png`, `/tmp/smplayer_music_dialog_compare_current_album_art_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_art_source_menu_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_art_delete_confirm_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_art_recommendation_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_artwork_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_artwork_source_menu_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_artwork_delete_confirm_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_artwork_dark.png`, `/tmp/smplayer_music_dialog_compare_current_album_artwork_mobile_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_art_library_picker_light.png`, `/tmp/smplayer_music_dialog_compare_current_album_art_library_picker_dark.png`, and `/tmp/smplayer_music_dialog_compare_current_album_art_library_picker_mobile_light.png`, plus matching `_cropped.png` fixed-body crops. The tool supports fixed-body crops to avoid light-mode shadow/backdrop foreground misdetection. Current focused fixed-crop measurements include album-art light `cropped_mean_abs_rgb=2.75`, `cropped_over_16=3.61%`; AlbumArtworkDialog light `cropped_mean_abs_rgb=2.57`, `cropped_over_16=3.35%`; AlbumArtworkDialog mobile light `cropped_mean_abs_rgb=2.56`, `cropped_over_16=2.88%`; AlbumArtworkDialog delete-confirm light `cropped_mean_abs_rgb=2.97`, `cropped_over_16=3.78%`; album-art recommendation light `cropped_mean_abs_rgb=3.02`, `cropped_over_16=3.69%`; refreshed album-art source-menu light `cropped_mean_abs_rgb=3.63`, `cropped_over_16=6.06%`; album-art library picker light exact dialog crop `cropped_mean_abs_rgb=2.95`, `cropped_over_16=3.23%`; album-art library picker dark exact dialog crop `cropped_mean_abs_rgb=3.27`, `cropped_over_16=3.15%`; refreshed AlbumArtworkDialog source-menu light `cropped_mean_abs_rgb=3.45`, `cropped_over_16=5.80%`; lyrics light `cropped_mean_abs_rgb=2.44`, `cropped_over_16=2.46%`; lyrics mobile light `cropped_mean_abs_rgb=2.59`, `cropped_over_16=2.82%`; lyrics mobile dark `cropped_mean_abs_rgb=4.27`, `cropped_over_16=3.03%`; lyrics dirty timed light `cropped_mean_abs_rgb=2.82`, `cropped_over_16=3.57%`; lyrics dirty timed dark `cropped_mean_abs_rgb=3.71`, `cropped_over_16=2.77%`; lyrics dark `cropped_mean_abs_rgb=3.53`, `cropped_over_16=2.68%`; lyrics empty light `cropped_mean_abs_rgb=1.49`, `cropped_over_16=1.66%`; lyrics empty dark `cropped_mean_abs_rgb=2.42`, `cropped_over_16=1.70%`; lyrics loading light `cropped_mean_abs_rgb=1.75`, `cropped_over_16=0.75%`; lyrics loading dark `cropped_mean_abs_rgb=1.57`, `cropped_over_16=0.78%`; refreshed lyrics saving light `cropped_mean_abs_rgb=3.33`, `cropped_over_16=1.56%`; refreshed lyrics saving dark `cropped_mean_abs_rgb=3.06`, `cropped_over_16=1.68%`; lyrics saving mobile light `cropped_mean_abs_rgb=3.26`, `cropped_over_16=1.77%`; lyrics saving mobile dark `cropped_mean_abs_rgb=3.14`, `cropped_over_16=1.93%`; dirty-lyrics confirm light dialog crop `cropped_mean_abs_rgb=10.75`, `cropped_over_16=21.37%`; dirty-lyrics confirm dark dialog crop `cropped_mean_abs_rgb=6.32`, `cropped_over_16=2.29%`. The refreshed source-menu crops prove the album-artwork rect is aligned at `{x:430,y:331,width:340,height:340}` in both Electron and Flutter; remaining source-menu residual is classified under shared MenuFlyout liquid-glass/text measurement rather than a MusicDialog artwork layout diff. The dirty timed and saving comparisons use matched unfocused textarea state; current runtime logs prove normal/dirty timed lyrics keep `More` absent, preserve the same textarea rect `{x:239,y:217,width:722,height:568}`, keep the timestamp toggle at y=151 with 48px height, keep mobile lyrics Save visible with More absent at 640px, keep empty lyrics textarea rect stable in dark mode, keep the loading spinner at `{x:581,y:397,width:38,height:38}` in dark mode, and keep saving disabled textarea/progress geometry aligned on desktop and mobile with no visible full-width progress stripe. `/tmp/smplayer_music_dialog_compare_current_lyrics_empty_light_cropped.png` and `/tmp/smplayer_music_dialog_compare_current_lyrics_empty_dark_cropped.png` show both sides keep the same empty-lyrics layout and `No lyrics found` placeholder. The dirty-confirm crop remains dominated by the user-requested liquid-glass/backdrop rendering and residual shadow/text rasterization differences; Electron source for that backdrop is plain CSS blur, so no further product change is made from that pixel delta alone. |
| Pixel parity against Electron | Not confirmed. Flutter and Electron screenshots can now be generated and compared with real text/icon fonts in the Flutter harness, but the current side-by-side outputs still show remaining visual differences. Do not claim final production pixel parity until those remaining diffs are classified and runtime-verified. |
| Text rasterization difference | Partially classified. Electron runtime computed style and Flutter visual harness style JSON agree on the checked property input/lyrics textarea layout-critical values: 16px text, 400 weight, 12px input padding, 42px property input height, and 1.7 lyrics line height. Electron globally declares `font-family: 'Segoe UI Variable Display', 'Aptos', 'Segoe UI', sans-serif` in `index.css`; Flutter `buildSmPlayerTheme` currently does not set a global product font family. The screenshot harness now uses Arial only for verification to better approximate Electron's macOS fallback. The remaining visible text difference in `/tmp/smplayer_music_dialog_compare_current_properties_dark_cropped.png` and `/tmp/smplayer_music_dialog_compare_current_lyrics_dark_cropped.png` is still not a confirmed MusicDialog implementation diff; do not change product font weight/size or global app font without a separate Electron-backed font decision. |
| Liquid-glass rendered blend | Partially confirmed. The PopupDialog backdrop now removes the old standalone blur layer and uses the same source-level liquid-glass parameters as MenuFlyout; `flutter test test/music_dialog_visual_verify_test.dart` regenerated nonblank root-overlay screenshots after the change. Album-art source-menu screenshots still show a visible menu-surface/shadow difference because Electron `.library-context-menu` is plain CSS (`background: var(--surface-menu)`, `0 18px 44px rgba(38,51,68,.18)`), while Flutter's shared `MenuFlyout` intentionally uses `GlassContainer` with MenuFlyout liquid-glass settings covered by `MenuFlyout uses liquid glass background` and `notification glass follows MenuFlyout panel settings`. Pixel-level comparison of Flutter liquid-glass rendering against Electron's plain CSS blur/menu surfaces is not claimed, and this is not patched as a MusicDialog-only difference. |
| Electron live comparison | Partially confirmed. Both Flutter and Electron runtime screenshot paths now exist for the same three broad MusicDialog modes, and side-by-side comparison artifacts can be generated. Remaining work is to inspect those artifacts, classify concrete visual diffs, and patch only Electron-backed differences. |
| Album-art visual rasterization | Partially confirmed. Electron screenshot proves the album-art case is a resolved current-artwork state (`img.album-art-control`, 256px natural size, no fallback DOM), and Flutter visual harness now waits until `MusicDialog.AlbumArtworkLoadingOverlay` is gone before screenshot capture. `/tmp/smplayer_music_dialog_compare_current_album_art_light_cropped.png` confirms the image and Electron loaded-artwork surface are visible on both sides. Runtime style logs prove the main album-art and AlbumArtworkDialog shell size/background/radius match Electron, and screenshot comparison confirms the custom shadow layer improves the visible soft drop-shadow match. Remaining unconfirmed difference: residual image/text/glass rasterization still differs visually, so pixel parity is not proven. |
| Full app-wide entrance audit after future changes | Partially covered by targeted entry tests above. Re-run relevant entry tests after touching any MusicDialog caller. |
| AlbumArtworkDialog shared editor/picker parity | Function and layout flow confirmed aligned. Electron album-level artwork editing uses shared `AlbumArtEditorControl`, the local/library source flyout, and `AlbumArtLibraryPickerDialog`; Flutter now uses shared `AlbumArtEditorControl`, exposes the same local/library entries, reuses the shared picker, supports nullable current-song ranking, applies selected library artwork through `sourceUrl/sourcePath`, saves the selected source path, and uses the same default PopupDialog size as Electron. Runtime visual evidence confirms the dialog/artwork rects match; pixel-level AlbumArtworkDialog screenshot identity remains unproven. |
| MusicDialog secondary dialogs functional verification | Electron MusicDialog-related secondary surfaces are dirty-lyrics `RemoveDialog` via `requestConfirmDialog`, nested `AlbumArtLibraryPickerDialog` PopupDialog from MusicDialog and AlbumArtworkDialog, and inline album-art delete confirmation from `AlbumArtEditorControl`. | Current targeted Flutter functional verification passed: `flutter test --no-pub test/music_dialog_test.dart --plain-name "lyrics"` covers dirty lyrics confirm, pending-save notification actions, shortcuts, search/import/save/reset/timestamp flows; `--plain-name "library artwork picker"` covers nested picker close order, search history, keyboard apply/select, stale loads, loading/failure, ranking, mobile behavior; `--plain-name "AlbumArtworkDialog"` covers shared editor local/library picker, save/delete, disabled controls, nested picker close order, mobile commandbar; `--plain-name "album art"` covers MusicDialog inline delete confirmation, recommendation, source menu, save/reset, and dirty lyrics isolation while on album-art tab. |
