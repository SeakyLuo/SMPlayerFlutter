# ArtistsPage Electron Parity Matrix

This document is the acceptance target for Flutter `ArtistsPage` parity. Electron is the source of truth. Do not implement behavior or UI that is not backed by the Electron evidence listed here.

## Electron Evidence

- Page/component: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/pages/ArtistsPage.tsx`
- Model/helper: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/pages/artistsPageModel.ts`
- Styles: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/artists.css`
- Responsive styles: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/responsive.css`
- Route wiring: `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/AppRoutes.tsx`

## Behavior Matrix

| Area | Electron Rule | Evidence | Acceptance Check |
|---|---|---:|---|
| Route entry | `/artists` renders `ArtistsPage` inside `RequireLibraryData songs` and passes `visibleSongs`, playback state, playlists, favorites playlist id, recent searches, and library mutation callbacks. | `AppRoutes.tsx:390-450` | Opening `/artists` shows artists derived from visible songs and all play/add/favorite/recent-search callbacks work through app state. |
| Legacy route | `/artists/:artistName` redirects with replace to `/artists?artist=<artistName>`. | `AppRoutes.tsx:544-545`, `AppRoutes.tsx:56-63` | Opening `/artists/Adele` replaces the route with `/artists?artist=Adele`. |
| Compact breakpoint | Compact artist layout is `window.matchMedia('(max-width: 720px)')`. | `ArtistsPage.tsx:83`, `ArtistsPage.tsx:138`, `ArtistsPage.tsx:441-452` | At width `720px` and below, compact master/detail behavior is active. Above it, desktop split layout is active. |
| Artist grouping | Build artist groups from every song artist returned by `getSongArtists(song, common.artistUnknown)`. A multi-artist song appears in every artist group. When Electron falls back to splitting `song.artist`, it dedupes artist names case-insensitively while preserving the first spelling. | `artistsPageModel.ts:210-227`, `artists.ts:5-24`, `artists.ts:42-48` | A song with two artists appears under both artists. A song with no artist appears under Unknown Artist. Duplicate fallback artist names such as `Artist A; artist a` produce one group. |
| Artist sort | Sort artists by quick-jump bucket first, then `Intl.Collator('zh-Hans-CN-u-co-pinyin', numeric: true, sensitivity: 'base')`. | `artistsPageModel.ts:29-32`, `artistsPageModel.ts:135-143`, `artistsPageModel.ts:251-252` | Chinese, Latin, numeric, and symbol artists sort exactly like Electron. |
| Artist artwork | Artist artwork chooses the first artwork song in album/title order. If none exists, use empty artwork URL and latest song id for later artwork refresh/fallback. | `artistsPageModel.ts:231-247`, `ArtistsPage.tsx:1149-1163` | A group with artwork shows that artwork. A group without artwork shows default artwork. |
| Artist summary | Artist row and detail summary use album count plus song count. | `ArtistsPage.tsx:642-646`, `ArtistsPage.tsx:684-692` | Each artist row/detail shows the same album/song summary as Electron, no IDs. |
| Default desktop selection | If artists exist, no target artist is active, and current selection is missing, desktop selects the first visible artist. | `ArtistsPage.tsx:532-559` | Desktop `/artists` auto-opens the first artist detail. |
| Default compact selection | Compact layout clears selection when no valid target artist is active. | `ArtistsPage.tsx:548-553` | Compact `/artists` starts on the artist list, not detail. |
| Target artist | Existing `targetArtistName` selects that artist, records search, updates search text, and scrolls the master list to the artist. | `ArtistsPage.tsx:285-298`, `ArtistsPage.tsx:518-529` | `/artists?artist=<existing>` opens that artist and positions the list on it. |
| Missing target artist | A missing `targetArtistName` shows `collection.artistNotFound` notification for `3200ms`. | `ArtistsPage.tsx:523-526` | `/artists?artist=<missing>` shows Artist Not Found and does not open a fake detail. |
| Search source | Search suggestions are `searchArtists(artistGroups, artistSearch || searchQuery)`. Visible suggestions require focused input and non-empty local query. | `ArtistsPage.tsx:146-158` | Typing in Artists search returns artist matches only when focused. |
| Search result limit | Visible search suggestions are the first 8 matches. | `ArtistsPage.tsx:150-152` | More than 8 matching artists still renders only 8 suggestions. |
| Search ranking | Search score order is exact, case-insensitive exact, prefix, case-insensitive prefix, contains, case-insensitive contains, query contains value, then edit-distance score. | `artistsPageModel.ts:84-95`, `artistsPageModel.ts:146-185` | Search order matches Electron for exact, prefix, contains, and fuzzy cases. |
| Search submit | Enter/search button chooses exact artist match first, otherwise first suggestion. If no match and trimmed text exists, only records recent search. | `ArtistsPage.tsx:324-340`, `ArtistsPage.tsx:346-379` | Existing artist search opens the artist. Unknown search records history but does not invent a result. |
| Search clear | Clear button appears only when `artistSearch` is non-empty and sets it to empty. | `ArtistsPage.tsx:381-394` | Typing shows clear button. Clicking it clears the field and hides query suggestions. |
| Search history | Empty focused search shows up to 10 recent searches filtered to `type === 'artists'`. | `ArtistsPage.tsx:153-158`, `ArtistsPage.tsx:420-435` | Sidebar/albums search entries do not appear in Artists search history. |
| Search history actions | History panel can select, remove one entry, and clear current artists entries. | `ArtistsPage.tsx:420-435` | Selecting history chooses artist query. Remove and clear call the provided recent-search removal callbacks. |
| AppBar search | `AppBarSearch` is always mounted. AppBar placement auto-focuses input and Escape closes AppBar search. | `ArtistsPage.tsx:343-379`, `ArtistsPage.tsx:566-573` | Compact/AppBar search opens focused; Escape closes only AppBar search. |
| Loading/scanning progress | While `loading || scanning`, master shows `.artists-progress`; empty detail shows shared `LoadingState`. The master progress thumb uses `var(--accent)` with `900ms`; `LoadingState` is also an `empty-state`, so it keeps `16px 18px` padding, uses an `18px` spinner with `2px` border, `var(--accent)` top color, and `720ms` linear rotation; night mode applies the Artists empty-state background and border. | `ArtistsPage.tsx:575-578`, `ArtistsPage.tsx:830-833`, `LoadingState.tsx:8-13`, `common.css:90-99`, `common.css:174-190`, `artists.css:536-540`, `artists.css:994-999`, `index.css:39-40` | During scan/load, progress indicator is visible and empty detail shows the Electron loading UI. |
| Empty state | No selected artist and not loading/scanning shows `artists.selectArtist` when artists exist, otherwise `collection.noArtists` plus `artists.emptyCopy`. | `ArtistsPage.tsx:830-837` | Empty library shows no-artists title and copy. Unselected non-empty detail asks to select an artist. |
| Quick jump keys | Quick jump renders `#ABCDEFGHIJKLMNOPQRSTUVWXYZ`; buttons without an artist bucket are disabled. | `artistsPageModel.ts:27`, `ArtistsPage.tsx:579-599` | Exactly 27 quick-jump buttons render. Missing buckets are disabled. |
| Quick jump bucket | Latin letters map to their uppercase first letter, non-Latin/non-CJK to `#`, CJK to pinyin boundary bucket. Electron's `zh-Hans-CN-u-co-pinyin` collator places the polyphonic first character `长` in the `Z` bucket. | `artistsPageModel.ts:29-58`, `artistsPageModel.ts:110-133` | Latin, symbol, and Chinese artist names map to Electron buckets, including `长江 -> Z`. |
| Quick jump action | Clicking an enabled key scrolls the master list to the first artist in that bucket. Active key follows effective scroll position. | `ArtistsPage.tsx:204-210`, `ArtistsPage.tsx:274-282`, `ArtistsPage.tsx:584-593` | Clicking `C` scrolls to the first `C` artist; active highlight changes while scrolling. |
| Master virtualization | Master list uses `ARTIST_ROW_HEIGHT = 64`, overscan `10`, and top/bottom virtual spacers. | `artistsPageModel.ts:21-22`, `ArtistsPage.tsx:188-203`, `ArtistsPage.tsx:609-653` | Large artist libraries render only visible rows plus overscan while preserving scroll height. |
| Artist row open | Artist rows are role button, focusable, open detail on click, Enter, or Space. | `ArtistsPage.tsx:612-629` | Mouse and keyboard both open the artist detail. |
| Artist row context menu | Right-click artist row opens group menu for artist songs. | `ArtistsPage.tsx:630-632` | Right-click does not open detail; it opens artist group menu. |
| Artist row hover play | Hover/focus on artwork reveals play button. Clicking it stops row open, records artist played, and random plays artist songs. | `ArtistsPage.tsx:634-640`, `ArtistsPage.tsx:1149-1184` | Hover play appears, and click random plays only that artist's songs. |
| Artist detail header | Selected artist detail shows compact command row, title, summary, shuffle button, and more button. | `ArtistsPage.tsx:671-723` | Detail header content and buttons match Electron. |
| Artist detail shuffle | Detail shuffle records artist played and random plays all selected artist songs. Disabled when no songs. | `ArtistsPage.tsx:695-707` | Clicking shuffle creates a shuffled queue from only this artist's songs. |
| Artist detail more | Detail more opens artist group menu with `showLocateArtist = true`; also opens on context menu. Disabled when no songs. | `ArtistsPage.tsx:708-721` | Artist detail More menu includes Locate Artist. |
| Locate artist action | Detail artist group menu Locate Artist calls `scrollToArtist(menu.label)` and scrolls the master artist list to the selected artist row. | `ArtistsPage.tsx:906-908`, `ArtistsPage.tsx:1128-1134` | Clicking Locate Artist after moving the master list returns the selected artist row into view. |
| Compact back | Compact detail has a back button that clears selected artist, exits multi-select, clears selection, and replace-navigates to `/artists`. | `ArtistsPage.tsx:314-322`, `ArtistsPage.tsx:674-683` | In compact detail, Back returns to master list and clears selection state. |
| Compact title | Compact detail calls `onCompactTitleChange(selectedArtist.name)` and clears it on cleanup. | `ArtistsPage.tsx:510-516` | AppBar title becomes selected artist name only while compact detail is open. |
| Detail scroll reset | Changing selected artist scrolls detail to top and resets detail scroll state. | `ArtistsPage.tsx:505-508` | Selecting another artist starts detail at the top. |
| Album grouping | Selected artist songs are grouped by album, using `common.albumUnknown` for empty album. Album groups sort by Electron artist text order. | `ArtistsPage.tsx:170`, `artistsPageModel.ts:255-276` | Empty albums appear as Unknown Album. Album card order matches Electron. |
| Album group duration | Album group duration is the sum of song durations. | `artistsPageModel.ts:268-270` | Album summary duration equals the sum of song durations. |
| Album card link | Album title link goes to `getAlbumRoute(routeBase, album.name)`. | `ArtistsPage.tsx:734-738`, `artistsPageModel.ts:318-321` | Clicking album title navigates to the matching album route/query. |
| Album shuffle | Album shuffle records album played and random plays only that album's songs. | `ArtistsPage.tsx:740-751` | Album shuffle queue contains only songs from that album group. |
| Album more | Album more opens album group menu on click or context menu. | `ArtistsPage.tsx:752-764` | Album More menu uses album behavior and includes See Album, not Locate Artist. |
| Album virtualization | Detail album list virtualizes by estimated card heights with album overscan `2`. | `ArtistsPage.tsx:171-186`, `artistsPageModel.ts:279-314` | Large album lists keep correct scroll height and render only visible card range plus overscan. |
| Song list | Album songs render `PlaylistControlItem` with `showAlbum=false`, non-draggable, and queue ids from all selected artist songs. Multi-artist song row text uses `common.artistSeparator` between artist links. Compact song row title uses Electron's `16px`, `font-weight: 760`, and `line-height: 1.3`. Song artwork play/current overlays are Electron `38px` circles with `17px` icons and `blur(18px) saturate(150%)` backdrop. | `ArtistsPage.tsx:768-817`, `PlaylistControlItem.tsx:91-95`, `PlaylistControlItem.tsx:336-345`, `now-playing.css:375-434`, `now-playing.css:493-497`, `now-playing.css:856-859` | Song rows do not show album column; playing a row uses the artist queue; localized artist separators, compact title typography, and artwork overlays match Electron. |
| Song row menu | Song context menu uses `MusicMenuFlyout` with play, move-to-music-or-play, play next, reveal, delete, favorite, add-to-playlist, and select. | `ArtistsPage.tsx:914-935` | Right-click a song shows the shared music menu with Select. |
| Song row current playback menu | When the menu song is the current track and playback is active, the first song context menu item is Pause and Play Next is not shown for that song. | `MenuFlyoutHelper.ts:310-322`, `MusicMenuFlyout.tsx:118-132` | Right-click the playing song shows Pause; clicking it toggles playback pause and does not show Play Next. |
| Song row add-to | Song row add-to opens add-to menu for exactly that song, default playlist name is song title. | `ArtistsPage.tsx:788-797` | Single-song Add To uses one song id and default name equals song title. |
| Song row favorites | Song context menu Add To > My Favorites is shown only when the song is not favorite and toggles favorite with an undo action that restores the original favorite state. | `MenuFlyoutHelper.ts:314-328`, `MusicMenuFlyout.tsx:166-175` | Right-click song Add To > My Favorites favorites only that song; Undo unfavorites it. |
| Song row New Playlist | Song context menu Add To > New Playlist uses the shared add-to helper text dialog with title `playlists.newName`, default value from the song title, placeholder `playlists.namePlaceholder`, no playlist-name validation, and no explicit `confirmText`, so `InputDialog` uses `common.confirm`. | `MenuFlyoutHelper.ts:184-205`, `dialogService.ts:31-34`, `DialogHost.tsx:31-40`, `InputDialog.tsx:6-18`, `InputDialog.tsx:44-46` | Right-click song Add To > New Playlist accepts typed text with the Electron confirm label and creates a playlist containing only that song. |
| Song row Add To Now Playing | Song context menu Add To > Now Playing appends only the clicked song id and the undo action removes the inserted one-song range. | `MenuFlyoutHelper.ts:314-328`, `MusicMenuFlyout.tsx:132-139` | Right-click song Add To > Now Playing appends that song; Undo restores the previous queue. |
| Song row Add To Playlist | Song context menu Add To > custom playlist adds only the clicked song id to that playlist and the undo action removes that same song id from the playlist. | `MenuFlyoutHelper.ts:314-328`, `MusicMenuFlyout.tsx:143-148` | Right-click song Add To > Mix adds the selected song; Undo removes it from Mix. |
| Add To playlist filtering | Add To hides built-in playlists and, for a single song, hides custom playlists that already contain that song. Multi-song group Add To keeps custom playlists even if they contain some selected songs. | `MenuFlyoutHelper.ts:150-160`, `ArtistsPage.tsx:944-965`, `ArtistsPage.tsx:1051-1085` | Single-song Add To hides a playlist containing that song; group Add To still shows the custom playlist for multiple songs. |
| Song row delete | Song context menu Delete From Disk requests confirmation, then uses the shared pending delete flow with undo and commit callback. | `MusicMenuFlyout.tsx:188-195`, `useDeleteSongFromDisk.ts:13-24`, `AppRoutes.tsx:439-440` | Right-click song Delete From Disk confirms the selected song id, begins pending delete, and Undo restores the pending delete without commit. |
| Song row preference | Song context menu Preference writes `type='song'`, item id from the song id, name from the song title, and the selected preference level; when a preference exists, Undo Prefer removes that song preference. | `MusicMenuFlyout.tsx:160-166`, `MenuFlyoutHelper.ts:350-382` | Right-click song Preference > Higher writes the song preference; existing song preference shows Undo Prefer and removes it. |
| See Artist/Album | Song row `onSeeArtist` navigates to artist route; `onSeeAlbum` navigates to album route using Unknown Album fallback. | `ArtistsPage.tsx:810-815` | Row menu navigation matches Electron. |
| Song row View dialog modes | Song row View submenu opens Music Info, Lyrics, or Album Art directly in the selected `MusicDialog` mode, keeps those dialog actions open from the menu, and includes See Local File. | `MenuFlyoutHelper.ts:410-418`, `MusicMenuFlyout.tsx:176-184` | Right-click song View > See Lyrics opens the lyrics view instead of the properties view; View also includes See Local File without invoking the system reveal during tests. |
| Selection state | Multi-select state is stored by page key `artists`; selected song ids are stored under `artists:selectedSongIds`. | `ArtistsPage.tsx:120-121` | Leaving and returning to page preserves state according to page selection store behavior. |
| Select one song | Song menu Select enters multi-select and selects only that song. | `ArtistsPage.tsx:244-247`, `ArtistsPage.tsx:933-934` | Selecting from one row leaves exactly that song selected. |
| Select album songs | Album group menu Select selects all songs in that album. | `ArtistsPage.tsx:1105-1115` | Album menu Select enters multi-select with only album songs selected. |
| Artist multi-select | Artist group menu Multi Select enters multi-select and clears current selection. | `ArtistsPage.tsx:900-904`, `ArtistsPage.tsx:1105-1115` | Artist menu Multi Select shows bar with zero selected until user selects. |
| Multi-select bar | Bar is visible when `multiSelect`; selected count uses selected ids that are present in current artist queue. | `ArtistsPage.tsx:166-168`, `ArtistsPage.tsx:846-874` | Selection from another artist is not counted in current artist detail. |
| Multi-select operation hiding | `hideMultiSelectCommandBarAfterOperation` hides the bar for the command bar's direct Play action. ArtistsPage uses `onAddToPlaylistMenuClick` for Add To, so opening the external Add To menu and choosing Now Playing/Favorites/New Playlist/custom playlist does not call the command bar `hideIfNeeded`. | `MultiSelectCommandBar.tsx:98-103`, `MultiSelectCommandBar.tsx:164-181`, `ArtistsPage.tsx:854-861`, `ArtistsPage.tsx:944-970` | Play Selected hides selection when the setting is on; Add To menu operations keep the selection bar visible. |
| Multi-select play | Multi-select Play starts from first effective selected id and uses effective selected ids as queue. | `ArtistsPage.tsx:851-853` | Selected songs play in selected-id order. |
| Select all | Select All selects every song id in selected artist queue. | `ArtistsPage.tsx:863-865` | Select All count equals selected artist song count. |
| Reverse selection | Reverse Selection selects every current artist queue id not already selected. | `ArtistsPage.tsx:866-868` | Reverse toggles selection relative to current artist queue only. |
| Clear/cancel selection | Clear Selection clears ids. Cancel exits multi-select and clears ids. | `ArtistsPage.tsx:263-265`, `ArtistsPage.tsx:869-873` | Clear keeps the bar open with zero selected; Cancel hides the bar and clears ids. |
| Group menu order | Group menu items are Shuffle, Add To, Multi Select/Select, Preference, then Locate Artist for artist detail menus or See Album for album menus. Locate Artist uses Electron `nowPlaying` icon, mapped by Electron icons to `AppsListDetailRegular`. | `ArtistsPage.tsx:1091-1145`, `icons.tsx:215-221`, `icons.tsx:430` | Right-click artist/album menu item order, labels, and Locate Artist icon match Electron. |
| Group menu shuffle | Group menu Shuffle random plays menu songs and records artist or album played depending on menu type. | `ArtistsPage.tsx:892-899`, `ArtistsPage.tsx:1096-1102` | Shuffle from artist menu records artist. Shuffle from album menu records album. |
| Group Add To | Group Add To includes Now Playing, Favorites only when some songs are not favorite, create playlist, and custom playlists. | `ArtistsPage.tsx:1051-1085` | Fully favorite group hides Add to Favorites; otherwise it appears. |
| Add to Now Playing undo | Group Add To Now Playing appends songs and undo removes the inserted queue range. | `ArtistsPage.tsx:1058-1063` | Undo removes only the just-added songs. |
| Add to Favorites undo | Group Add To Favorites only adds non-favorite songs and undo removes them from favorite playlist. | `ArtistsPage.tsx:1034`, `ArtistsPage.tsx:1065-1070` | Undo after favorite add un-favorites only those newly added songs. |
| Add to Playlist undo | Group Add To Playlist adds songs to target playlist and undo removes them from that playlist. | `ArtistsPage.tsx:1078-1083` | Undo after playlist add restores the playlist membership. |
| Create playlist dialog | Request create playlist opens `RenameDialog` with default name from group label, add-to menu default name, or selected artist/common artists; validation receives `customPlaylists`, so built-in playlists do not block the name; `RenameDialog` does not pass `confirmText`, so `InputDialog` uses `common.confirm`. | `ArtistsPage.tsx:187`, `ArtistsPage.tsx:854-861`, `ArtistsPage.tsx:944-987`, `ArtistsPage.tsx:1072-1077`, `RenameDialog.tsx:5-31`, `InputDialog.tsx:6-18` | Create playlist default name and confirm label match Electron, and a built-in playlist with the same name does not block creation. |
| Preference menu | Group preference item type is `artist` or `album`; it refreshes preference settings and updates menu state. | `ArtistsPage.tsx:1035-1049`, `ArtistsPage.tsx:1087-1089`, `ArtistsPage.tsx:1117-1124` | Setting preference for artist updates `settings.artists`; album updates `settings.albums`. |
| Custom scrollbars | Master and detail both use `CustomScrollbar` and `useCustomScrollbar` with their own scroll frame/container/track refs. | `ArtistsPage.tsx:126-132`, `ArtistsPage.tsx:472-483`, `ArtistsPage.tsx:655-658`, `ArtistsPage.tsx:841-844` | Dragging each custom scrollbar moves only its associated list/detail. |

## UI Style Matrix

| Area | Electron Style Rule | Evidence | Acceptance Check |
|---|---|---:|---|
| Page layout | Root is grid with columns `minmax(240px, 300px)` and `minmax(0, 1fr)`, height `100%`, no padding, hidden overflow, transparent background. | `artists.css:1-10` | Desktop screenshot shows fixed-width master and flexible detail with no outer padding. |
| Master panel | Master has CSS vars: right gutter `14px`, scrollbar right `-12px`, shadow gutter `8px`; padding `16px 14px 8px`; right border `rgba(86,98,113,.18)`. | `artists.css:12-23` | Master spacing and divider match Electron. |
| Progress bar | Progress is `3px` high, fully rounded, accent track at 12% alpha; animated thumb width `34%`, `900ms`. | `artists.css:25-45` | Loading/scanning progress line size, color, and animation match Electron. |
| Search shell | Search shell is relative and full-width. | `artists.css:47-50` | Suggestions align to the input shell width. |
| Search field | Search form is 40px high grid, columns `40px 1fr`, gap `10px`, radius `10px`, background `rgba(13,24,38,.035)`, inset border/shadow. With query, columns become `40px 1fr 24px`. | `artists.css:52-74` | Search field height, icon column, clear column, radius, and background match Electron. |
| Search input text | Input is transparent, borderless, font size `14px`, `line-height: normal`, placeholder `rgba(61,73,88,.62)`. | `artists.css:76-99` | Text metrics and placeholder color match Electron. |
| Search focus | Focus background is white with accent inset border `.48` and outer glow `0 0 0 3px rgba(accent,.1)`. | `artists.css:91-96` | Focus state visibly matches Electron. |
| Search buttons | Submit button `40x40`, clear button `24x24` radius `6px`; submit icon `19px`, clear icon `14px`; hover uses accent alpha. | `artists.css:107-150` | Search and clear button size, icon size, and hover states match Electron. |
| Search suggestions panel | Suggestions popup top offset `8px`, z-index `121`, gap `6px`, max-height `min(360px, 100vh - 190px)`, padding `8px`, border radius `14px`, background `rgba(244,246,249,.96)`, shadow, blur `18px`. | `artists.css:153-170` | Popup position, blur, shadow, and max height match Electron. |
| Search suggestion row | Suggestion row min-height `38px`, radius `10px`, padding `5px 10px`, text left, hover accent `.08`; main text `14px` weight `560`, small text `12px`. | `artists.css:172-208` | Suggestion row height, padding, hover, and typography match Electron. |
| Master list shell | List shell is flex, fills remaining height, gap `4px`, top margin `14px`. | `artists.css:211-217` | Quick jump/list spacing starts 14px below search. |
| Master scroll frame | Master scroll frame flexes, min size zero, overflow visible; scrollbar track right is CSS var, width `9px`; thumb insets `2px`, hover `1px`. | `artists.css:219-241` | Custom scrollbar position and hover width match Electron. |
| Quick jump rail | Quick jump rail width `22px`, 27 equal rows, padding `2px 0`. | `artists.css:244-252` | Rail contains 27 evenly distributed buttons. |
| Quick jump buttons | Buttons are `20px` wide, full row height, radius `5px`, font `10px` weight `650`, muted color; hover/active accent background `.12`; disabled color `rgba(91,105,122,.24)` opacity `.62`. | `artists.css:254-285` | Enabled, active, hover, and disabled states match Electron. |
| Artists list container | List is vertical flex, margin-left `-8px`, margin-right `-14px`, padding-left `8px`, padding-right `14px`, overflow auto. | `artists.css:288-300` | Row hover shadows are not clipped and right gutter aligns with scrollbar. |
| Generic artist row | Generic row height `88px`; item grid `64px 1fr`, min-height `86px`, padding `11px 14px`, radius `10px`, hover translateY `-1px`, hover card shadow, selected accent border and shadow. | `artists.css:308-348` | Any non-master artist card surface matches Electron. |
| Master artist row | Master override uses grid `48px 1fr`, virtual row height `64px`, min-height `62px`, column gap `12px`, padding `5px 6px`; hover does not translate. | `artists.css:396-424` | Left list rows are compact 64px rows with 48px artwork. |
| Artist artwork | Generic artwork/avatar `64x64`, radius `8px`; master artwork/avatar `48x48`; artwork object-fit cover and fallback fills the box. | `artists.css:350-385`, `artists.css:413-418` | Artwork size changes between generic and master contexts exactly as Electron. |
| Artist hover play | Hover play overlay is centered `44x44` circle, background `rgba(30,34,40,.72)`, white icon, blur `18px`, opacity 0 until hover/focus, hover scale `1.1`; icon `19px`. | `artists.css:435-482` | Hover/focus reveals overlay play button with matching size and animation. |
| Artist row text | Copy uses `display: contents`; text ellipsizes; title font `15px`, weight `720`; subtitle `13px`, muted. Master line heights are `1.2` and `1.15`. | `artists.css:427-432`, `artists.css:485-508` | Long artist names/subtitles truncate like Electron and line heights match. |
| Detail surface | Detail scroll frame background `rgba(248,251,254,.96)`; detail padding `0 28px calc(player radius + 12px)`; empty state is centered with transparent card styling. | `artists.css:510-540` | Detail panel background and padding match Electron. |
| Detail header | Header is sticky top `0`, z-index `4`, flex, gap `18px`, margin `0 -28px`, padding `22px 28px 18px`, gradient translucent background, blur `18px`, shadow. | `artists.css:543-557` | Scrolling keeps header sticky with matching translucent blur. |
| Detail back button | Back button hidden by default, `38x38`, fully rounded, transparent, hover background `rgba(12,22,35,.06)`, icon `20px`. | `artists.css:559-580` | Desktop hides back. Compact shows matching circular back button. |
| Detail title text | Detail title margin `0`, color strong, font `28px`, line-height `1.2`; summary margin top `8px`, color `#111`, font `16px`. | `artists.css:590-605` | Header title and summary typography match Electron. |
| Header action buttons | Artist and album action buttons are `38x38`, fully rounded, transparent, icon `20px`, gap between buttons `6px`; disabled opacity `.45`; hover background `rgba(12,22,35,.06)`. | `artists.css:608-653` | Shuffle/more controls match size, radius, disabled, hover, and icon size. |
| Album list spacing | Album list top padding `22px`; card margin-bottom `22px`. | `artists.css:656-669` | Album cards start and separate by Electron spacing. |
| Album card | Album section overflow hidden, border `rgba(126,139,154,.18)`, radius `10px`, background `rgba(255,255,255,.64)`, shadow `0 16px 38px rgba(68,88,112,.08)`. | `artists.css:667-674` | Card background, border, radius, and shadow match Electron. |
| Album header | Header grid `80px 1fr 84px`, gap `16px`, min-height `112px`, padding `16px 18px`. | `artists.css:676-684` | Album header layout and action column width match Electron. |
| Album artwork | Album artwork `80x80`, radius `6px`, object-fit cover, fallback centered; fallback image fills box. | `artists.css:686-705` | Album artwork and fallback size/radius match Electron. |
| Album text | Album link font `18px` weight `700`, ellipsis, no underline; hover accent. Summary margin top `8px`, muted, font `14px`. | `artists.css:707-730` | Album title and summary typography/hover match Electron. |
| Artist song list | Song list is vertical, no gap, top border `rgba(126,139,154,.16)`. | `artists.css:732-737` | Separator above song rows matches Electron. |
| Artist song row columns | Desktop artist song row grid columns are `58px 1fr 136px 50px`. At max-width `1120px`, columns become `58px 1fr 34px 20px`, and hover/focus action column becomes `max-content`. | `artists.css:739-760` | Desktop and narrower row columns match Electron. |
| Song favorite visibility | At max-width `800px`, favorite action in artist compact playlist row is hidden. | `artists.css:762-765` | 800px and below hides favorite action in this list. |
| Nav-minimal master | Nav-minimal master padding `8px 14px calc(player radius + 8px)`, no right border; search hidden except appbar placement; list shell top margin 0; native scrollbars hidden. | `artists.css:768-809` | Minimal nav Artists layout matches Electron. |
| Night root/detail | Night workspace and detail backgrounds use `var(--night-shell-bg)`. | `artists.css:811-818` | Night mode Artists detail background matches Electron. |
| Night master | Night master border `var(--night-border)`, background `rgba(255,255,255,.025)`; nav-minimal master is transparent and borderless. | `artists.css:820-828` | Master panel night background and border match Electron. |
| Night search | Night search background `rgba(255,255,255,.055)`, text `var(--night-text)`, placeholder alpha `.52`; focus uses accent alpha `.14`; hover buttons use accent alpha `.18`. | `artists.css:830-875` | Search field night colors and focus/hover states match Electron. |
| Night suggestions | Night suggestions border `var(--night-border)`, background `rgba(29,35,43,.98)`, shadow `rgba(0,0,0,.36)`; row hover accent `.16`; small text muted. | `artists.css:877-898` | Search popup night colors match Electron. |
| Night quick jump | Night quick jump muted text; hover/active accent `.18`; disabled color `rgba(222,231,242,.25)`. | `artists.css:900-913` | Quick jump night enabled/active/disabled states match Electron. |
| Night artist rows | Night artist row text `var(--night-text)`, hover accent `.13`, active accent `.2` plus mixed accent text; avatar background mixes accent and `#2a3440`; subtitle muted. | `artists.css:916-940` | Master row night colors, hover, active, and fallback artwork match Electron. |
| Night detail header/actions | Night detail header gradient `rgba(15,19,25,.97/.88)` with shadow; buttons text night, hover accent `.16`; title/link night text; summaries muted. | `artists.css:942-978` | Detail header and action states match Electron in night mode. |
| Night album card | Night album card border `var(--night-border)`, background `var(--night-panel)`, shadow `rgba(0,0,0,.2)`; album link hover mixed accent. | `artists.css:980-991` | Album cards and links match night-mode Electron. |
| Night empty/song rows | Empty state night background `rgba(255,255,255,.045)`; song row selected/hover accent `.14`, current accent `.2`, row action hover accent `.18`. | `artists.css:994-1035` | Empty state and song row night states match Electron. |
| Responsive 860px | At max-width `860px`, detail padding becomes `0 18px 18px`; detail header margin `0 -18px`, padding `18px`; album header columns `64px 1fr 36px`; album artwork `64x64`. | `responsive.css:202-226` | 860px screenshot matches adjusted detail padding and album header/artwork sizes. |
| Responsive compact master/detail | At max-width `720px`, Artists page is block; master/detail/detail-scroll-frame height `100%`; detail frame hidden by default; when `.is-compact-detail-open`, master hidden and detail frame flex. | `responsive.css:312-339` | 700px list and detail are separate screens, never side-by-side. |
| Responsive compact search | At max-width `720px`, page master search is hidden; appbar page search is displayed. | `responsive.css:300-305` | Compact Artists uses AppBar search, not in-page master search. |
| Responsive compact back | At max-width `720px`, `.artist-detail-back-button` displays grid. | `responsive.css:341-347` | Compact detail shows the circular back button. |
| Responsive compact detail header controls | At max-width `720px`, detail header becomes a compact command/actions grid; back, shuffle, more, album shuffle, and album more buttons are `32x32`; icons are `17px`; detail action gap is `4px`. | `responsive.css:349-417` | Compact detail header buttons use the Electron compact sizing, not the desktop `38x38` controls. |
| Nav-minimal compact | In nav-minimal max-width `720px`, master right gutter `12px`, scrollbar right `-11px`, padding `0 12px 0`, list gap `2px`, quick jump padding top `2px` and bottom `calc(player radius + 2px)`, row padding `4px 6px`, column gap `10px`. | `responsive.css:560-590` | Minimal compact list spacing and quick-jump rail match Electron. |

## Verification Requirements

- Compare Flutter and Electron screenshots for desktop width `>= 1120px`, medium width around `860px`, compact width `700px`, and compact nav-minimal mode.
- Verify both day and night mode for search, quick jump, selected/hover rows, detail header, album cards, song rows, and empty states.
- Verify mouse and keyboard interactions: row click, Enter, Space, right-click, hover play, AppBar search Escape, compact back.
- Verify data behavior using songs that include: multi-artist tracks, unknown artist, unknown album, CJK names, Latin names, symbol-leading names, multiple albums, missing artwork, favorites and non-favorites.
- Any Flutter deviation not listed above needs new Electron evidence before implementation.

## Current Verification Notes

- `flutter test test/artists_page_test.dart --plain-name 'buildArtistGroups picks Electron artwork song'`: passed. Confirms artist artwork selection uses Electron's album/title ordering with `common.albumUnknown` for empty albums, while the artist song queue keeps Electron's raw album ordering.
- `flutter test test/artists_page_test.dart --plain-name 'artist quick jump buckets Chinese names like Electron pinyin'`: passed. Confirms the known Electron collator bucket case `长江 -> Z` in addition to existing CJK quick-jump buckets.
- `flutter test test/artists_page_test.dart --plain-name 'buildArtistGroups mirrors Electron multi-artist and unknown grouping'`: passed. Confirms multi-artist, unknown artist, and Electron case-insensitive fallback `song.artist` dedupe behavior.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage desktop defaults to first artist like Electron'`: passed. Confirms desktop Artists opens the first visible artist when no target/current selection exists.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage compact defaults to master list like Electron'`: passed. Confirms compact Artists starts on the master list instead of opening detail.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage artist row opens with Electron keyboard actions'`: passed. Confirms artist rows open detail by mouse, Enter, and Space without exposing IDs.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage artist row context menu mirrors Electron scope'`: passed. Confirms right-clicking an artist row opens the artist group menu without changing the selected detail.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage compact back replaces route and clears selection like Electron'`: passed. Confirms compact Back returns to `/artists`, clears the compact selection, exits multi-select, and clears selected ids.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage target route scrolls master list like Electron'`: passed. Confirms an existing target artist route records/applies the target and scrolls the master list to that artist.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage quick jump scrolls compact list like Electron'`: passed. Confirms compact quick-jump scrolls to the first artist in the selected bucket while keeping the rail visible.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage multi-select adds selected songs to playlist'`: passed. Confirms Artists external Add To keeps multi-select visible after adding selected songs to a playlist.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage Play Selected hides multi-select when Electron setting is on'`: passed. Confirms direct Play Selected still honors `hideMultiSelectCommandBarAfterOperation`.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage keeps selection when Electron setting is off'`: passed.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage multi-select bar mirrors Electron selection actions'`: passed. Confirms Select All, Reverse Selection, Clear Selection, Cancel, and Play Selected use the Electron current-artist queue scope and selection state.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage song menu New Playlist mirrors Electron text dialog'`: passed. Confirms the shared Add To text dialog uses the Electron `common.confirm` label and creates only the clicked song.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage create playlist defaults match Electron contexts'`: passed. Confirms Artists create-playlist dialogs use Electron defaults and the `common.confirm` button label from `RenameDialog`/`InputDialog`.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage create playlist ignores built-in name collisions like Electron'`: passed.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage song view menu opens MusicDialog'`: passed. Confirms View keeps dialog actions open and includes the Electron See Local File item; system reveal is not clicked in this test.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage song row uses Electron artist separator'`: passed. Confirms artist links in ArtistsPage song rows use localized `common.artistSeparator`, matching Electron `PlaylistControlItem`.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage night empty and song rows match Electron colors'`: passed. Confirms night song-row colors, compact song title typography, and current artwork overlay match Electron `16px` / `font-weight: 760` / `line-height: 1.3` plus `blur(18px) saturate(150%)`.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage song row uses PlaylistControlItem actions'`: passed. Confirms song-row hover actions and the Electron `38px` / `17px` / `blur(18px) saturate(150%)` artwork play overlay.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage loading state shows Electron progress strip'`: passed. Confirms loading master progress keeps Electron `3px`/`34%`/`900ms` visual rules, uses Electron `var(--accent)` for the progress thumb, exposes the Electron `nowPlaying.loading` progress label, and renders the detail loading spinner with Electron `empty-state` padding, night background/border, and `18px`/`2px`/`720ms` rules instead of Flutter's default Material spinner.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage scrollbar uses Electron night hover colors'`: passed. Confirms custom scrollbar thumb starts hidden like Electron `opacity: 0`, becomes visible on hover, and uses Electron night thumb colors.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage artist row focus reveals Electron hover play'`: passed. Confirms hover play opacity/size/scale/background and the Electron `blur(18px) saturate(150%)` overlay structure.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage night detail header matches Electron colors'`: passed. Confirms detail header gradient/shadow/text/action colors and the Electron `blur(18px) saturate(120%)` header structure.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage light detail actions keep Electron hover icon color'`: passed. Confirms light detail action hover keeps the Electron foreground color while applying the Electron hover background.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage light album title hover uses Electron accent'`: passed. Confirms light album title typography, no splash overlay, summary spacing, and accent hover color.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage compact detail More includes Locate Artist'`: passed. Confirms compact detail header back/shuffle/more controls follow Electron responsive compact `32x32` button and `17px` icon sizing while preserving Locate Artist menu behavior, including Electron `nowPlaying` / `AppsListDetailRegular` icon parity.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage group menu order matches Electron'`: passed. Confirms artist/album group menu order, labels, and Locate Artist icon mapping follow Electron.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage album group Select only selects album songs'`: passed. Confirms album group Select enters multi-select with only that album's song ids.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage 860px detail and album layout match Electron'`: passed. Confirms the medium-width detail padding, header spacing, album header columns, and album artwork sizing from Electron responsive CSS.
- `flutter test test/artists_page_test.dart --name 'ArtistsPage (Add To filters existing single-song playlists like Electron|group Add To keeps partial playlists like Electron|group Add To hides favorites when all are favorite|group Add To mirrors Electron undo and favorites|multi-select play starts from first effective song|clears stored selection on wide remount like Electron|artist summaries use Electron album and song counts|uses Electron 720px compact breakpoint|song View See Artist routes like Electron|song View See Lyrics opens lyrics mode|song View See Album Art opens album art mode|album title opens the Electron album route|song artist text opens the Electron artist route|See Album routes unknown albums through the i18n label|target route does not double-decode artist query|target route resets detail scroll like Electron|resets detail scroll when artist changes)'`: passed. Confirms the remaining non-search route, Add To filtering, group undo, View submenu modes, summary, compact breakpoint, detail scroll reset, and target-route decoding behaviors listed above.
- `flutter test test/artists_page_test.dart --name 'ArtistsPage (mounts Electron master and detail scrollbars|master scrollbar drag scrolls artist list|master list uses Electron overscan cache|compact master list uses Electron overscan cache|detail scrollbar drag scrolls only artist detail|virtualizes artist album cards like Electron|desktop detail bottom padding matches Electron|light master border matches Electron color|night nav-minimal master is Electron transparent|nav-minimal master matches Electron shell spacing|night surfaces match Electron shell colors|night quick jump matches Electron colors|night artist rows match Electron colors|light artist rows match Electron colors|empty library keeps Electron no-artists copy|800px song row hides Electron favorite action|801px song row keeps Electron favorite action)'`: passed. Confirms the non-search scrollbar, virtualization, responsive spacing, night/light row colors, empty state, and 800px favorite-action visibility rules listed above.
- `flutter test test/artists_page_test.dart --name 'ArtistsPage (song row play mirrors Electron shuffle queue|song row Add To writes the target playlist|compact detail writes Electron appbar title|legacy artist route redirects like Electron|artist group menu writes Electron preference|artist group menu can undo Electron preference|album group menu writes Electron preference|album group menu mirrors Electron actions|album More context menu mirrors Electron|Locate Artist scrolls master list like Electron|target route resets detail scroll like Electron|keeps quick jump visible in compact master list)'`: passed. Confirms the remaining non-search row playback/Add To, compact title, legacy route replacement, preference write/undo, album menu, Locate Artist, target-scroll reset, and compact quick-jump visibility behaviors listed above.
- `dart format lib/src/library/ui/artists_page.dart lib/src/library/ui/artists_page_master.dart lib/src/library/ui/artists_page_chrome.dart`: passed after an equivalent structural split. `_CompactArtistsPage`, `_ArtistsMaster`, and `_ArtistQuickJump` now live in `artists_page_master.dart`; loading, custom scrollbar, empty-state, summary formatting, and color helpers live in `artists_page_chrome.dart`. Search widget classes stayed in `artists_page.dart` and were not modified.
- `wc -l lib/src/library/ui/artists_page.dart lib/src/library/ui/artists_page_master.dart lib/src/library/ui/artists_page_chrome.dart lib/src/library/ui/artists_detail.dart lib/src/library/ui/artists_list_item.dart lib/src/library/ui/artists_master_detail.dart lib/src/library/ui/artists_page_state_helpers.dart`: `artists_page.dart` 1047, `artists_page_master.dart` 519, `artists_page_chrome.dart` 837, `artists_detail.dart` 929, `artists_list_item.dart` 372, `artists_master_detail.dart` 222, `artists_page_state_helpers.dart` 728. Confirms touched ArtistsPage structure remains below the project 1200-line class/file limit after the split.
- `dart analyze lib/src/library/ui/artists_page.dart lib/src/library/ui/artists_page_master.dart lib/src/library/ui/artists_page_chrome.dart lib/src/library/ui/artists_detail.dart lib/src/library/ui/artists_list_item.dart lib/src/library/ui/artists_master_detail.dart lib/src/library/ui/artists_page_state_helpers.dart test/artists_page_test.dart`: passed after the split.
- `flutter test test/artists_page_test.dart --name 'ArtistsPage (loading state shows Electron progress strip|mounts Electron master and detail scrollbars|master scrollbar drag scrolls artist list|detail scrollbar drag scrolls only artist detail|master list uses Electron overscan cache|compact master list uses Electron overscan cache|quick jump scrolls compact list like Electron|keeps quick jump visible in compact master list|nav-minimal master matches Electron shell spacing|night nav-minimal master is Electron transparent|empty library keeps Electron no-artists copy|night surfaces match Electron shell colors|night quick jump matches Electron colors|light artist rows match Electron colors|night artist rows match Electron colors|desktop defaults to first artist like Electron|compact defaults to master list like Electron|uses Electron 720px compact breakpoint)'`: passed after the split. Confirms the moved non-search master/compact/quick-jump/loading/scrollbar/color surfaces still satisfy the Electron evidence-backed tests.
- `flutter test test/artists_page_test.dart --name '^ArtistsPage (?!.*[sS]earch|.*suggestions|.*recent artist searches).*$'`: passed with 95 non-search ArtistsPage widget tests. Confirms the current non-search ArtistsPage behavior and UI assertions pass as a broad set after excluding the user-frozen search box/suggestion/history cases.
- `flutter test test/artists_page_test.dart --name '^(?!ArtistsPage).*Electron|formatDuration matches Electron hour display|artist quick jump buckets Chinese names like Electron pinyin|buildArtistGroups mirrors Electron multi-artist and unknown grouping|buildArtistGroups picks Electron artwork song'`: passed with 11 model/helper tests. Confirms Electron-backed artist grouping, artwork selection, raw album queue order, fallback album grouping/duration, quick-jump collation/buckets, numeric collation, virtual-window math, master overscan, duration formatting, and search scoring helper behavior.
- `ARTISTS_VERIFY_WIDTH=1200 ARTISTS_VERIFY_HEIGHT=800 ARTISTS_VERIFY_BRIGHTNESS=dark flutter run -d macos -t tool/artists_page_visual_verify.dart`: passed after the ArtistsPage structural split and regenerated `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_dark_1200.png` as a `1200 x 800` PNG. Confirms the split `artists_page_chrome.dart`/`artists_page_master.dart` runtime still renders the dark desktop non-search Artists surface.
- `ARTISTS_VERIFY_WIDTH=700 ARTISTS_VERIFY_HEIGHT=900 ARTISTS_VERIFY_BRIGHTNESS=light ARTISTS_VERIFY_NAV_MINIMAL=1 flutter run -d macos -t tool/artists_page_visual_verify.dart`: passed after the ArtistsPage structural split and regenerated `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_light_700_nav_minimal.png` as a `700 x 900` PNG. Confirms the split compact nav-minimal master/quick-jump surface still renders at runtime; top AppBar search differences remain intentionally frozen.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage quick jump renders Electron keys and disabled state'`: passed after correcting the master list/quick-jump geometry. Confirms `#ABCDEFGHIJKLMNOPQRSTUVWXYZ` still renders, the master list right edge now aligns to the master content/search right edge, and the final `Z` key stays above the Flutter player-safe bottom.
- `flutter test test/artists_page_test.dart --name '^ArtistsPage (?!.*[sS]earch|.*suggestions|.*recent artist searches).*$'`: passed again with 95 non-search ArtistsPage widget tests after the master list/quick-jump geometry fix.
- `ARTISTS_VERIFY_WIDTH=626 ARTISTS_VERIFY_HEIGHT=900 ARTISTS_VERIFY_BRIGHTNESS=light flutter run -d macos -t tool/artists_page_visual_verify.dart`: passed and wrote `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_light_626.png` as a `626 x 900` PNG. Confirms the narrow light Artists master surface renders after the quick-jump bottom and list-right-edge fix.
- `dart analyze lib/src/library/ui/artists_page_state_helpers.dart test/artists_page_test.dart`: passed.
- `dart analyze lib/src/library/ui/artists_page_model.dart test/artists_page_test.dart`: passed.
- `dart analyze lib/src/library/ui/artists_page.dart test/artists_page_test.dart`: passed.
- `dart analyze lib/src/library/ui/artists_page.dart lib/src/library/ui/artists_detail.dart lib/src/library/ui/artists_list_item.dart lib/src/playback/playlist_control_item.dart test/artists_page_test.dart`: passed after adding Electron saturate filters to the non-search blur overlays and splitting `PlaylistControlItem` overlay helpers into `playlist_control_item_overlays.dart` to keep the touched structure below the project length limit.
- `dart analyze tool/artists_page_visual_verify.dart lib/src/library/ui/artists_page.dart lib/src/library/ui/artists_detail.dart lib/src/library/ui/artists_list_item.dart lib/src/playback/playlist_control_item.dart test/artists_page_test.dart`: passed after adding a runtime ArtistsPage screenshot verifier.
- `node --check tool/electron_artists_visual_verify.mjs && ELECTRON_ARTISTS_VERIFY_PORT=5195 node tool/electron_artists_visual_verify.mjs`: passed. The Electron harness imports the original Electron `ArtistsPage`, `index.css`, and `App.css`, copies `public/app-icon.png` so `DefaultAlbumArtwork` renders the real fallback asset, captures frameless content screenshots, and writes same-size PNGs:
  - `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_artists_verify_light_1200.png`
  - `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_artists_verify_dark_1200.png`
  - `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_artists_verify_light_860.png`
  - `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_artists_verify_light_700.png`
  - `/var/folders/j8/78gnf9694sv7mygt9l_9smsr0000gn/T/electron_artists_verify_light_700_nav_minimal.png`
- `flutter test test/default_album_artwork_test.dart`: passed after aligning Flutter `DefaultAlbumArtwork` with Electron `DefaultAlbumArtwork.tsx` and `index.css`: `app-icon.png`, 68% logo size, light opacity `.9`, dark opacity `.78`, light `0 10px 18px rgba(38,57,82,.12)`, and dark `0 12px 22px rgba(0,0,0,.32)`.
- Runtime Flutter screenshot verifier: `tool/artists_page_visual_verify.dart` renders ArtistsPage through the macOS Flutter runtime with fixed Riverpod data and writes PNG screenshots after layout. Its local English strings are aligned with the Flutter/Electron `en-US.json` `artists.artistSummary` and `artists.albumSummary` middle-dot separators. Verified outputs:
  - `ARTISTS_VERIFY_WIDTH=1200 ARTISTS_VERIFY_HEIGHT=800 ARTISTS_VERIFY_BRIGHTNESS=light flutter run -d macos -t tool/artists_page_visual_verify.dart` -> `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_light_1200.png`
  - `ARTISTS_VERIFY_WIDTH=1200 ARTISTS_VERIFY_HEIGHT=800 ARTISTS_VERIFY_BRIGHTNESS=dark flutter run -d macos -t tool/artists_page_visual_verify.dart` -> `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_dark_1200.png`
  - `ARTISTS_VERIFY_WIDTH=860 ARTISTS_VERIFY_HEIGHT=800 ARTISTS_VERIFY_BRIGHTNESS=light flutter run -d macos -t tool/artists_page_visual_verify.dart` -> `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_light_860.png`
  - `ARTISTS_VERIFY_WIDTH=700 ARTISTS_VERIFY_HEIGHT=900 ARTISTS_VERIFY_BRIGHTNESS=light flutter run -d macos -t tool/artists_page_visual_verify.dart` -> `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_light_700.png`
  - `ARTISTS_VERIFY_WIDTH=700 ARTISTS_VERIFY_HEIGHT=900 ARTISTS_VERIFY_BRIGHTNESS=light ARTISTS_VERIFY_NAV_MINIMAL=1 flutter run -d macos -t tool/artists_page_visual_verify.dart` -> `/Users/luohaitian/Library/Containers/com.seaky.smplayerFlutter/Data/tmp/flutter_artists_verify_light_700_nav_minimal.png`
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage night empty and song rows match Electron colors'`: passed again after the `PlaylistControlItem` overlay split.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage song row uses PlaylistControlItem actions'`: passed again after the `PlaylistControlItem` overlay split.
- `flutter test test/artists_page_test.dart --plain-name 'ArtistsPage song row uses PlaylistControlItem actions'`: passed again after the default artwork parity fix.
- Electron-vs-Flutter screenshot comparison after the default artwork parity fix:
  - 1200 light: non-search Artists master/detail, fallback artwork, album cards, song rows, quick-jump rail, and header actions visually match the Electron harness.
  - 1200 dark: non-search Artists master/detail, fallback artwork, album cards, song rows, quick-jump rail, and header actions visually match the Electron harness.
  - 860 light: medium detail padding, album sizing, fallback artwork, and narrow song-row duration wrapping match the Electron harness.
  - 700 light: compact master list layout and fallback artwork match the Electron harness. The top AppBar/page search difference is intentionally not changed because search-box work is paused by user instruction.
  - 700 light nav-minimal: compact master list row sizing, quick-jump rail, fallback artwork, and summary text match the Electron nav-minimal harness for the ArtistsPage content. The top AppBar search entry remains outside this pass because search/AppBar search work is paused by user instruction.
- `git diff --check`: passed after the latest non-search overlay changes.
- Earlier widget-test screenshot writing through `RenderRepaintBoundary.toImage` did not complete reliably in the test runner and was not kept; the stable Flutter-side capture path is now the macOS runtime verifier above.
- `flutter test test/artists_page_test.dart`: 123 tests run; 120 passed after the ArtistsPage structural split and runtime screenshot re-verification. The 3 remaining failures are search-box/AppBar search/suggestion/history related and are intentionally not modified in this pass:
  - `ArtistsPage appbar search mirrors Electron night focus style`
  - `ArtistsPage night suggestions match Electron colors`
  - `ArtistsPage recent artist searches are limited like Electron`

## Paused Items

- Search box related ArtistsPage/AlbumsPage work is paused by user instruction. Do not change `PageSearchField`, suggestion/history panels, AppBar search sizing/spacing/radius/colors/button states, or search-box behavior without a new explicit request.
- Detail title inherited Electron `h2` negative `letter-spacing` from `index.css:129-142` is explicitly not changed because the project frontend constraint requires letter spacing to stay `0`; this is a recorded non-change, not an inferred Flutter parity decision.
- Remaining screenshot differences involving search boxes, AppBar search placement, suggestion/history panels, or search tests are paused by user instruction and are not part of this pass.

## Current Boundary

- Non-search ArtistsPage behavior and UI are covered by the broad 95-test ArtistsPage non-search run, 11 model/helper tests, targeted widget tests, Electron and Flutter runtime screenshot verifiers, and analyzer checks above.
- The only currently failing full-suite ArtistsPage tests are the three search-box/AppBar search/suggestion/history tests listed above. They are not fixed in this pass because the user explicitly froze search-box related work.
- Further movement on the remaining failing tests requires a new explicit instruction to resume search-box/AppBar search/suggestion/history parity work.
