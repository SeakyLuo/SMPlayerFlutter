# NowPlayingFullPage MediaControl UI Layout Acceptance Matrix

Electron is the source of truth for this target. Existing Flutter visual
components may be reused; this matrix validates structure, layout roles,
states, menus, and responsive behavior.

This target is not a pixel-style rewrite. Existing Flutter components such as
the shared media-control buttons, sliders, menu flyouts, icons, artwork/default
artwork, and loading indicators can be reused when they preserve the same
layout slot, visibility rule, state, and interaction. Colors, shadows, radius,
and animation curves are acceptance requirements only when they change
visibility, hit area, ordering, alignment, disabled/active meaning, or
responsive behavior.

## Electron Evidence

| Area | Source |
| --- | --- |
| Full page footer call site | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/pages/NowPlayingFullPage.tsx` |
| Shared player surface content | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/MediaControl.tsx` |
| Full page footer layout and theme overrides | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/now-playing-full.css` |
| Base player layout, transport, progress, utility rules | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/player.css` |
| 800px responsive full-footer override | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/styles/responsive.css` |
| Playback mode labels and mode order | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/mediaControlModel.ts` |
| Volume icon thresholds | `/Users/luohaitian/Desktop/Projects/SMPlayerElectron/src/components/volumeIcon.ts` |

## Extracted Electron CSS Dimensions

| Area | Electron selector/source | Confirmed CSS dimensions |
| --- | --- | --- |
| Base footer shell | `player.css` `.player-bar` | `height: var(--player-height)`, `padding: 10px 16px`, grid columns `minmax(280px, 0.9fr) minmax(420px, 1fr) minmax(280px, 0.9fr)`, rows `52px 38px`, `gap: 0`, top radius only. |
| 1200-721px non-minimal footer | `responsive.css` `@media (max-width: 1200px) and (min-width: 721px)` | side min `clamp(200px, 24vw, 280px)`, center min `clamp(280px, 40vw, 420px)`, utility padding `0 8px`, condensed utility gaps `8px`. |
| 800-721px compact footer | `responsive.css` `@media (max-width: 800px) and (min-width: 721px)` | grid `minmax(168px, 1fr) minmax(174px, 228px) 80px`, rows `74px 28px`, `column-gap: 10px`, `padding: 8px 16px 10px`. |
| Full-page compact footer | `now-playing-full.css` `.app-shell.nav-minimal .now-playing-full-player-bar.player-bar` and responsive override | columns `80px minmax(174px, 1fr) 80px`; at `<=520px`, columns `68px minmax(136px, 1fr) 68px` and inherits base nav-minimal `column-gap: 8px`, `padding: 9px 12px 11px`. |
| Transport | `player.css` `.transport-row`, `.transport-button` plus nav-minimal rules | wide row height `52px`, gap `26px`, skip buttons `36px`, primary `56px`; compact/nav-minimal row height `56px`, gap `16px`, primary `52px`; at `<=520px` primary is `48px` with `12px` padding. |
| Progress | `player.css` `.progress-row` plus nav-minimal rules | wide columns `44px minmax(0, 1fr) 44px`, height `36px`, gap `12px`; compact columns `42px minmax(0, 1fr) 42px`, height `28px`, gap `8px`, padding `0 9px`. |
| Utility | `player.css` `.player-utility`, `.player-volume-row`, `.player-mode-row` plus compact rules | wide utility rows `44px 44px`, padding `0 16px 0 12px`; buttons `36px`; volume row columns `36px 148px 36px`, gap `14px`; mode row gap `14px`; responsive condensed utility with voice needs `140px` (`36 + 8 + 36 + 8 + 36` plus `8px` side padding); compact/minimal utility width `80px` or `68px` at `<=520px`, mode gap `6px`, button size `36px` until `<=520px`, then `34px`. |

## Component Reuse Policy

| Component area | Electron source role | Flutter reuse allowed | Acceptance boundary |
| --- | --- | --- | --- |
| Footer shell | `footer.player-bar.now-playing-full-player-bar` hosts the whole immersive control. | Reuse Flutter full-page footer/container component. | Must keep fixed bottom placement, top-only radius, raised/idle transform role, and three layout columns. |
| Exit control | `button.player-track.now-playing-full-player-exit` contains only artwork shell plus fullscreen-exit overlay. | Reuse existing icon/artwork shell widgets. | Must stay in the left column, keep the same hit area role, and not display title/artist text in the immersive footer. |
| Transport controls | `MediaControlSurface` renders previous, primary play/pause/loading, next. | Reuse existing transport button components. | Must preserve order, center alignment, primary size per breakpoint, disabled/loading states, and callbacks. |
| Progress row | `MediaControlSurface` renders elapsed time, range input/loading strip, duration time. | Reuse existing progress/slider implementation. | Must preserve row order, fixed time columns, full-width center slider, draft seek behavior, and disabled duration handling. |
| Utility controls | `MediaControlSurface` renders wide volume/favorite/modes plus compact mode/voice/more. | Reuse existing utility buttons, volume slider, and menus. | Must preserve which controls are visible at each width and the right-column containment. |
| Menus/flyouts | More, compact playback mode, compact volume, and voice flyouts open from player controls. Full page pins the footer only for `moreMenu` and `dialogMode`; compact mode and compact volume are internal `MediaControl` overlays and are not part of `NowPlayingFullPage`'s `isPlayerBarPinned` state. | Reuse `MenuFlyout` and Flutter flyout helpers. | Must preserve anchor side, checked items, disabled states, and the confirmed page-level pinning rules. |

## Flutter Surfaces

| Area | Source |
| --- | --- |
| Full page host and footer state | `lib/src/playback/immersive_mode_page.dart` |
| Shared player frame | `lib/src/playback/media_control_frame.dart` |
| Transport and progress | `lib/src/playback/media_control_surface.dart` |
| Utility controls | `lib/src/playback/media_control_utility.dart` |
| Player menus | `lib/src/playback/media_control_menus.dart` |
| Full page tests | `test/now_playing_page_test.dart` |
| Shared media control tests | `test/media_control_widget_test.dart` |

## Content And Behavior Matrix

| ID | Electron rule | Flutter target | Proof |
| --- | --- | --- | --- |
| NPF-MC-01 | Full page footer renders a `player-bar now-playing-full-player-bar` with a left exit button and a shared `MediaControlSurface`. | Full page footer has exactly one player surface: exit button on the left, transport/progress in the center, utility on the right. | Widget geometry test and screenshot. |
| NPF-MC-02 | Exit button label/title is `nowPlaying.exitImmersiveMode`; click calls `goBack`. | Exit button leaves immersive route and does not expose song ID or debug text. | Widget route/callback test. |
| NPF-MC-03 | Full page passes `isLoading={false}` into `MediaControlSurface`. Electron playback load failure recovers playback state to paused/idle and does not write the full-page `error` prop; `NowPlayingFullPage` only renders `.now-playing-full-error` for the page-level `error` prop. | Full footer never shows the loading progress bar solely because the shared controller is loading, and playback load failure alone does not create an unconfirmed full-page error banner above the footer. | Existing widget test. |
| NPF-MC-04 | Disabled state comes from no current song. | With no current song, transport, progress, wide volume, compact volume, and favorite are disabled/hidden like Electron; the 721-1200px compact volume replacement is visible but disabled and cannot open its popover. | Widget test at wide and 1000px condensed utility widths. |
| NPF-MC-05 | Transport order is previous, play/pause, next. Electron full-page previous is a plain button with `aria-label/title=player.previous`; restart-current behavior is inside the playback controller click action and there is no extra long-hold/force-previous UI in `MediaControlSurface`. | Buttons keep order and callbacks; full-page previous keeps the plain Electron tooltip and has no hold-release action even when restart-current settings would make the click restart the current track. | Widget test. |
| NPF-MC-06 | Progress uses draft seek while dragging and commits only after a seek began. Electron range input also exposes `aria-label={t('player.trackProgress')}` and `title={formatDuration(progressValue)}`. | Drag updates displayed progress; release without begin does not call seek; Flutter progress slider exposes the same localized semantic label and formatted progress value. | Shared media control widget test plus full-page semantics test. |
| NPF-MC-07 | Duration fallback is `durationSeconds || currentSong.duration || 0`. | Backend duration 0 falls back to current song duration for both duration label and progress slider max. | Widget test. |
| NPF-MC-08 | Disabled surface uses volume value 0. | Disabled full footer shows disabled volume controls with value 0. | Widget test. |
| NPF-MC-09 | Favorite is hidden when `trackId == null`; active uses filled heart. | No song hides main favorite; current song toggles favorite. | Widget test. |
| NPF-MC-10 | Wide mode row exposes shuffle, repeat, repeat-one independently; only the current mode button receives `is-active`, while `once` leaves all three wide mode buttons inactive. | Wide full footer has independent mode buttons, active styling on only the matching mode, and repeat can toggle back to once without using the compact mode button. | Widget test. |
| NPF-MC-11 | Compact mode cycles once, shuffle, repeat, repeat-one, once; long press or context menu opens the checked mode menu, and both the cycle action and mode-menu open path close any open compact volume popover first. | Compact full footer follows the same cycle and menu checked state, and the compact volume popover does not remain open behind the compact mode interaction. | Widget test. |
| NPF-MC-12 | Voice assistant appears only on Windows from `platform === 'win32'`. | macOS path omits voice; when shell exposes voice, utility keeps voice plus More without overflow. | Widget test. |
| NPF-MC-13 | Full-page More requests the More button's `rect.left`/`rect.top`, then `MenuFlyout` clamps the panel inside the viewport boundary; because the anchor is inside `.player-bar`, the bottom boundary is `window.innerHeight - 8`. This differs from the shared bottom-player More menu, which requests `rect.top - 8`. | Opening More does not allow footer auto-hide until menu closes, and its first panel follows the full-page requested anchor plus viewport clamp. | Widget test. |
| NPF-MC-14 | Full page More has queue-level actions: Quick Play, Random Play, Save Playlist, Clear Now Playing. Random Play is a root-level submenu from `getShuffleMenuItems`; it is disabled only when both queue and library are empty, and this rule still applies when compact More adds Playback Mode, Volume, and Favorite. | These items are present; Random Play is disabled when both queue and library are empty, remains in the root More panel, cannot open its shuffle submenu while disabled, and when enabled opens the Electron shuffle submenu without closing the root More panel in both wide and compact full-page More. | Widget/menu test. |
| NPF-MC-15 | Compact More also includes Playback Mode, Volume, Favorite. | At compact widths, More includes those utility substitutes; Favorite is disabled without a current song. | Widget/menu test. |
| NPF-MC-16 | Add To exists only with a current song; it includes Now Playing, excludes built-in playlists, and compact excludes the Favorites shortcut because compact has its own Favorite item. | Shared Add To helper is used with `includeNowPlaying: true` and `includeFavorites: !isCompact && !currentSong.favorite`; wide current-song Add To shows My Favorites for an unfavorited song, compact current-song Add To does not. | Widget/menu submenu test. |
| NPF-MC-17 | Preference state is refreshed after More opens. | Preference lookup is async and updates the existing menu; no repeated build-time database calls. When the lookup resolves with an existing level, the Preference submenu gains Undo Preference and checks the matching level. | Widget/menu async update test. |
| NPF-MC-18 | Full page More includes Play Artist and Play Album as direct current-song actions. Both actions filter from the full library by the current song's artist/album, randomize the matching IDs, replace the queue, and play the randomized first item. Electron full page uses `getNowPlayingFullMoreItems`, which returns after the View submenu and does not append the shared bottom-player Full Screen/Exit Full Screen or Mini Mode items. | Keep Play Artist and Play Album outside the View submenu, matching `getNowPlayingFullMoreItems`; do not add bottom-player Full Screen/Exit Full Screen or Mini Mode actions to full-page More; verify artist/album replace Now Playing with only the Electron-matching ID set. | Widget/menu behavior test. |
| NPF-MC-19 | View submenu in the current full page opens Music Info, Lyrics, Album Art dialogs. Unlike the shared bottom player View submenu, full page does not include See Artist, See Album, or Local File in this submenu. | Keep only those three confirmed dialog entries; do not add unconfirmed global bottom-player view entries here. | Widget/menu test. |
| NPF-MC-20 | Clear Now Playing closes full page and replaces the queue with empty. | Clear action exits immersive page and clears queue. | Widget test. |
| NPF-MC-21 | Add to Now Playing undo removes only the inserted queue range recorded from the pre-add queue length, matching Electron `removeQueueRange(currentQueue, insertedIndex, 1)`. | Undo does not remove existing duplicate song IDs; a queue like `[1, 2, 1]` becomes `[1, 2, 1, 1]` after Add To Now Playing and returns to `[1, 2, 1]` after Undo. | Widget undo behavior test. |

## UI Layout Matrix

| ID | Electron rule | Flutter target | Proof |
| --- | --- | --- | --- |
| NPF-UI-01 | The immersive footer is `footer.player-bar.now-playing-full-player-bar` with `position: fixed`, `left/right/bottom: 0`, top-only radius, and no bottom radius. Wide/base footer keeps the full-page `z-index: 260`, while the queue popover is `z-index: 220`, so wide footer paints above the queue. In the compact `max-width: 800px` and nav-minimal cascades, later `player.css`/`responsive.css` rules set `.player-bar` to `z-index: 110`, so the `z-index: 220` queue popover paints above the footer. Wide/base footer keeps the base `1px` border on all sides; nav-minimal/compact keeps only the top border. Night wide full footer is controlled by the later and more specific `body.night-mode .app-shell .player-bar` cascade from `player.css`: `blur(28px)`, border `var(--night-border)`, background `linear-gradient(135deg, rgba(255,255,255,0.055), rgba(var(--accent-rgb),0.12)), linear-gradient(90deg, rgba(var(--player-cover-rgb),0.22), transparent 46%), rgba(17,22,28,0.9)`, and shadow `0 -18px 48px rgba(0,0,0,0.34), inset 0 1px 0 rgba(255,255,255,0.045)`. Night compact/nav-minimal is controlled by the later `player.css` cascade: `blur(28px) saturate(145%)`, border `var(--night-border)`, background `linear-gradient(135deg, rgba(var(--player-cover-rgb),0.2), rgba(17,22,28,0.78) 56%), linear-gradient(180deg, rgba(29,35,43,0.88), rgba(16,20,25,0.88)), rgba(16,20,25,0.92)`, and shadow `0 -12px 36px rgba(0,0,0,0.34), inset 0 1px 0 rgba(255,255,255,0.045)`. Day full footer overrides `backdrop-filter: blur(18px) saturate(140%)`, border to `rgba(204,213,224,0.72)`, background to `radial-gradient(circle at 20% 22%, rgba(var(--now-playing-full-cover-rgb),0.24), transparent 42%), rgba(255,255,255,0.78)`, and shadow to `0 -18px 56px rgba(68,88,112,0.14), inset 0 1px 0 rgba(255,255,255,0.78)`. | Footer stays bottom-aligned, independent from lyric/queue scrolling, keeps the Electron wide-vs-compact queue paint order, keeps top-only rounded corners, Electron backdrop blur/saturation/background/border/shadow/inset highlight per width and theme bucket, and no detached card wrapper. | Widget positioned/rect/style/paint-order test at desktop and compact widths; visual pixel proof still requires screenshots outside this no-screenshot pass. |
| NPF-UI-02 | Idle footer is translated down with `translateY(calc(100% - 10px))` and `opacity: 0.24`; `.is-player-bar-raised` resets transform and opacity to 1. Transform transition is `260ms cubic-bezier(0.2,0,0,1)` and opacity transition is `180ms ease`. | Idle and raised states match the interaction role, transition timing/easing, exact 10px visible idle geometry, and pinning behavior; dialogs or menus pin the bar, queue panel alone does not. | Widget state/timing/rect test; visual pixel proof still requires screenshots outside this no-screenshot pass. |
| NPF-UI-03 | The footer has three semantic columns: left exit, center `MediaControlSurface`, right utility. Full-page CSS forces `.player-center` to column 2 and `.player-utility` to column 3. | Exit, play/pause center, progress row, and More/utility controls keep Electron column ownership. | Geometry test at 1000, 780, and 500 width. |
| NPF-UI-04 | In wide base layout, `.player-bar` grid is `minmax(280px, 0.9fr) minmax(420px, 1fr) minmax(280px, 0.9fr)` with rows `52px 38px`; `.player-center` is a centered column with `gap: 4px`. | Flutter wide full footer keeps a large center player area with transport over progress, preserves the 4px center gap, and keeps utility on the right. | Widget geometry test above 800px. |
| NPF-UI-05 | At compact immersive widths, full footer columns are `80px minmax(174px, 1fr) 80px`; base compact player rows are `74px 28px`; the progress row spans `grid-column: 1 / 4` with 9px side padding. | At 721-800px or nav-minimal compact mode, exit shell, transport, and utility are centered in row 1; progress starts in row 2; play button center equals viewport center; utility remains in the right 80px side; elapsed/duration columns expand to the full footer span rather than staying inside the center column. | Widget geometry test at 780/800. |
| NPF-UI-06 | At <=520px, full footer columns are `68px minmax(136px, 1fr) 68px` with `column-gap: 8px`, `grid-template-rows: 72px 28px`, and `padding: 9px 12px 11px`; base compact player columns are `minmax(112px, 1fr) minmax(136px, 176px) 68px`. Nav-minimal `.player-utility` centers the 74px two-button mode row inside the 68px column. | At 500/520 widths, exit shell, transport, and utility are centered in row 1; progress starts in row 2; center controls do not overflow; utility buttons keep the Electron-centered overhang. | Widget geometry test at 500/520. |
| NPF-UI-07 | Wide transport row is horizontal previous, primary play/pause/loading, next with 26px gap and 52px row height. | Transport order is previous, play/pause, next; play button remains visually centered between skip buttons. | Widget order and center alignment test. |
| NPF-UI-08 | Compact/nav-minimal transport row uses 16px gap, 56px row height, and remains in the center column. | Compact skip buttons stay balanced around primary and do not collide with exit or utility columns. | Widget geometry test at 780 and 500. |
| NPF-UI-09 | Primary play button is 56px in wide layout, 52px in compact/nav-minimal layout, and 48px at `<=520px`. Full-page night/default primary border is `rgba(var(--accent-rgb), 0.42)` with shadow `0 12px 26px rgba(0, 0, 0, 0.32)`; full-page day primary border is transparent with shadow `0 12px 26px var(--accent-shadow)`. Full-page primary hover background is `var(--accent-strong)` in day and night/default. Shared loading state replaces icon with `.player-loading-spinner`: 22px square, 2px border, full ring `rgba(255,255,255,0.38)`, white top border, and `800ms linear` rotation inside the same primary slot. | Flutter primary button follows the Electron size bucket, full-page border/shadow bucket, day/night hover bucket, and shared loading spinner size, two-tone border, stroke, and 800ms linear rotation do not shift layout; NowPlayingFullPage still passes `isLoading=false` for its footer. | Widget size/style/hover/loading-spinner test for normal/loading states. |
| NPF-UI-10 | Skip transport buttons are 36px with 6px padding; previous/next icon drawing is explicitly preserved from the current Flutter implementation per user request and is not changed in this pass. Base utility buttons are 36px. At <=520px compact utility buttons are 34px. Electron base buttons transition `transform 140ms ease` and `background-color 140ms ease`. Full-page night/default non-primary buttons use `rgba(255,255,255,0.86)` foreground; after the Electron `now-playing-full.css` -> `player.css` cascade, non-favorite utility hover resolves to `rgba(var(--accent-rgb),0.18)` and utility active resolves to `rgba(var(--accent-rgb),0.22)`. Full-page day buttons use `var(--text-strong)` foreground and `rgba(var(--accent-rgb),0.1)` hover/active background. Disabled non-primary buttons keep that full-page foreground and dim via the shared `opacity: 0.65` rule. | Previous/next keep the existing Flutter icon drawing while mode, voice, compact volume, and More use the Electron hit-area sizes, transition timing/easing, disabled opacity, and full-page color bucket for their width/theme bucket. | Widget size/style/timing test. |
| NPF-UI-11 | Wide progress row is `44px minmax(0, 1fr) 44px` with elapsed time, slider/loading strip, duration; compact progress row is `42px minmax(0, 1fr) 42px`. The `.media-slider` input itself is `18px` high and centered inside the 36px/28px progress row. Full-page night/default progress labels use `rgba(255,255,255,0.66)`, progress track uses `rgba(255,255,255,0.86)` / `rgba(255,255,255,0.2)`, and 18px slider thumbs use `#fff` with `0 1px 8px rgba(0,0,0,0.38)`. Full-page day labels use `var(--text-muted)`, progress track uses `rgba(91,105,122,0.76)` / `rgba(91,105,122,0.18)`, and 18px slider thumbs use `#fff` with `0 1px 8px rgba(68,88,112,0.32)`. Disabled progress keeps the current track colors under the shared `opacity: 0.65`, and the thumb also applies Electron's `:disabled::-webkit-slider-thumb { opacity: 0.8 }`. | Time labels keep fixed columns and full-page color bucket; the 18px slider input is centered in the progress row, fills the center track width, disabled thumb opacity follows Electron, and the progress row never compresses transport or utility controls. | Widget geometry/style test and seek test. |
| NPF-UI-12 | Full page passes `isLoading={false}` to `MediaControlSurface`, so the immersive footer does not show a loading strip just because the track object is loading. Electron shared `MediaControlSurface` would otherwise swap the primary icon for `.player-loading-spinner` and the slider for `.media-progress-loading`; that shared loading strip is 2px high, the animated segment is 35% wide from `left: -35%`, and `translateX(385%)` travels 385% of that segment width over 1.2s with `ease-in-out`. | Full footer progress row shows normal progress and preserves the same transport/progress geometry while the playback controller track is loading, unless the full-page source explicitly confirms loading behavior. Shared non-full-page loading follows the Electron strip height, 35% segment width, `0.35 * 3.85` travel distance, duration, and curve. | Widget test for no loading strip, no primary spinner, unchanged 1400px transport/progress geometry in full page footer, plus shared loading strip animation test. |
| NPF-UI-13 | Exit button is `player-track now-playing-full-player-exit`; inside it only `player-artwork-shell`, transparent `album-swatch`, and `player-artwork-overlay` with fullscreen-exit icon are rendered. Electron does not render the current song artwork inside this full-page exit shell. | Immersive footer exit slot does not render title, artist, lyrics, song ID, or real album artwork; it only shows the Electron exit shell/swatch/overlay. | Widget content absence/presence test. |
| NPF-UI-14 | Exit artwork shell is 72px in base player rules and 68px in compact/nav-minimal full footer; fullscreen-exit icon is 36px. | Flutter exit shell and icon follow these buckets or equivalent component constraints. | Widget size test. |
| NPF-UI-15 | Exit hover/focus keeps the button background transparent and changes only the inner artwork shell background/border/shadow; shell transition is `background-color 140ms ease, border-color 140ms ease, box-shadow 140ms ease`; night/default shell uses `rgba(255,255,255,0.12)` / `rgba(255,255,255,0.04)` gradient over `rgba(255,255,255,0.08)`, `rgba(255,255,255,0.12)` border, and `0 12px 28px rgba(0,0,0,0.34)`, then hovers to `rgba(255,255,255,0.14)`, `rgba(255,255,255,0.18)` border, and `0 12px 30px rgba(0,0,0,0.3)`; day mode overrides the shell to transparent/no shadow, hovers to `rgba(33,43,58,0.12)` with `rgba(33,43,58,0.08)` border, makes the overlay transparent, sets `backdrop-filter: none`, removes the icon filter, and hides the album swatch; night/default exit overlay remains `rgba(8,12,18,0.42)` with opacity 1 before and during hover, keeps the inherited `.player-artwork-overlay` `backdrop-filter: blur(12px) saturate(145%)`, and its 36px icon keeps `drop-shadow(0 2px 6px rgba(0,0,0,0.34))`. | Hover test sees shell state change, not a full left-column hover card, and the day/night overlay/swatch/icon differences follow the Electron shell cascade. | Widget hover/style/timing test. |
| NPF-UI-16 | Wide utility has two 44px rows: top volume row with compact-volume placeholder hidden, volume toggle, 148px volume slider, favorite, and 14px gaps; bottom mode row with shuffle, repeat, repeat-one, hidden compact mode, optional voice, More, and 14px gaps. Muted wide volume toggle receives `is-active` without resizing the 36px button or 148px slider. Electron's wide volume slider is `.volume-slider-wrap`; because its `input` is `opacity: 0`, full-page `.media-slider` track/thumb overrides do not recolor the visible volume track/thumb. Day/default visible volume uses active `rgba(var(--accent-rgb),0.92)`, inactive `rgba(50,62,78,0.18)`, accent thumb, and `0 1px 4px rgba(0,0,0,0.28)` thumb shadow; night uses active `rgba(var(--accent-rgb),0.95)`, inactive `rgba(203,213,225,0.18)`, and the same thumb. | Wide footer exposes independent wide controls in the same row grouping, order, dimensions, active state, gaps, and keeps volume-slider colors separate from the full-page progress-slider color overrides. | Widget order/visibility/geometry/style test above 800px. |
| NPF-UI-16a | Electron `getVolumeIconName` maps muted to `volumeMuted`, exactly `0` to `volumeOff`, `1..33` to `volumeLow`, `34..66` to `volumeMedium`, and `67..100` to `volume`. Electron `icons.tsx` draws the high-volume icon with three wave paths and the medium-volume icon with two wave paths. Electron `MediaControlButtons` also keeps `liveVolumeValue` and updates it from `VolumeSlider.onLiveValueChange`, so the volume icon changes immediately while the slider is dragged, before any parent playback state round trip. | Full-page wide volume, compact volume, mini mode, and More volume item use the same threshold helper and Electron path-backed custom icon; medium and high volume are visually distinct, and utility-row/More-menu volume icons follow live slider changes instead of waiting for a parent rebuild. | Unit test for shared helper plus widget coverage through the shared controls and compact More. |
| NPF-UI-17 | Compact utility hides `.player-volume-row`; `.player-mode-row button:not(:nth-last-child(-n + 2))` is hidden, so only the final two mode-row buttons remain. Without voice that leaves compact mode plus More; with voice that leaves voice plus More and hides compact mode. | Compact footer keeps only the Electron final-two utility buttons for the current platform, without wide volume/favorite/mode buttons taking space or overflowing the 80px side column. | Widget visibility/geometry test at <=800px and platform variants. |
| NPF-UI-18 | Compact playback-mode button cycles once -> shuffle -> repeat -> repeat-one -> once; context menu or 520ms long press opens checked mode menu above the button from `rect.top - 8`. Electron also calls `setCompactVolumeOpen(false)` before cycling or opening the mode menu. | Existing Flutter menu/flyout can be reused but must keep cycle order, checked item, an 8px above-anchor relation, and the confirmed compact-volume close side effect. | Widget menu and long-press test. |
| NPF-UI-19 | Compact volume is a hidden wide replacement that opens a vertical `mini-mode-volume-popover player-compact-volume-popover` above the compact volume button with `right: -6px` and `bottom: 44px`; the popover is `48px x 116px`, 8px radius, background `rgba(34,34,34,0.96)`, border `rgba(255,255,255,0.18)`, and shadow `0 16px 36px rgba(0,0,0,0.42)`. Its `mini-mode-volume-slider` is a rotated 96px track with a 12px white thumb and no thumb box-shadow. The compact volume button receives `is-active` only while the popover is open; opening compact volume clears the mode menu in Electron. | If compact volume is exposed in Flutter, it opens from the compact utility slot, overhangs 6px to the right of the button, sits 8px above the button top, does not resize the footer, keeps the Electron popover frame styling, keeps the compact thumb shadowless instead of inheriting full-page progress thumb shadow, does not stay active merely because audio is muted, and is mutually exclusive with compact mode interactions according to the confirmed Electron state flow. | Widget/flyout geometry/style test. |
| NPF-UI-20 | Voice assistant appears only when app info platform is `win32`; the flyout is separate from the footer but controlled by the utility button. | Non-Windows Flutter builds omit voice through `supportsVoiceAssistant()` gating; Windows-capable shell keeps voice before More without overflowing the right column, and tapping voice calls the shell flyout/dialog action without replacing More. | Platform-gated widget/action test. |
| NPF-UI-21 | More opens from the More utility button using the full-page button top-left request, clamps the panel within the `window.innerHeight - 8` / `window.innerWidth - 8` bounds, and pins the player bar while `moreMenu !== null`; MusicDialog also pins while `dialogMode !== null`, then closing the dialog clears the pin and allows the 5s auto-hide path to restore the idle `opacity: 0.24` state. | More menu keeps footer raised, preserves the full-page clamp behavior, opens View dialogs with the footer pinned, and closes without leaving a stale pinned state. | Widget state/menu/dialog test. |
| NPF-UI-22 | Day full footer is cover-tinted and light: border `rgba(204,213,224,0.72)`, shadow `0 -18px 56px rgba(68,88,112,0.14)`, radial cover wash `rgba(var(--now-playing-full-cover-rgb),0.24)`, dark utility foreground, accent hover background `rgba(var(--accent-rgb),0.1)`, muted progress text, and day progress track/thumb colors. | Day mode preserves the Electron light full-page frame, control foreground/hover, progress contrast, active states, and control visibility using the CSS-derived color/shadow buckets. | Widget geometry/style test; no screenshot proof in this no-screenshot pass. |
| NPF-UI-23 | Night full footer is dark and cover-tinted. In wide layout the later `player.css` night `.player-bar` rule wins over the earlier full-page default frame: border `var(--night-border)`, shadow `0 -18px 48px rgba(0,0,0,0.34)`, inset `rgba(255,255,255,0.045)`, surface `rgba(17,22,28,0.9)`, cover wash `rgba(var(--player-cover-rgb),0.22)` to transparent at `46%`, and highlight `rgba(255,255,255,0.055)` to `rgba(var(--accent-rgb),0.12)`. Regular transport/utility foreground comes from the later `body.night-mode .app-shell .transport-button/.player-utility button` cascade (`--night-text`, i.e. `rgba(246,249,252,0.94)`), with cascaded accent utility hover/active backgrounds, muted progress text, and night progress track/thumb colors. | Night mode preserves the Electron dark full-page frame, control foreground/hover, progress contrast, active states, and control visibility using the CSS-derived color/shadow buckets. | Widget style test; no screenshot proof in this no-screenshot pass. |
| NPF-UI-24 | Favorite active state uses `var(--favorite-accent)`, keeps transparent background while active, and applies full-page hover background overrides: night/default `rgba(255,255,255,0.22)`, day `rgba(var(--accent-rgb),0.1)`. | Favorite active state is not confused with shuffle/repeat active state in day or night full-page footer. | Widget style test. |
| NPF-UI-25 | Wide full-page volume tooltip overrides the shared tooltip with border `rgba(255,255,255,0.18)`, background `rgba(20,24,30,0.88)`, shadow `0 10px 24px rgba(0,0,0,0.34)`, white text, and a matching 7px rotated arrow. The horizontal tooltip is anchored by `.volume-slider-tooltip { bottom: calc(100% + 8px) }`, so its bottom sits 8px above the 22px slider wrap. Compact volume uses `mini-mode-volume-popover player-compact-volume-popover`; because `mini-mode.css` loads after `now-playing-full.css`, its tooltip uses background `rgba(34,34,34,0.96)`, shadow `0 8px 18px rgba(0,0,0,0.34)`, and shows on mount. Electron `VolumeSlider` keeps tooltip visible for 900ms on mount/focus but 650ms after pointer release. | Wide and compact full-page volume tooltips follow their Electron cascade-specific color/shadow buckets and the 8px above-slider horizontal anchor without resizing the footer; compact tooltip appears immediately when the compact volume popover opens; drag-release tooltip timing follows Electron. | Widget style/timing test. |
| NPF-UI-26 | Disabled state comes from no current song; transport, progress, volume, compact volume, favorite, mode controls, and More-contained substitutes must be disabled/hidden according to Electron behavior. Electron applies `transform: none` and `opacity: 0.65` to disabled transport, utility, volume, and progress inputs, and the slider thumb also applies `opacity: 0.8`. Base disabled transport/utility buttons use `rgba(255,255,255,0.08)`, while full-page primary keeps the more-specific `.now-playing-full-player-bar .transport-button.primary` accent background, day transparent border, full-page primary shadow, and dims through disabled opacity. | Disabled controls are visibly disabled, do not hover-lift, full-page primary keeps Electron accent background/border/shadow under disabled opacity, non-primary disabled controls use disabled surface, favorite is hidden without `trackId`, and disabled sliders apply the Electron active-track, inactive-track, and thumb opacity values without calling callbacks. | Widget disabled-state test. |

## Not Confirmed For This Target

| Item | Reason |
| --- | --- |
| Adding the bottom-player global View entries See Artist, See Album, Local File into the full page More menu | The full page currently builds its own More menu; only Music Info, Lyrics, and Album Art are confirmed in that full-page menu path. |
| Forcing a click through an open compact playback-mode menu to the compact volume button | Electron source shows the compact volume button click calls `setModeMenu(null)` before toggling volume, but at the tested compact utility geometry the mode menu panel covers the compact volume button. Without Electron runtime evidence that this covered click should pass through the panel, Flutter must not add a synthetic pass-through fallback. |
| Pixel-perfect visual completion | Requires paired Electron and Flutter screenshots with the same song, theme, width, and platform. |

## Verification Commands

```sh
dart analyze lib/src/playback/immersive_mode_page.dart lib/src/playback/media_control.dart test/now_playing_page_test.dart
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact footer reuses nav-minimal surface"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact footer matches Electron column geometry"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact footer keeps voice and More inside utility"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage nav-minimal footer keeps voice and More in 80px utility"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage mid-width footer uses compact utility like Electron"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage utility switches at Electron 1200px breakpoint"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage wide footer uses Electron 0.9fr side columns"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage keeps wide layout with compact menu at 780px"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage wide More matches Electron queue and current song items"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact More includes Electron utility substitutes"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact mode context menu opens like Electron"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact mode cycles in Electron order"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact mode menu checks current Electron item"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact mode long press opens Electron menu"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage wide mode row keeps Electron voice slot"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage compact More disables Favorite without current song"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage no active track disables full footer volume like Electron"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage Clear Now Playing exits fullscreen and clears queue"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage More pins player bar while menu is open"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage more view dialog pins player bar"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage favorite active style follows Electron day and night"
flutter test --no-pub test/now_playing_page_test.dart --plain-name "ImmersiveModePage primary and utility hover follow Electron cascade"
flutter test --no-pub test/media_control_widget_test.dart --plain-name "VolumeSlider keeps tooltip 650ms after drag release like Electron"
flutter test --no-pub test/media_control_widget_test.dart --plain-name "compact playback mode button opens Electron-style long press menu"
flutter test --no-pub test/macos_privacy_metadata_test.dart --plain-name "voice assistant availability follows Electron win32 gate"
```

Screenshot proof remains required before claiming pixel-level visual alignment.
