import 'package:flutter/material.dart';

const nowPlayingFullPlayerHeight = 120.0;
const nowPlayingFullPlayerIdleVisibleHeight = 10.0;
const nowPlayingFullPlayerIdleSlideOffset =
    (nowPlayingFullPlayerHeight - nowPlayingFullPlayerIdleVisibleHeight) /
    nowPlayingFullPlayerHeight;
const nowPlayingFullPlayerTopRadius = 18.0;
const nowPlayingFullLayoutCompactBreakpoint = 760.0;
const nowPlayingFullImmersiveCompactBreakpoint = 800.0;
const nowPlayingFullQueueRowHeight = 78.0;

const nowPlayingFullMiniModeWindowSize = Size(360, 360);
